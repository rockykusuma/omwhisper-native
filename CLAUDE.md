# OmWhisper Native — Claude Code Context

> Read this file at the start of every session to get full project context.

Native Swift rewrite of OmWhisper (menu-bar dictation for macOS), versioned **2.0**.
Supersedes the Tauri app (`github.com/rockykusuma/omwhisper` — frozen after native parity,
kept as the Windows release and as a spec/behavior reference — see `docs/NATIVE_MIGRATION_PLAN.md`).

---

## What This Project Is

- **Platform**: macOS 26+, Apple Silicon only. No availability gating, no fallback paths —
  SpeechTranscriber and Foundation Models are assumed present.
- **UI**: SwiftUI-first, AppKit where the platform demands it (`MenuBarExtra`, `NSPanel`
  non-activating overlay, `CGEventTap` for PTT).
- **Distribution**: Developer ID + notarization, direct download — **not** the App Store.
  The app is NOT sandboxed: Accessibility-based paste (`CGEventPost`) and global event taps
  require entitlements the App Store sandbox forbids.
- **Bundle ID**: `com.omwhisper.mac` (imports transcription history from the old app's
  `com.omwhisper.app` data dir on first run — see M2).
- **Apple Developer Team ID**: `Y87BZN47C5` (already set in `project.pbxproj`).
- **Language mode**: Swift 6 (`SWIFT_VERSION = 6.0`, all targets), with
  `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` + `SWIFT_APPROACHABLE_CONCURRENCY = YES` — every
  unannotated declaration is `@MainActor` by default. See "Concurrency" below before writing
  anything that touches audio callbacks or background work.
- **Naming gotcha**: the Xcode *target* (and its scheme) is named `omwhisper-native` —
  `PRODUCT_NAME` is set to `OmWhisper` (so the built app/bundle display as "OmWhisper"), but
  `xcodebuild -scheme ...` and CI must use `omwhisper-native`. The scheme is committed at
  `omwhisper-native.xcodeproj/xcshareddata/xcschemes/omwhisper-native.xcscheme` — it is NOT
  autocreated on a fresh clone, so don't delete it.

## North Star

Match Claude Code's dictation feel — server-side streaming ASR, words appearing while you
speak — while keeping OmWhisper's actual differentiator: **user choice of local vs. cloud
transcription/polish backends**, not "always local." macOS is the primary target; Windows
stays on the frozen Tauri release.

Sign-off criteria (gate M1 and M4):
1. Live dimmed partials in the overlay, < 1s behind speech, finalized on stop.
2. Key-release → punctuated text pasted in the target app in < 700ms typical.
3. Punctuation/capitalization work with zero configuration (in-engine).
4. Technical vocabulary respected via context hints (custom vocab → engine biasing / cloud keyterms).
5. PTT starts instantly on keydown.

**Measured status (2026-07-07, live on-device, 9 dictation runs via `Logger(category: "Latency")`):**
- **#2 passes solidly**: stop-to-paste measured 108ms–697ms across every run, always under the 700ms bar.
- **#3 confirmed** live — pasted output shows correct capitalization/terminal punctuation with zero configuration.
- **#5 shipped** — Fn/Globe push-to-talk (`PushToTalkMonitor`), feels instant in practice (not separately latency-measured).
- **#1 not consistently met**: early runs measured ~4s and were initially suspected as an engine/setup bug — root-caused instead to human speech-onset delay after pressing the hotkey (the engine's own setup chain measured 8–42ms; mic buffers reach the analyzer within ~0.1s — see git history on `AppleEngine.swift`, since reverted). With speech content pre-decided to remove "what do I say" think-time, repeat runs landed at 1.3–2.0s, with one outlier at 0.89s — so sub-1s is achievable but not the norm. The gap is likely a mix of unavoidable human reaction time and the model's own minimum buffering before its first hypothesis; the current instrumentation (timed from recording-start, not actual speech-onset) can't separate the two. Untangling further needs VAD-based speech-onset detection — deferred, not pursued.
- **#4 shipped (2026-07-07)** — Vocabulary settings tab (custom words + whole-word
  replacements + fuzzy-match toggle), biasing `AppleEngine` via `AnalysisContext.contextualStrings`
  (the real mechanism — confirmed from the macOS 26 SDK's `Speech.swiftinterface`;
  `SpeechTranscriber` itself has no vocab parameter). Parakeet/cloud biasing follows the
  same `TranscriptionEngine.transcribe(_:vocabulary:)` parameter when those engines land (M4).

## Tech Stack

| Layer | Technology |
|-------|-----------|
| UI | SwiftUI + AppKit (`MenuBarExtra`, `NSPanel`, `CGEventTap`) |
| State | Single `@Observable` `AppState` — no per-view settings read-modify-write (fixes the Tauri app's settings-sync debt) |
| Transcription | `TranscriptionEngine` protocol → `AppleEngine` (SpeechTranscriber, default) / `ParakeetEngine` (FluidAudio CoreML, optional) / `CloudEngine` (streaming WebSocket) |
| Polish | `PolishBackend` protocol → `SystemLLM` (Foundation Models, default) / `Ollama` / `CloudLLM` (OpenAI-compatible, keys in Keychain) |
| Capture | AVAudioEngine (`AudioCapture.swift`) |
| Paste | `CGEventPost` Cmd+V + clipboard save/restore (`PasteService.swift`) |
| History | SQLite (GRDB, M2) — same schema as the Tauri app, with a one-time importer |
| Updates | Sparkle (replaces `updater.rs` + `version.json`) |

## Project Structure

```
omwhisper-native/
├── omwhisper-native.xcodeproj/
│   ├── xcshareddata/xcschemes/omwhisper-native.xcscheme  # committed — see naming gotcha above
│   └── project.pbxproj                 # file-system-synced groups — no manual edits to add/remove files
├── omwhisper-native/                   # App target
│   ├── OmWhisperApp.swift              # @main — MenuBarExtra + Settings scene; starts the global hotkey
│   ├── AppState.swift                  # @MainActor @Observable root state + M1 core-loop orchestration
│   ├── Transcription/
│   │   ├── TranscriptionEngine.swift   # Core contract: protocol + TranscriptEvent (.partial/.final)
│   │   ├── AppleEngine.swift           # Default engine: SpeechAnalyzer + SpeechTranscriber (macOS 26)
│   │   └── BufferConverter.swift       # AVAudioConverter wrapper: mic format -> analyzer's format
│   ├── Capture/
│   │   └── AudioCapture.swift          # AVAudioEngine mic capture → AsyncStream<AVAudioPCMBuffer>
│   ├── Hotkeys/
│   │   └── GlobalHotkey.swift          # System-wide Cmd+Shift+V via NSEvent monitors
│   ├── Paste/
│   │   └── PasteService.swift          # Frontmost-app capture, CGEventPost paste, clipboard restore
│   ├── Polish/
│   │   └── PolishBackend.swift         # AI text-polish contract (stub — wired in M3)
│   ├── UI/
│   │   ├── MenuContent.swift           # Menu-bar dropdown (Start/Stop, Settings, Quit)
│   │   ├── SettingsView.swift          # Settings window (General tab only so far)
│   │   ├── OverlayPanel.swift          # Non-activating NSPanel HUD, bottom-center, never steals focus
│   │   └── OverlayView.swift           # SwiftUI content: status dot + live partial/final transcript
│   └── Assets.xcassets
├── omwhisper-nativeTests/              # Swift Testing (@testable import OmWhisper)
├── omwhisper-nativeUITests/            # XCUITest
├── docs/
│   ├── NATIVE_MIGRATION_PLAN.md        # Milestones M0–M5, architecture, risks, parity checklist (Appendix B)
│   └── DICTATION_GAP_ANALYSIS.md       # Why SpeechTranscriber vs. the Tauri app's pipeline
├── scripts/
│   └── build-release.sh                # xcodebuild archive → notarytool → .dmg → SHA-256
└── .github/workflows/ci.yml            # build + test on macOS runner
```

**Important**: the Xcode groups are **file-system-synchronized** (`PBXFileSystemSynchronizedRootGroup`).
Adding, moving, or deleting a `.swift` file on disk is enough — Xcode picks it up automatically.
Do not hand-edit `project.pbxproj` file references.

## Concurrency

The project sets `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor`: every type/function without an
explicit annotation is implicitly `@MainActor`-isolated. This is the right default for UI and
state (`AppState`, `GlobalHotkey`, `OverlayPanel` are all effectively `@MainActor`), but it is
**wrong** for anything that genuinely runs off the main thread — most importantly
`AVAudioEngine`'s tap callback, which fires on its own real-time render thread regardless of
what Swift infers. Two escape hatches are used deliberately, and any new code touching audio
buffers or background transcription work should follow the same pattern:

- **`nonisolated`** on functions/properties that must run (or be callable) off MainActor —
  e.g. `AudioCapture.start()/stop()`, `AppleEngine.transcribe(_:)`. A `nonisolated func`'s
  `Task { }` runs on the cooperative thread pool, not MainActor.
- **`nonisolated(unsafe)`** on the small pieces of mutable state those nonisolated code paths
  touch, paired with a real lock (`OSAllocatedUnfairLock` in `AudioCapture`) — never rely on
  actor isolation to protect state a background thread writes to.
- **`@preconcurrency import AVFoundation`** wherever `AVAudioPCMBuffer`/`AVAudioConverter`
  cross a `Task` boundary — these AVFoundation types are not `Sendable`, and we've asserted the
  actual safety invariant ourselves (single producer → single consumer per buffer) rather than
  fighting the compiler. `Speech` (SpeechAnalyzer/SpeechTranscriber) is a newer, concurrency-first
  API and needs no such treatment.

If a build error mentions an actor-isolation or Sendable violation in `AudioCapture`,
`BufferConverter`, or `AppleEngine`, check that the `nonisolated`/`nonisolated(unsafe)` markers
are still in place before assuming the fix is elsewhere.

## Key Contracts

- `TranscriptionEngine` — `func transcribe(_ audio: AsyncStream<AVAudioPCMBuffer>, vocabulary: [String]) -> AsyncThrowingStream<TranscriptEvent, Error>`,
  where `TranscriptEvent = .partial(String) | .final(String)`. SpeechTranscriber's volatile/finalized
  results map directly onto this; Parakeet and cloud engines conform to the same shape. This
  replaces the Tauri app's VAD worker, sentinel channels, and two-pass decode entirely.
  `vocabulary` is read fresh at the start of each call (engines are rebuilt per session
  anyway) and biases `AppleEngine` via `AnalysisContext.contextualStrings`. Word replacements
  and fuzzy correction (`Vocabulary/VocabularyProcessing.swift`) are engine-agnostic
  post-processing, applied in `AppState` to both `.partial` and `.final` text — not part of
  the engine contract itself.
- `AppState` — the single `@Observable` store. Settings persist via `UserDefaults` (with a
  one-time importer for the old `settings.json` planned in M2). No component does its own
  read-modify-write of settings. Also owns the M1 core loop: `toggleDictation()` →
  `startDictation()`/`stopDictation()` wire `AudioCapture` → `AppleEngine` → `OverlayPanel` → `PasteService`.
- `PolishBackend` — `func polish(_ text: String, style: PolishStyle) async throws -> String`.

## Milestones (see `docs/NATIVE_MIGRATION_PLAN.md` for full detail)

- **M0 — Repo + pipeline** ✅ done: Xcode project, package/folder skeleton, CI, committed shared
  scheme, build-release script. Confirmed building and running in Xcode (menu bar icon shows).
- **M1 — Core loop MVP** (in progress): hotkey → capture → SpeechTranscriber streaming → live
  overlay partials → paste on stop. Implemented and **now compiles clean** (Xcode 26.6 / macOS 26.5
  SDK, commit `410fd2a` fixed the first-compile Swift 6 concurrency errors). Not yet live-smoke-tested
  or measured against the sign-off criteria (partial lag, stop-to-paste latency, accuracy spike vs.
  Parakeet) — that's the next step.
- **M2 — Daily-driver parity**: Settings UI, PTT, history + importer, vocabulary UI, sounds,
  launch-at-login, Sparkle, onboarding.
- **M3 — AI polish**: Foundation Models default backend, styles system, Ollama + cloud backends,
  Keychain key storage, smart dictation (Cmd+Shift+B).
- **M4 — Backend flexibility (the USP)**: cloud streaming engine, Parakeet CoreML optional engine,
  per-backend selector UI.
- **M5 — Beta → release → freeze**: parity audit, landing page + version.json migration, beta
  soak, then freeze the Tauri repo.
- **Phase S — Smriti integration** (decided 2026-07-07, see `docs/SMRITI_INTEGRATION_PLAN.md`):
  port Smriti's features into this app (smriti repo stays untouched; copy or rewrite here).
  S1 memory core (AX capture + FTS5 store) · S2 context-aware dictation (screen terms →
  `contextualStrings`) ⭐ · S3 meeting intelligence (consent-first, our engines) · S4 voice
  reply assist · S5 memory surfacing + MCP server · S6 website update (OmWhisper-centric).
  Order (priorities set 2026-07-07): M2 → S2 → M3 → S3 → S4 → S1 → S5 → M4 → S6/M5 —
  S4 ships window-context-only (no stored memory), gains memory snippets when S1 lands.
  "Learning the user" scope within Phase S = writing tone only (tone.md, in S4). All
  Smriti-derived features are OFF by default.
- **Phase F — Frontier features** (decided 2026-07-07, all approved; see
  `docs/FRONTIER_FEATURES_PLAN.md`): post-2.0 differentiator waves — 2.1: F5 brain-dump
  mode, F6 per-app output shaping + voice snippets, F7 vocab packs (after M3) · 2.2:
  F1 voice editing ("scratch that"), F4 cross-lingual dictation (speak Telugu/Hinglish →
  polished English) · 2.3: F3 only-my-voice (FluidAudio speaker ID), F2 whisper mode
  (after M4). F2's whispered-WER benchmark runs early as a parallel research spike.
  None block the committed spine or M5.
- **Phase 3 — Digital Twin** (decided 2026-07-07, after S5; same plan doc): nightly local
  distillation of the day's memory into a facts store (confidence/recency/source) +
  editable `profile.md` → twin-grounded replies & memory chat, auto-vocabulary learning
  from corrections, habit-level nudges. T1 profile distillation → T2 grounding →
  T3 auto-vocab → T4 habits. Local-only, inspectable, off by default; framed as "writes
  like you, knows what you know" — never as cloned judgment.

## Progress Tracker

| Milestone | Status | Notes |
|-----------|--------|-------|
| M0 — Repo + pipeline | ✅ Done | pbxproj configured (bundle ID, macOS 26.0 target, sandbox off, Swift 6 language mode); source skeleton; CLAUDE.md/README/CI/build-release.sh; committed shared xcscheme (`omwhisper-native`, not autocreated — see naming gotcha). Confirmed build+run in Xcode. |
| M1 — Core loop MVP | 🔶 Live, sign-off pending on #1 | Running live end-to-end (2026-07-07) after fixing three session-blocking bugs: SwiftUI `MenuBarExtra` silently dropping real clicks on macOS 26 (→ AppKit `NSStatusItem`); Hardened Runtime with no `audio-input` entitlement (mic permission silently denied, no prompt); an off-MainActor `SFSpeechRecognizer` callback crashing under Swift 6 isolation checks. Pulled forward from M2: push-to-talk (`PushToTalkMonitor`, hold Fn/Globe), start/stop sounds, paste-reliability hardening (Accessibility-gated, no more silent no-ops), richer menu-bar icon states. See "Measured status" under Sign-off criteria above for the numbers — #2/#3/#5 pass, #1 doesn't consistently, #4 shipped below. WER spike vs. Parakeet still owed. |
| M2 — Daily-driver parity | 🔶 In progress | PTT/sounds/paste-hardening/menu-bar states already shipped under M1 (see above). Vocabulary UI + engine biasing shipped (2026-07-07): `Vocabulary/VocabularyProcessing.swift` (whole-word replacements + length-gated bounded-Levenshtein fuzzy correction, ported from the Tauri app's `engine.rs`/`vocab_correct.rs`), `UI/VocabularySettingsView.swift`, `SettingsView` restructured into a `TabView` (General/Vocabulary). History + importer shipped (2026-07-07), per `docs/superpowers/specs/2026-07-07-history-importer-design.md`: GRDB added as SPM dependency (linked to both the app and test targets); `History/HistoryStore.swift` (GRDB `DatabaseQueue`, `transcriptions` table via `DatabaseMigrator`, record/fetchPage/search/delete/deleteAll/deleteOlderThan/storageInfo/exportAll — txt/md/json ports of the Rust `export_history`); `History/LegacyHistoryImporter.swift` (one-time read-only copy from the old app's `com.omwhisper.app/history.db`, gated by `hasImportedLegacyHistory`); `UI/HistoryView.swift` (paginated/searchable list, tap-to-expand rows, bulk-select delete, txt/md/json export via `NSSavePanel`, Clear All, Auto-Delete-After stepper) wired as a new `Window("History", id: "history")` scene + menu-bar item. `HistoryStore`/`TranscriptionEntry`/`LegacyHistoryImporter` are `nonisolated` (GRDB I/O has no MainActor affinity — see AppState concurrency note); `AppState.init()` opens the store and runs import+cleanup via a `nonisolated` background-Task launcher, matching the `AudioCapture` pattern. 33 new tests (`HistoryStoreTests`, `LegacyHistoryImporterTests`), all passing; live-verified against the real Tauri app's history.db (94 rows imported correctly, rendered in the History window). Also fixed a bug the `Window` scene introduced: macOS's window-state restoration (triggered by Xcode's Stop button killing the process uncleanly) was auto-reopening Settings/History at next launch, and closing that window then quit the whole app — fixed via `.defaultLaunchBehavior(.suppressed)` on both scenes plus `AppDelegate.applicationShouldTerminateAfterLastWindowClosed` returning `false` (menu-bar/`LSUIElement` apps must survive their last window closing). Launch-at-login shipped (2026-07-07): `AppState.launchAtLogin` wraps `ServiceManagement.SMAppService.mainApp` directly (no UserDefaults mirroring — the system login-item registry is its own source of truth), toggle added to `GeneralSettingsView`; live-verified both directions via `sfltool dumpbtm` (`disposition` flips enabled ⇄ disabled). Audio/About settings tabs shipped (2026-07-07), ported from the old app's Settings.tsx Audio/About sections (researched directly, not assumed — analytics/crash-reporting toggles, feedback form, and update UI deliberately dropped: no backing infra in this app, and updates are Sparkle's job). `Capture/AudioCapture.swift` gained mic device selection: `availableInputDevices()` via `AVCaptureDevice.DiscoverySession`, `start(preferredDeviceUID:)` resolves the UID to a `AudioDeviceID` via `AudioObjectGetPropertyData`/`kAudioDevicePropertyDeviceUID` and applies it with `AudioUnitSetProperty(kAudioOutputUnitProperty_CurrentDevice)` before `engine.start()` (AVAudioEngine has no higher-level device-picker API on macOS) — persisted UID, not name (old app matched by name, which breaks for two identical mic models). `AppState.soundVolume` (default 0.2, matches old app) applied via `NSSound.volume` in `SoundPlayer`. `UI/AudioSettingsView.swift` (input device picker, sound toggle + volume slider — no live level meter, matching the old app which only had one in the recording overlay, not Settings) and `UI/AboutSettingsView.swift` (version from `CFBundleShortVersionString`/`CFBundleVersion`, doc link, footer credit) added as new `SettingsView` tabs; "Recording sounds" toggle moved from General into Audio. Live-verified on real hardware: device picker lists actual devices (USB mic, Continuity iPhone mic, built-in), explicitly selecting the built-in mic and running a real start/stop dictation cycle worked end-to-end (proves the CoreAudio device-ID resolution path, not just the picker UI). Sparkle runtime wiring shipped (2026-07-07) — code only, not yet live (no signing key/appcast published, see below). Added as SPM dependency (app target only). `SPUStandardUpdaterController` on `AppDelegate` + "Check for Updates…" menu item (enabled state bound to `updater.canCheckForUpdates`). `SUFeedURL` = `https://omwhisper.in/appcast.xml` — chosen over a bare GitHub Pages URL because the Tauri app is being deprecated 2026-07-08 and `omwhisper.in` becomes this app's actual home (see `tauri-app-deprecation` memory); confirmed via research that `omWhisperWebApp`'s existing `version.json`/`updater.json` are Tauri-updater-format (JSON + minisign sig) and structurally incompatible with Sparkle's appcast.xml + EdDSA, so nothing there was reusable beyond the hosting slot. Hit and fixed a real Xcode issue along the way: `GENERATE_INFOPLIST_FILE`'s `INFOPLIST_KEY_*` synthesis only recognizes Apple's own known keys — a third-party key like `SUFeedURL` resolves fine as a build setting but is silently dropped from the generated Info.plist. Fixed with a `PBXShellScriptBuildPhase` ("Patch Info.plist (custom third-party keys)") that PlistBuddy-injects it after generation; needed `ENABLE_USER_SCRIPT_SANDBOXING = NO` on the app target too (Xcode's script-phase sandbox otherwise blocks the write with "Operation not permitted", and declaring the same path as a formal `outputPaths` entry instead causes a "Multiple commands produce Info.plist" conflict with the real generator). Live-verified: menu item appears, clicking it doesn't crash (fails gracefully — no appcast published yet), Info.plist correctly contains `SUFeedURL` alongside every pre-existing key. **Explicitly not done yet** (needs the user's go-ahead first — semi-irreversible/public): generating the real EdDSA signing keypair (`SUPublicEDKey` not in Info.plist), publishing an actual `appcast.xml`, and the `build-release.sh` `generate_appcast` step. Onboarding still not started — `docs/onboarding-prototype.html` exists in the working tree (not yet reviewed/wired in), deferred by user's choice. |
| M3 — AI polish | 🔶 Sub-project 1 shipped, sub-project 2 not started | Split into two sub-projects during brainstorming — sub-project 1 (styles + `SystemLLM`/Foundation Models backend + Smart Dictation + Polish Selected Text) shipped 2026-07-07, per `docs/superpowers/specs/2026-07-07-m3-core-ai-polish-design.md` and `docs/superpowers/plans/2026-07-07-m3-core-ai-polish.md`, executed inline (7 tasks) in an isolated worktree. `Polish/PolishStyles.swift`: 7 built-in styles (Professional/Casual/Concise/Translate/Email Format/Meeting Notes/Smart Correct — correcting a stale "6 built-ins" reference elsewhere; Smart Correct was added later upstream in the old Tauri app), fixed UUIDs so `activePolishStyleID` survives relaunches, one canonical prompt per style (not the old app's short/blunt-for-tiny-model variant — Foundation Models is meaningfully more capable than that app's bundled 0.5B GGUF). `Polish/SystemLLM.swift`: wraps `LanguageModelSession`/`SystemLanguageModel` (API verified directly against the macOS 26 SDK's `FoundationModels.swiftinterface`, not guessed), fresh session per call, 5s timeout race via `withThrowingTaskGroup`. `AppState` gained `polishBackend`/`activePolishStyleID`/`translateTargetLanguage`/`customPolishStyles` settings, `beginSmartDictation()` (Cmd+Shift+B, refactored `toggleDictation()` into a shared `toggleOrStop(smart:)` rather than duplicating the state machine), `beginPolishSelectedText()` (Cmd+Shift+P), and a unified fallback: any reason polish doesn't produce text (backend Disabled, Apple Intelligence off, model error, timeout) pastes the *original* text unconditionally — fixing a real gap in the old app, where one specific failure mode silently dropped the user's dictated text instead of falling back. `Paste/PasteService.swift` gained `copySelection()` (simulated Cmd+C, mirroring the existing `paste()` save/restore pattern in reverse — chosen over `kAXSelectedTextAttribute`, which is inconsistently supported across third-party apps). New `POLISHING` overlay sub-phase reuses the existing closed color palette (no new colors, per `OVERLAY_SPEC.md` §2); the orb needed zero changes since it already renders calm at `dictation == .idle`, which is Polish Selected Text's state throughout (confirmed by reading `OmOrbView`'s existing floor/glow functions before assuming a change was needed). New `UI/AISettingsView.swift` tab (backend picker, style picker, target-language picker, custom style CRUD). 14 new tests, all passing (78 total). Two real, non-obvious bugs found via live verification (by the user, not caught by the automated suite or by my own AX-based headless testing, which repeatedly proved unreliable this session): (1) `PasteService.copySelection()` compared the freshly-copied text against the *prior* clipboard content and treated a match as "nothing selected" — so re-selecting text that happened to already be on the clipboard silently no-op'd Polish Selected Text even though a real copy had just occurred; fixed to rely solely on `NSPasteboard.changeCount`, which increments on every write regardless of content equality. (2) The AI tab's backend radio buttons didn't visually update until switching Settings tabs and back — root-caused to `polishBackend`/`activePolishStyleID`/`translateTargetLanguage`/`customPolishStyles` being plain computed properties over `UserDefaults` with no stored backing: `@Observable` only auto-instruments *stored* properties, so a plain get/set computed property never fires a change notification on its own. Invisible for every pre-existing Toggle-bound setting in this file (a `Toggle`'s own click animation looks correct regardless of whether the view body actually re-renders), but a `.pickerStyle(.radioGroup)` `Picker` needs a genuine Observation signal to re-highlight the selected option. Fixed the four new settings with manual `access(keyPath:)`/`withMutation(keyPath:)` calls — Apple's documented pattern for `@Observable` properties backed by external storage. **This same gap likely exists on every pre-existing UserDefaults-backed setting in `AppState.swift` too** (`pasteAfterStop`, `soundEnabled`, `fuzzyVocabCorrection`, `contextAwareDictationEnabled`, etc.) — masked there by `Toggle`'s forgiving behavior, not actually fixed; worth a dedicated follow-up pass. Live-verified end-to-end on real hardware by the user directly (automated AX-based verification proved too flaky this session — see below): Smart Dictation with backend Disabled pastes raw text unchanged; Smart Dictation with backend System + a filler/self-correction-laden sentence produces genuinely cleaned-up text (fillers removed, self-correction resolved) with the overlay showing `POLISHING`; Polish Selected Text replaces a selection in place with polished text and no-ops cleanly when nothing is selected; no stray processes left running. Sub-project 2 (Ollama + Cloud backends, Keychain API key storage, test-connection UI) intentionally deferred, not started. |
| M4 — Backend flexibility | 🔶 M4.1 shipped, M4.2 (CloudEngine) not started | M4 was split during brainstorming into M4.1 (this — the local CoreML alternative engine, no new privacy surface) and M4.2 (CloudEngine — provider choice, redaction on egress, Keychain key storage), matching this project's established pattern for multi-subsystem milestones. M4.1 — ParakeetEngine + backend selector UI shipped 2026-07-09, per `docs/superpowers/specs/2026-07-09-m4-1-parakeet-engine-design.md` and `docs/superpowers/plans/2026-07-09-m4-1-parakeet-engine.md`, executed inline (5 tasks) in `.worktrees/m4-1-parakeet-engine`. Directly addresses the migration plan's flagged risk #1 (SpeechTranscriber accuracy is "the big unknown," with ParakeetEngine as the documented promotion candidate) — a formal WER comparison between the two engines was explicitly deferred per brainstorming decision; only the engine + picker shipped. FluidAudio (new SPM dependency, app-target only, mirrors Sparkle's existing app-only pattern) added via hand-edited `project.pbxproj` `XCRemoteSwiftPackageReference`/`XCSwiftPackageProductDependency` entries, following GRDB/Sparkle's exact existing pattern (no `Package.swift` in this project). Research into FluidAudio's actual API (cloning the repo and reading source directly, not from training-data memory) found a real fork: `StreamingAsrManager`-conforming engines (EOU/Nemotron variants) have genuine incremental streaming via a partial-transcript callback but **no vocabulary biasing**; `SlidingWindowAsrManager` — despite its own doc comment explicitly saying it does *not* conform to `StreamingAsrManager` ("offline encoder with overlapping windows, not a cache-aware streaming architecture") — actually has both: `streamAudio(_:)`/`transcriptionUpdates: AsyncStream<SlidingWindowTranscriptionUpdate>` for live partials (`isConfirmed` maps directly onto this app's `.final`/`.partial`) AND `configureVocabularyBoosting(vocabulary:ctcModels:)`. Chose `SlidingWindowAsrManager` specifically because it's the only one satisfying both this app's streaming-partials UX goal and sign-off criterion #4 (vocabulary respected). `Transcription/ParakeetEngine.swift`: unlike `AppleEngine` (a stateless struct recreated per session), a `final class` holding one persistent, lazily-created `SlidingWindowAsrManager` for the engine's lifetime — loading Parakeet's CoreML models is expensive (multi-second) and must not repeat per dictation session; `reset()` (not model reload) is the per-session boundary. Vocabulary boosting needs a *second*, separate model (`CtcModels`, the CTC keyword-spotting model) — only downloaded lazily when the caller actually has custom vocabulary, so most Parakeet users never pay that extra download. Mutable `manager`/`ctcModels` state guarded by `OSAllocatedUnfairLock` (matching `AudioCapture`'s established pattern) rather than actor isolation, since `transcribe()` must stay `nonisolated` per the `TranscriptionEngine` protocol. `AppState` gained `engineKind: EngineKind` (default `.apple` — ships an opt-in alternative, not a default-experience change), `parakeetEngine` (not private — the Settings UI reads `isReady`/calls `ensureModelsLoaded` directly), and a computed `activeEngine` replacing the old fixed `engine` property at the one `engine.transcribe(...)` call site. New `UI/TranscriptionSettingsView.swift` tab: radio picker (Apple/Parakeet selectable, Cloud shown disabled "coming in a future update"), a "Download Parakeet Model" button wired to FluidAudio's own `loadModels(progressHandler:)` (real download progress, no custom SHA-pinned downloader needed unlike the old Tauri app's hand-rolled ONNX Parakeet downloader — architecturally unrelated anyway, reference-only). 2 new tests (`ParakeetEngineTests`, testing the pure `isConfirmed`→`.final`/`.partial` mapping function directly rather than importing FluidAudio into the test target — kept FluidAudio app-target-only end to end), all passing (176 total in the full suite). Three real, non-obvious bugs found and fixed during implementation, none anticipated by the design or plan: (1) the SPM version requirement (`minimumVersion = 0.4.0`) resolved to a real but *stale* tagged release from September 2025 that predates `SlidingWindowAsrManager`/vocabulary boosting entirely — caught immediately by a real "cannot find type" build failure, root-caused to the brainstorming research having read FluidAudio's `main` branch via GitHub's contents API rather than the actual tagged release a real SPM dependency resolves to; fixed by checking all available tags, confirming the same API exists identically at the latest (`v0.15.5`), and bumping the version requirement — a concrete lesson that "read the default branch" and "read what a versioned dependency will actually resolve to" are different research questions. (2) `ParakeetEngine` (like `MCPServer` in S5.2) silently became `@MainActor` under this project's `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` default, breaking `nonisolated func transcribe` and the pure `mapUpdate` test calls — fixed by marking the class `nonisolated`. (3) A latent, pre-existing gap: `TranscriptEvent`/`EngineKind` (in the original M1-era `TranscriptionEngine.swift`) were never marked `nonisolated`, masked until this sub-project's tests were the first code in the whole project to actually compare two `TranscriptEvent` values with `==` from a nonisolated context — fixed by adding `nonisolated` to both. Live-verified end-to-end on real hardware: a normal dictation cycle with no Settings changes confirmed Apple's engine is completely unaffected (the critical regression check, since `activeEngine` must default to `.apple` for every existing user); Parakeet model download showed real, advancing progress; a real dictation session with Parakeet active produced live partials in the overlay and correct final pasted text; a custom vocabulary word triggered the one-time CTC model download on first use and was transcribed correctly; switching back to Apple worked cleanly with no leftover state. No formal WER benchmark run (explicitly deferred, see above). |
| S1–S6 — Smriti integration | 🔶 S1 + S2 + S3 sub-project 1 + S4 + S5.1 + S5.2 shipped, S6 not started | `docs/SMRITI_INTEGRATION_PLAN.md`. S2 — context-aware dictation shipped (2026-07-07), per `docs/superpowers/specs/2026-07-07-s2-context-aware-dictation-design.md`, built via `superpowers:subagent-driven-development` (5 tasks, fresh implementer + reviewer subagent per task, all approved). `Context/SalientTermExtractor.swift`: `properNouns(in:)` (NLTagger `.nameType` — on-device NER for personal/place/org names), `codeIdentifiers(in:)` (regex catching camelCase/PascalCase/snake_case/dotted.paths, including acronym-prefixed identifiers like `NSAttributedString`/`URLSession` via a case-transition lookahead rather than an enumerated pattern list), `rareWords(in:)` (`@MainActor` — `NSSpellChecker` has main-thread affinity, unlike the other two pure-Foundation passes; anything the system dictionary doesn't recognize, a free proxy for "technical/rare" with no bundled corpus), `extractSalientTerms(from:limit:)` (merges all three, case-insensitive dedupe, caps at 30). `Context/ScreenContextReader.swift`: ported from `smriti`'s `AXReader.swift` (same author, MIT, read-only reference) with a 0.6s time budget (vs Smriti's 2.0s — this sits on the critical path to first-partial latency) and hardcoded exclusions (password managers, private/incognito browsing) matching Smriti's `Config.defaults` verbatim; `BrowserURL.swift` deliberately not ported (nothing in this pass calls it — the design spec listed it as "for future use," which the implementation plan correctly treated as scope creep to drop). `AppState` fires the capture the instant `dictation = .starting` is claimed (same moment as the overlay's early-show), concurrently with permission checks/`audioCapture.start()` via a `nonisolated` Task launcher; awaited once right before `engine.transcribe(...)`, merged into a separate `engineVocabulary` value — `vocabSnapshot` itself (which also feeds `fuzzyCorrect`'s harder post-hoc rewrite) stays exactly `customVocabulary`, unchanged. `contextAwareDictationEnabled` toggle in the Vocabulary settings tab, off by default (every Smriti-derived feature ships off by default). 13 new tests, all passing (64 total in the full suite). Two real bugs found and fixed along the way, both via live testing rather than caught by review alone: (1) every `xcodebuild test` run — via CLI or Xcode's own Cmd+U — was launching a real, fully-interactive instance of the app (live global-hotkey `CGEventTap` actually intercepting real keystrokes, a menu-bar `NSStatusItem` that didn't self-terminate, a real Sparkle update check); fixed with `isRunningUnderTests` (`NSClassFromString("XCTestCase") != nil` — the only one of three candidate checks that's launch-method-agnostic; env-var and launch-argument checks each only worked for one invocation path) guarding `AppState.init()`'s hotkey/PTT/permission-prompt/HistoryStore setup, `AppDelegate.applicationDidFinishLaunching`'s status item, and Sparkle's `startingUpdater`. (2) `rareWords`'s `while` loop could hang on a zero-length `NSSpellChecker.checkSpelling` match (never advancing `offset`) — since it's a synchronous `@MainActor` call with no internal `await`, a stuck loop monopolized MainActor and blocked every other task waiting on it, not just its own caller; fixed with a length guard plus a 500-iteration safety cap. Live-verified end-to-end on real hardware: toggled the setting on in a running instance, brought a text-heavy window to front, ran a real start/stop dictation cycle through the menu bar — no crash, clean menu teardown, setting persists correctly; toggled back off and confirmed the same clean cycle. (Automated/headless verification couldn't produce real spoken audio, so the `start-to-first-partial` latency claim from the design spec's Timing section — that concurrent-with-startup timing adds no perceptible delay — is architecturally sound and code-reviewed but not independently re-measured against the 1.3–2.0s baseline in this pass; worth a real spoken-word check before considering the latency risk fully closed.) S3 sub-project 1 (meeting detection, consent, recording) shipped 2026-07-08, per `docs/superpowers/specs/2026-07-07-s3-meeting-detection-consent-recording-design.md` (amended) and `docs/superpowers/plans/2026-07-07-s3-meeting-detection-consent-recording.md` (amended), executed inline (7 tasks) in `.worktrees/s3-meeting-detection`. `Meetings/CallDetection.swift` (pure bundle-ID/AX/URL matching — allowlist of Zoom/FaceTime/Teams/WhatsApp/Slack/Discord/Webex, verification-required apps also need frontmost or a call-like window title), `Meetings/MeetingWatcher.swift` (2s CoreAudio mic-in-use poll driving a pure, fully-tested `nextState` state machine: idle → micActive → prompting → recording/declined, with debounce/timeout durations), `Meetings/MeetingConsentPanel.swift` (interactive `NSPanel` with a 10s countdown — not `OverlayPanel`, which is deliberately click-through), `Meetings/MeetingRecorder.swift`, `UI/MeetingsSettingsView.swift` (single toggle, off by default), debug-only `Meetings/MeetingSelfTest.swift` + a `#if DEBUG` menu item. `MeetingRecorder` went through a significant architecture pivot mid-implementation: the original design (matching `smriti`'s reference implementation) used ScreenCaptureKit's audio-only capture, which shipped and was live-verified working — but the user then raised a firm, correct objection: SCK's audio-only mode still triggers the full Screen Recording permission and a system-wide "Currently Sharing" indicator for a feature that only ever wanted audio, and a real crash (see below) left that indicator orphaned, requiring a logout to clear. Backed by a screenshot showing macOS's own System Settings has a distinct "System Audio Recording Only" permission tier (separate from "Screen & System Audio Recording"), with a real app (Littlebird) already using it. Rewrote `MeetingRecorder` around CoreAudio process taps instead: `AudioHardwareCreateProcessTap` + `CATapDescription.initStereoGlobalTapButExcludeProcesses` taps system-wide output (no screen involvement at all), combined with the real mic device into an `AudioHardwareCreateAggregateDevice` aggregate, read via raw HAL I/O (`AudioDeviceCreateIOProcIDWithBlock`/`AudioDeviceStart`) — not `AVAudioEngine`; an `AVAudioEngine`-based version (pointing `inputNode` at the aggregate via the same `kAudioOutputUnitProperty_CurrentDevice` mechanism `AudioCapture.setInputDevice(_:on:)` uses) never worked live despite the aggregate device itself verifiably having the right channel count, across three targeted fixes — `AVAudioEngine`'s higher-level abstraction evidently doesn't negotiate cleanly with a freshly-created virtual/tap-backed device. Two more real, non-obvious findings from live debugging the CoreAudio-tap rewrite: (1) macOS silently denies process-tap audio delivery (tap/aggregate objects create fine, `AudioDeviceStart` reports success, but the IO callback never fires) unless `NSAudioCaptureUsageDescription` is present in Info.plist — a separate TCC gate from `NSMicrophoneUsageDescription`, no prompt and no error without it; hit the exact same `GENERATE_INFOPLIST_FILE`/`INFOPLIST_KEY_*` synthesis gap Sparkle's `SUFeedURL` hit (M2, this same table), fixed by extending the existing PlistBuddy Info.plist-patch build phase. (2) The aggregate device delivers the mic sub-device and the stereo tap as two *separate* `AudioBuffer`s within one `AudioBufferList` (confirmed via live buffer-shape logging: `mNumberBuffers=2`), not one combined multi-channel buffer as originally assumed — simplified the capture code to deinterleave each buffer independently rather than channel-range-slicing a single combined buffer. Also fixed, from the original SCK version, a real crash (`OmWhisper-2026-07-07-232744.ips`): `MeetingRecorder`'s stream callbacks had no `nonisolated` marker, so were implicitly `@MainActor` by project default, but the real callback queue is a background thread — Swift 6's runtime isolation checker trapped the instant real audio arrived; fixed by following `AudioCapture.swift`'s established pattern exactly (`nonisolated` throughout, mutable state behind `OSAllocatedUnfairLock`). 20 new tests (`CallDetectionTests`, `MeetingWatcherLogicTests`), all passing (98 total in the full suite). Live-verified end-to-end on real hardware, final CoreAudio-tap architecture: debug self-test captures both tracks correctly (system audio -8.7dBFS OK, mic -17.8dBFS OK); System Settings confirms OmWhisper listed under "System Audio Recording Only", not the full Screen Recording section — no video frame, no system sharing indicator, matching the user's original ask. Real-call consent-flow test (detection → 10s countdown panel → accept/decline/timeout with an actual meeting app) and the dictation-suppression test are **deferred, not yet run** — the user chose to postpone that pass rather than block on it this session. Transcription/summary/UI (sub-project 2) remain a separate, not-yet-started plan. S4 — reply assist shipped 2026-07-08, per `docs/superpowers/specs/2026-07-08-s4-reply-assist-design.md` (amended) and `docs/superpowers/plans/2026-07-08-s4-reply-assist.md` (amended), executed inline (7 tasks, later amended by 3 more live-verification fix commits) in `.worktrees/s4-reply-assist`. `ReplyAssist/DoubleTapDetector.swift` (pure timing state machine — consumes/resets a pending single tap on a 0.45s window, ported near-verbatim from smriti's `AssistListener.swift`, same author/MIT/read-only-reference), `ReplyAssist/ReplyAssistMonitor.swift` (`NSEvent.flagsChanged` global+local monitors distinguishing right-⌥ via `keyCode == 61` — modeled directly on `PushToTalkMonitor`, NOT smriti's 30ms `CGEventSource` polling, which exists there only because smriti runs as a launchd daemon; `isSuppressed` closure mirrors `MeetingWatcher`'s exact contract), `ReplyAssist/ReplyContext.swift` (AX focused-element resolution with smriti's full fallback chain — per-app → system-wide → flip `AXManualAccessibility`/`AXEnhancedUserInterface` and retry 8×200ms for slow Electron trees → walk for `AXFocused==true` — plus the placeholder-read-as-value gotcha for empty web/Electron fields; `reply`/`continueDraft`/`rewrite` classification is a pure, fully-tested function), `ReplyAssist/ReplyStreamTypist.swift` (synthesized-keystroke streaming with a sentinel-checked buffered prefix so a backend failure never types garbage into the user's field), `ReplyAssist/ToneProfile.swift` (tone.md digest/prompt-shaping helpers — sourced from this app's own `HistoryStore`, not smriti's continuous screen-capture approach, since S1's capture mechanism doesn't exist yet at this point in the build order and the app's own dictation history is a better fit besides). `AppState` gained `replyAssistEnabled` (off by default, same `access(keyPath:)`/`withMutation(keyPath:)` + collaborator-wiring pattern as `meetingsEnabled`) and `beginReplyAssist()`/`draftAndStream(...)`, drafting via a fixed-UUID internal `PolishStyle` (never shown in the AI tab's user-facing picker) through the existing `PolishBackend.polish(_:style:targetLanguage:)` — no protocol changes needed. 19 new tests (`DoubleTapDetectorTests`, `ReplyContextClassificationTests`, `ReplyStreamTypistSentinelTests`, `ToneProfileTests`), all passing (117 total in the full suite). The original design's `ReplyAssistPanel` (type-intent/leave-blank/hold-to-speak UI, plus a scoped voice-capture path bypassing the `dictation` state machine) was built and worked live, but the user changed direction after seeing it in practice: double-tap right ⌥ now silently auto-drafts from context with **no panel and no voice path at all**; a second double-tap while a draft is in flight cancels it (`ReplyStreamTypist.cancel()`), replacing the panel's now-nonexistent Escape handler — the panel file and voice-capture method were deleted outright, not kept behind a flag. Three more real, non-obvious bugs found only via live testing, all fixed in the same pass: (1) `SystemLLM.polish(...)` timed out on every draft — `ScreenContextReader.captureFrontmostWindowText()` can return up to 50,000 characters (fine for S2's local vocabulary extraction) and the AX-read draft/selection text was equally uncapped, so a text-heavy background document blew the 5s on-device timeout; both capped at 2,000 characters, with the continuation case keeping the text's *tail* via `.suffix(...)` rather than its head, since continuing a draft cares about its most recent content. (2) Drafted text initially had nowhere to land — showing the panel called `NSApp.activate(ignoringOtherApps: true)` so it could reliably receive input, which silently stole focus from the target field's app; fixed (and later made moot entirely once the panel was removed, since nothing activates OmWhisper in the final silent-only flow) by capturing `NSWorkspace.shared.frontmostApplication` before showing the panel and reactivating it before streaming. (3) Typing into Claude's web chat produced real, scattered character corruption ("been" → "bn", "Thank" → "Tha") — smriti's ported 20-UTF16-unit chunk size, sent as one synthetic `keyboardSetUnicodeString` event, was too large a burst for that input's event handling to reliably absorb; reduced to single-character chunks. Live-verified end-to-end on real hardware across the final architecture: double-tap in a real field (both a native-ish context and Claude's web chat, a complex non-native input surface) correctly classifies reply/continue/rewrite mode and silently drafts+types clean text; a second double-tap mid-stream cancels cleanly, leaving already-typed text in place; double-tapping while regular dictation is recording correctly does not trigger the assist at all, confirming the suppression contract holds. S1 — memory core shipped 2026-07-08, per `docs/superpowers/specs/2026-07-08-s1-memory-core-design.md` and `docs/superpowers/plans/2026-07-08-s1-memory-core.md`, executed inline (7 tasks, plus 1 live-verification fix commit) in `.worktrees/s1-memory-core`. `Memory/BrowserURL.swift` (ported near-verbatim from smriti — AX-only page-URL resolution across 9 browsers, no AppleScript/Automation permission needed; deferred as scope creep at S2 time, genuinely needed now for domain exclusion). `Context/ScreenContextReader.swift` had 3 members (`collectText`, `copyAttribute`, `textBearingRoles`) changed from `private` to internal so a new `Memory/WindowSnapshotReader.swift` could reuse S2's proven AX tree-walk rather than forking it, while returning richer metadata (`bundleID`/`appName`/`windowTitle`/`url`) than S2's own `captureFrontmostWindowText()` needs — S2's public contract and behavior are unchanged (confirmed: full suite stayed at 124/124 immediately after the visibility change, before any new S1 code was added). `Memory/MemorySnapshot.swift` + `Memory/MemoryStore.swift`: a *separate* GRDB `DatabaseQueue`/file (`memory.db`, not `history.db`) from `HistoryStore` — memory (background capture) and dictation history are differently-sensitive, differently-defaulted data; a user must be able to wipe one without touching the other. Schema is a from-scratch GRDB design (not a port of smriti's raw-SQLite3 `Store.swift`): `snapshots` table + `UNIQUE(bundleID, windowTitle, contentHash)` dedup index + an FTS5 virtual table kept in sync via GRDB's `synchronize(withTable:)` (verified directly against the pinned GRDB 7.11.1 source before writing any code — it auto-generates the AFTER INSERT/DELETE/UPDATE sync triggers, no hand-written trigger SQL needed, confirmed real and worked on the first attempt live). `Memory/MemoryCapture.swift`: `Timer`-driven daemon modeled on `MeetingWatcher`'s poll pattern (5s default interval, matching smriti's own proven default) — NOT smriti's `SIGUSR1` signal handler, which exists there only because smriti runs as a separate launchd CLI daemon with no other IPC channel; pause here is just a settings-backed flag the daemon's `isSuppressed` closure reads each tick. `AppState` gained `memoryEnabled`/`memoryPaused`/`memoryRetentionDays` (same `access(keyPath:)`/`withMutation(keyPath:)` + collaborator-wiring pattern as `meetingsEnabled`/`replyAssistEnabled`) and a `memoryStore` opened independently of `historyStore` in `init()` (one failing to open must not affect the other) via a shared Application Support directory computation. `UI/MemorySettingsView.swift` (toggle + pause + retention stepper only — no search/browse UI, that's S5's job entirely), debug-only `Memory/MemorySelfTest.swift` + a `#if DEBUG` menu item. 25 new tests (`BrowserURLTests`, `MemoryStoreTests`, `MemoryCaptureExclusionTests`), all passing (137 total in the full suite) — notably `MemoryStoreTests` includes a real prune-deletion test (not just the `olderThanDays: 0` no-op path caught in self-review before implementation): `dbQueue` was made internal (not private) specifically so the test could backdate a row's `lastSeenAt` directly and verify `prune()` actually deletes it, since `upsert()`'s production API always stamps "now" with no injectable clock. One real bug found via live testing: `MemorySelfTest`'s search-probe selection used `split(separator: " ")`, which only splits on literal spaces — capturing a newline-separated file-tree listing (Xcode's navigator) produced a giant multi-line "first word" as the probe instead of a real single token; fixed with `split(whereSeparator: { $0.isWhitespace })`. Live-verified end-to-end on real hardware: self-test correctly captures app/bundleID/window title/content and finds it via FTS5 search; **direct `sqlite3` inspection of the real `memory.db`** (not just the self-test's own one-shot write) confirmed the background `Timer` daemon is genuinely populating the store independently — 8 distinct rows captured across Fork/Arc/Xcode/cmux/OmWhisper itself over about a minute of real usage, with correct metadata and small per-app row counts (2 each, not one row per 5s tick), confirming dedup is collapsing unchanged repeat captures rather than growing unbounded. Exclusion behavior (password managers, private/incognito browsing, excluded domains) and the pause/disable toggle paths are **deferred, not yet live-verified** — the user chose to postpone that pass rather than block on it this session; the underlying logic is unit-tested (`MemoryCaptureExclusionTests`, `ScreenContextReader.isExcluded` already proven by S2) but not confirmed against the real daemon end-to-end. S5.1 — chronicles + memory browse UI shipped 2026-07-09, per `docs/superpowers/specs/2026-07-09-s5-1-chronicles-search-ui-design.md` and `docs/superpowers/plans/2026-07-09-s5-1-chronicles-search-ui.md`, executed inline (6 tasks) in `.worktrees/s5-1-chronicles-search-ui`. S5 was split into two sub-projects during brainstorming (S5.1 here, S5.2 MCP server next) and in-app "Memory Chat" was descoped entirely — this app has no `PolishBackend` capable of true multi-turn tool-calling (unlike smriti's approach of shelling out to a live `claude` CLI process against its own MCP server, which needs Claude Code installed plus a subscription), and the MCP server alone already satisfies S5's exit criterion ("what was I working on before lunch?" answerable from Claude Desktop). `Memory/MemoryStore.swift` gained a `chronicles` table (`MemoryChronicle`: day/summary/snapshotCount/createdAt, `day` as primary key, GRDB's `PersistableRecord.save` giving insert-or-replace upsert for free — no hand-written `ON CONFLICT` SQL needed, simpler than smriti's raw-SQLite upsert) plus `fetchPage`/`delete`/`deleteAll`/`storageInfo` (mirroring `HistoryStore`'s exact method shapes) and `upsertChronicle`/`getChronicle`/`listChronicles`/`snapshotsForDay`. `Memory/Chronicler.swift`: the one real architectural departure from smriti — this app has exactly one `PolishBackend` (`SystemLLM`, on-device Foundation Models) whose `polish()` call has a hardcoded 5s timeout that S4 already found needs inputs capped around 2,000 chars, so a whole day's snapshots (smriti pipes up to 120,000 chars through a full `claude -p` CLI call) can't go through in one shot; instead, map-reduce: `chunk(_:limit:)` (pure, greedily packs formatted snapshot blocks into ≤1,800-char groups, a single oversized block gets its own group rather than being split or dropped) each summarized individually via `polish()` (map, two new fixed-UUID internal `PolishStyle`s — chunk-summarizer and chronicle-writer, same hidden-style pattern as S4's reply draft, never shown in the AI tab's picker), then the concatenated chunk-summaries (themselves capped at 1,800 chars) get one final `polish()` call that writes the actual markdown chronicle (reduce) — every individual LLM call stays inside the already-proven-safe zone, zero changes to `PolishBackend`/`SystemLLM`. `Memory/ChronicleScheduler.swift`: `@MainActor` Timer wrapper mirroring `MemoryCapture`'s own poll/prune shape exactly, fires once at `start()` plus every 24h, generates *yesterday's* chronicle only if one doesn't already exist (idempotent — restarting the app repeatedly doesn't regenerate the same day over and over). `AppState` gained a `chronicleScheduler` wired into the existing `memoryEnabled` setter (no new master toggle — implicit under the same flag as `MemoryCapture`), additionally gated on `polishBackend == .system && SystemLLM.isAvailable()` via the same live-read `isSuppressed` closure contract as every other background daemon in this app, plus `regenerateChronicle(day:)` (the view layer's only path to `systemLLM`, which stays `private`). New `Window("Memory", id: "memory")` scene + "Memory…" menu item (mirrors "History…" exactly): `UI/MemoryView.swift` (Snapshots/Chronicles segmented picker; Snapshots tab is a near-verbatim structural copy of `HistoryView`'s proven shape — searchable list, debounced reload, tap-to-expand rows, pagination — deliberately dropping `HistoryView`'s Export menu and multi-select bulk delete as unneeded scope for now) and `UI/MemoryChroniclesView.swift` (`NavigationSplitView` day-list + markdown-rendered detail pane, SwiftUI-native rebuild of smriti's raw-AppKit `ChronicleTimelineSection`, which per this project's standing convention is wireframe reference only). One real design gap caught during the plan's own self-review, before any code was written: the original design only offered a per-day "Regenerate" button inside the detail pane, which is unreachable when zero chronicles exist yet (the normal first-run state) — nothing to select, no button ever appears, so a user could never generate their first chronicle. Fixed by moving to a single always-visible toolbar button ("Generate Today's Chronicle", doubling as regenerate-today since `Chronicler.generate` always overwrites) rather than a per-selected-day action; regenerating an arbitrary *past* day beyond today is intentionally not supported yet (`ponytail` comment marks it — add if it turns out to matter). Also caught in the plan's own self-review, before implementation: a broken `??`/`==` operator-precedence bug in the scheduler's idempotency check, a non-mutating-vs-mutating `PersistableRecord.save` mismatch, and an unproven `#expect(throws:)` async-closure pattern with zero prior usage anywhere in this test suite (swapped for the same do/catch + `Issue.record` idiom already used elsewhere). 19 new tests (`MemoryStoreTests` chronicle/pagination additions, `ChroniclerTests`), all passing (156 total in the full suite) — including one bug the tests themselves caught before any live run: `fetchPage`'s `ORDER BY lastSeenAt DESC` alone is non-deterministic for same-second inserts (`ISO8601DateFormatter`'s default precision is whole seconds, and three `upsert()` calls in one test executed within the same second), fixed with a secondary `Column("id").desc` tiebreaker — a genuine correctness fix for real same-second capture bursts, not just a test artifact. Live-verified end-to-end on real hardware by the user directly: enabling Memory + confirming the AI backend is System, opening the new Memory window from the menu bar, Snapshots tab showing recent captures with working search, and — the core new mechanism — clicking "Generate Today's Chronicle" in the Chronicles tab produced a real markdown-rendered chronicle from actual captured snapshots and a real `SystemLLM` call end-to-end. S5.2 — MCP server shipped 2026-07-09, per `docs/superpowers/specs/2026-07-09-s5-2-mcp-server-design.md` and `docs/superpowers/plans/2026-07-09-s5-2-mcp-server.md`, executed inline (6 tasks, plus 1 live-verification fix commit) in `.worktrees/s5-2-mcp-server`. The real architectural question: this app is a GUI menu-bar app (`@main struct OmWhisperApp: App`), not a CLI tool like smriti, so there was no existing subcommand dispatch to hang an `mcp` subcommand off of. Resolved by removing `@main` from `OmWhisperApp` and adding a new `main.swift` (the target's actual entry point) that branches on `CommandLine.arguments.contains("--mcp")` before SwiftUI's lifecycle starts — calling `OmWhisperApp.main()` explicitly is the same static call `@main` would have synthesized. The `--mcp` path (`MCP/MCPLauncher.swift`) never touches `AppState` — no hotkeys, no `NSStatusItem`, no Sparkle, no audio — it opens `HistoryStore`/`MemoryStore` directly via a new shared `AppSupportDirectory.resolve()` helper (extracted from `AppState.init()`'s inline `FileManager` lookup so both paths resolve the identical on-disk location without duplicating it; confirmed a pure extraction via the full suite staying green immediately after, before any MCP code existed) and runs `MCP/MCPServer.swift`, a stdio JSON-RPC 2.0 server ported from smriti's `MCPServer.swift` (same author, MIT, read-only reference) with GRDB-record field names instead of raw SQLite3 row reads. Six read-only tools: `search_memory`/`get_recent_activity`/`get_snapshot`/`get_chronicle`/`list_chronicles` (over the S5.1 memory/chronicle store, via two new `MemoryStore` methods this task added — `recent(minutes:limit:)` and `getSnapshot(id:)`) plus `search_transcriptions` (over `HistoryStore`, smriti has no equivalent since it doesn't do dictation). Gated by a new `AppState.mcpAccessEnabled` toggle (off by default, same `access(keyPath:)`/`withMutation(keyPath:)` pattern as every other setting) — unlike every other feature toggle in this app it wires no in-process collaborator, since its only effect is being read fresh by a *future* `--mcp` subprocess launch, not anything the current process does; `MCPLauncher.run()` checks it before opening any store and refuses with a clear stderr message + `exit(1)` if off, never silently serving with it disabled. New `UI/MCPSettingsView.swift` Settings tab shows a copy-paste-ready Claude Desktop config JSON block (built from `Bundle.main.executablePath`, so it always reflects wherever this exact running copy actually lives) with a Copy Config button, plus the toggle. 18 new tests (`MemoryStoreTests` recent/getSnapshot additions, `MCPServerTests` — the latter calling `MCPServer.callTool(name:args:)` directly against in-memory stores rather than driving the actual stdio loop, which isn't cleanly unit-testable in-process), all passing (174 total in the full suite, one `SalientTermExtractorTests.detectsRareWords()` flake during a parallel full-suite run confirmed unrelated and passing cleanly in isolation — pre-existing `NSSpellChecker` system-state sensitivity, not caused by this work). Two real, non-obvious build errors hit and fixed during implementation, neither anticipated by the plan: (1) `MCPServer` silently became `@MainActor` under this project's `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` default, which broke calling `callTool` synchronously from plain (non-MainActor) test functions — fixed by marking the class `nonisolated`, matching `HistoryStore`/`MemoryStore`'s existing convention for anything meant to run off the GUI path. (2) Swift 6 then flagged the tool-definitions static array (`[[String: Any]]`) as not provably `Sendable` — fixed with `nonisolated(unsafe)` on that one `let`, safe here since it's immutable literal data never mutated after init, exactly the case the compiler's own suggested fix targets. One real bug found only via live testing, not anticipated by the plan or design: adding MCP as a 9th Settings tab pushed the tab bar past its fixed 520pt window width, so macOS silently collapsed the overflow into a ">>" menu — hiding the new tab entirely on first launch after this change; caught by the user via screenshot, fixed by widening `SettingsView`'s frame to 660pt. Live-verified end-to-end on real hardware: the toggle-off refusal path (piping a raw `initialize` JSON-RPC message into `OmWhisper --mcp` with the toggle at its default `false` correctly printed the disabled-access stderr message and exited 1, no stdout); the Settings UI (toggle, real non-placeholder config path, Copy Config) after the width fix; and the full protocol end-to-end with the toggle on — `initialize`/`tools/list`/`tools/call` piped directly into the real built binary returned correct `serverInfo`, all 6 tool schemas, and a real (empty-but-correct, no captures in the immediate test window) `get_recent_activity` result, proving the complete dispatch → store → render path runs without crashing against the real compiled binary. An actual live Claude Desktop connection (editing its config, restarting it, asking a real question) was **not completed this session** — deferred after being started, matching this session's established pattern of deferring the last, externally-dependent leg of live verification (S1's exclusion tests, S3's real-call test) once the core mechanism is already proven; the MCP protocol layer itself carries no remaining risk, a real Claude Desktop round-trip at this point is client-side plumbing, not a question about this app's own correctness. S6 not started. |
| T1–T4 — Digital Twin (Phase 3) | ⬜ Planned | Same plan doc — after S5. Profile distillation → grounding → auto-vocab → habits. |
| F1–F7 — Frontier (Phase F) | ⬜ Planned | `docs/FRONTIER_FEATURES_PLAN.md` — post-2.0 waves 2.1/2.2/2.3 after M3/M4. |

## Explicitly Dropped vs. the Tauri App

Windows support (stays on Tauri) · Whisper/Moonshine engines · VAD settings UI · model manager UI
(except an optional Parakeet download) · auto-punctuation toggle (always on, in-engine).

## Build

```bash
# Build + test
xcodebuild -scheme omwhisper-native -project omwhisper-native.xcodeproj build test

# Release (archive → notarize → dmg)
bash scripts/build-release.sh
```

Requires: Xcode with macOS 26 SDK, a paid Apple Developer account for notarization, and
`APPLE_SIGNING_IDENTITY` / `APPLE_ID` / `APPLE_ID_PASSWORD` (app-specific password) /
`APPLE_TEAM_ID=Y87BZN47C5` set in the environment or a local `.env` (gitignored).

## Working Alongside the Old Repo

Keep `omwhisper` (Tauri) checked out alongside this repo during development — it's the
functional spec (see Appendix B of the migration plan), not code to port line-by-line. Rust
idioms ported directly into Swift produce bad Swift; rebuild against the parity checklist instead.
