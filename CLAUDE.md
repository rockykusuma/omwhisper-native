# OmWhisper Native — Claude Code Context

> Read this file at the start of every session to get full project context.

OmWhisper — menu-bar dictation for macOS, written in Swift. Currently **2.0.x**, shipping.

---

## What This Project Is

- **Platform**: macOS 26+, Apple Silicon only. No availability gating, no fallback paths —
  SpeechTranscriber and Foundation Models are assumed present.
- **UI**: SwiftUI-first, AppKit where the platform demands it (`MenuBarExtra`, `NSPanel`
  non-activating overlay, `CGEventTap` for PTT).
- **Distribution**: Developer ID + notarization, direct download — **not** the App Store.
  The app is NOT sandboxed: Accessibility-based paste (`CGEventPost`) and global event taps
  require entitlements the App Store sandbox forbids.
- **Bundle ID**: `com.omwhisper.mac`. Debug builds are `com.omwhisper.mac.dev` — see the Build
  section. On first run the app imports transcription history from a pre-2.0 install's
  `com.omwhisper.app` data dir if one exists (read-only, one-time).
- **Apple Developer Team ID**: `Y87BZN47C5` (already set in `project.pbxproj`).
- **Language mode**: Swift 6 (`SWIFT_VERSION = 6.0`, all targets), with
  `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` + `SWIFT_APPROACHABLE_CONCURRENCY = YES` — every
  unannotated declaration is `@MainActor` by default. See "Concurrency" below before writing
  anything that touches audio callbacks or background work.
- **Naming gotcha**: the Xcode *target* (and its scheme) is named `omwhisper-native` —
  `PRODUCT_NAME` is set to `OmWhisper` (so the built app/bundle display as "OmWhisper"), but
  `xcodebuild -scheme ...` and CI must use `omwhisper-native`. The scheme is committed at
  `omwhisper-native.xcodeproj/xcshareddata/xcschemes/omwhisper-native.xcscheme` — it is NOT
  autocreated on a fresh clone, so don't delete it.

## North Star

Match Claude Code's dictation feel — server-side streaming ASR, words appearing while you
speak — while keeping OmWhisper's actual differentiator: **user choice of local vs. cloud
transcription/polish backends**, not "always local." macOS only.

Sign-off criteria (gate M1 and M4):
1. Live dimmed partials in the overlay, < 1s behind speech, finalized on stop.
2. Key-release → punctuated text pasted in the target app in < 700ms typical.
3. Punctuation/capitalization work with zero configuration (in-engine).
4. Technical vocabulary respected via context hints (custom vocab → engine biasing / cloud keyterms).
5. PTT starts instantly on keydown.

**Measured status (2026-07-07, live on-device, 9 dictation runs via `Logger(category: "Latency")`):**
- **#2 passes solidly**: stop-to-paste measured 108ms–697ms across every run, always under the 700ms bar.
- **#3 confirmed** live — pasted output shows correct capitalization/terminal punctuation with zero configuration.
- **#5 shipped** — Fn/Globe push-to-talk (`PushToTalkMonitor`), feels instant in practice (not separately latency-measured).
- **#1 not consistently met**: early runs measured ~4s and were initially suspected as an engine/setup bug — root-caused instead to human speech-onset delay after pressing the hotkey (the engine's own setup chain measured 8–42ms; mic buffers reach the analyzer within ~0.1s — see git history on `AppleEngine.swift`, since reverted). With speech content pre-decided to remove "what do I say" think-time, repeat runs landed at 1.3–2.0s, with one outlier at 0.89s — so sub-1s is achievable but not the norm. The gap is likely a mix of unavoidable human reaction time and the model's own minimum buffering before its first hypothesis; the current instrumentation (timed from recording-start, not actual speech-onset) can't separate the two. Untangling further needs VAD-based speech-onset detection — deferred, not pursued.
- **#4 BUILT, BUT MEASURED AS INEFFECTIVE ON THE DEFAULT ENGINE (2026-08-01)** — was recorded
  as "shipped 2026-07-07": Vocabulary settings tab (custom words + whole-word replacements +
  fuzzy-match toggle), biasing `AppleEngine` via `AnalysisContext.contextualStrings` (the real
  mechanism — confirmed from the macOS 26 SDK's `Speech.swiftinterface`; `SpeechTranscriber`
  itself has no vocab parameter). **The first A/B measurement (`--wer`, see the M4 row and
  `docs/wer-corpus/README.md`) shows Apple Speech and both Parakeet variants emit
  byte-identical transcripts with and without a vocabulary list** — the same characters, with
  the listed terms still wrong ("app cast" for `appcast`, "whisper kit" for `WhisperKit`). A
  `log.debug` kept inside `AppleEngine`'s biasing branch fires `contextualStrings applied: N
  term(s)`, so the branch runs and the context is set before `start()`; the transcript simply
  does not change. The control: the same vocabulary array, same audio, same call site,
  measurably improves Whisper (base 8.3% → 4.2%) — so the value reaches the engines and two
  ignore it. **This criterion was recorded as met on the strength of the code being written
  and the API confirmed against the SDK, never on an observed change in a transcript** — the
  same shape as the Verification section below. Caveat before treating it as settled: the
  corpus is synthetic (macOS `say`) and small, and biasing may only move decisions a model is
  already uncertain about. Parakeet's evidence is weaker than Apple's (its boosting needs a
  CTC model whose download was not separately confirmed). Word replacements and fuzzy
  correction are unaffected — they are `AppState` post-processing, not engine biasing.
  **S2 context-aware dictation routes screen terms through this same path**, so it is in
  the same doubt.

## Tech Stack

| Layer | Technology |
|-------|-----------|
| UI | SwiftUI + AppKit (`MenuBarExtra`, `NSPanel`, `CGEventTap`) |
| State | Single `@Observable` `AppState` — no per-view settings read-modify-write |
| Transcription | `TranscriptionEngine` protocol → `AppleEngine` (SpeechTranscriber, default) / `ParakeetEngine` (FluidAudio CoreML, optional) / `CloudEngine` (streaming WebSocket) |
| Polish | `PolishBackend` protocol → `SystemLLM` (Foundation Models, default) / `Ollama` / `CloudLLM` (OpenAI-compatible, keys in Keychain) |
| Capture | AVAudioEngine (`AudioCapture.swift`) |
| Paste | `CGEventPost` Cmd+V + clipboard save/restore (`PasteService.swift`) |
| History | SQLite (GRDB), with a one-time importer for pre-2.0 installs |
| Updates | Sparkle (replaces `updater.rs` + `version.json`) |

## Project Structure

```
omwhisper-native/
├── omwhisper-native.xcodeproj/
│   ├── xcshareddata/xcschemes/omwhisper-native.xcscheme  # committed — see naming gotcha above
│   └── project.pbxproj                 # file-system-synced groups — no manual edits to add/remove FILES
│                                       #   (build-SETTING edits are fine; see the .dev fork + PlistBuddy phase)
├── omwhisper-native/                   # App target
│   ├── main.swift                      # REAL entry point: branches on --mcp / --wer / --diagnose-meeting
│   │                                   #   before SwiftUI starts, else calls OmWhisperApp.main() (S5.2)
│   ├── OmWhisperApp.swift              # App scenes + AppDelegate (AppKit NSStatusItem, NOT MenuBarExtra),
│   │                                   #   Sparkle updater, hub/onboarding windows
│   ├── AppState.swift                  # @Observable root state: every setting, the dictation core loop,
│   │                                   #   and the orchestration for meetings/memory/polish/reply-assist
│   ├── AppSupportDirectory.swift       # THE data root — bundle-ID-aware; never hardcode a data path
│   ├── AppMarkdown.swift               # Stored markdown → UI structure (transcript turns, summary sections)
│   ├── SingleInstance.swift            # Refuses a second copy (by bundle ID); --mcp exempt by construction
│   ├── DebugInfo.swift                 # Copy Debug Info — config + permissions only, never content
│   ├── CrossLingual.swift              # F4: speak another language, get English
│   ├── Transcription/                  # 4 engines behind TranscriptionEngine + Keychain + BufferConverter
│   │                                   #   Apple (default) · Parakeet/FluidAudio · Whisper/WhisperKit · Cloud
│   ├── Capture/AudioCapture.swift      # AVAudioEngine mic capture → AsyncStream<AVAudioPCMBuffer>
│   ├── Hotkeys/                        # GlobalHotkey, PushToTalkMonitor (Fn/Globe), KeyCombo
│   ├── Paste/PasteService.swift        # Frontmost-app capture, CGEventPost paste, clipboard restore
│   ├── Polish/                         # PolishBackend + SystemLLM / Ollama / CloudLLM, styles, Redactor
│   ├── Meetings/                       # Detection, consent panel, CoreAudio-tap recorder, transcriber,
│   │                                   #   diarization, summarizer + templates, calendar match, store
│   ├── Memory/                         # S1/S5: window snapshots, MemoryStore (FTS5), Chronicler
│   ├── Context/                        # S2: screen-text reader + salient-term extraction for biasing
│   ├── ReplyAssist/                    # S4: double-tap right ⌥ → draft from context, stream-type it
│   ├── History/                        # HistoryStore (GRDB) + the one-time pre-2.0 importer
│   ├── Vocabulary/VocabularyProcessing.swift  # Replacements, fuzzy correction, engine-vocab merge
│   ├── MCP/                            # --mcp stdio JSON-RPC server + launcher (read-only tools)
│   ├── Benchmark/                      # --wer corpus runner + pure WER maths
│   ├── UI/                             # Hub (sidebar + sections), overlay HUD, onboarding, mini-panel,
│   │                                   #   Porcelain design system (OmColors/PorcelainComponents)
│   └── Assets.xcassets                 # AppIcon — regenerate via scripts/make-app-icon.swift, never by hand
├── omwhisper-nativeTests/              # Swift Testing (@testable import OmWhisper). NO UI-test target —
│                                       #   it was removed deliberately; never add XCUITest back.
├── docs/                               # See "Docs map" below
├── scripts/                            # build-release.sh (+ --publish), publish-release.sh,
│                                       #   make-app-icon.swift, make-og-image.swift, make-wer-corpus.sh
└── .github/workflows/ci.yml            # build + test on macOS runner
```

**Important**: the Xcode groups are **file-system-synchronized** (`PBXFileSystemSynchronizedRootGroup`).
Adding, moving, or deleting a `.swift` file on disk is enough — Xcode picks it up automatically.
Do not hand-edit `project.pbxproj` file references.

### Docs map

Read the one you need; they are not all live documents.

| Doc | Status |
|---|---|
| `SMRITI_INTEGRATION_PLAN.md` | Phase S (S1–S6) scope. S1–S6 all shipped; kept for the rationale behind each. |
| `FRONTIER_FEATURES_PLAN.md` | Phase F + Phase 3 (Digital Twin). **Live** — F1/F2/F3/F6/F7 and T1–T4 are unbuilt. |
| `DESIGN_DIRECTION.md` | Phase D hub/Porcelain direction. Superseded on one point: the "always light" rule was reversed (tokens are adaptive), and the default is now Light. |
| `OVERLAY_SPEC.md` | **Live and authoritative** for the HUD: geometry, motion formulas, states, a11y. |
| `RELEASE_SETUP.md` | **Live** — one-time signing/notarization/Sparkle setup, referenced by README. |
| `LAUNCH_READINESS.md` | **Live** — non-feature launch gaps; still has open **DECIDE** items (license posture, support channel, launch languages, sustainability). |
| `COMPETITOR_FLUIDVOICE.md` | FluidVoice analysis — the closest competitor. See also the Meetily/Littlebird research inside `superpowers/specs/2026-08-01-meetings-competitive-parity-design.md`. |
| `*.html` prototypes | Design references, not shipped code: `hub-concept` (Porcelain palette source of truth), `onboarding-prototype`, `overlay-styles`, `sidebar-brand-ideas`, `meetings-transcript-ideas`. |
| `superpowers/specs/` + `plans/` | Per-sub-project design and implementation records, dated. The narrative of *why* each piece looks the way it does. |
| `releases/README.md`, `wer-corpus/README.md` | Release notes index; WER benchmark corpus + findings. |

## Concurrency

The project sets `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor`: every type/function without an
explicit annotation is implicitly `@MainActor`-isolated. This is the right default for UI and
state (`AppState`, `GlobalHotkey`, `OverlayPanel` are all effectively `@MainActor`), but it is
**wrong** for anything that genuinely runs off the main thread — most importantly
`AVAudioEngine`'s tap callback, which fires on its own real-time render thread regardless of
what Swift infers. Two escape hatches are used deliberately, and any new code touching audio
buffers or background transcription work should follow the same pattern:

- **`nonisolated`** on functions/properties that must run (or be callable) off MainActor —
  e.g. `AudioCapture.start()/stop()`, `AppleEngine.transcribe(_:)`. A `nonisolated func`'s
  `Task { }` runs on the cooperative thread pool, not MainActor.
- **`nonisolated(unsafe)`** on the small pieces of mutable state those nonisolated code paths
  touch, paired with a real lock (`OSAllocatedUnfairLock` in `AudioCapture`) — never rely on
  actor isolation to protect state a background thread writes to.
- **`@preconcurrency import AVFoundation`** wherever `AVAudioPCMBuffer`/`AVAudioConverter`
  cross a `Task` boundary — these AVFoundation types are not `Sendable`, and we've asserted the
  actual safety invariant ourselves (single producer → single consumer per buffer) rather than
  fighting the compiler. `Speech` (SpeechAnalyzer/SpeechTranscriber) is a newer, concurrency-first
  API and needs no such treatment.

If a build error mentions an actor-isolation or Sendable violation in `AudioCapture`,
`BufferConverter`, or `AppleEngine`, check that the `nonisolated`/`nonisolated(unsafe)` markers
are still in place before assuming the fix is elsewhere.

## Key Contracts

- `TranscriptionEngine` — `func transcribe(_ audio: AsyncStream<AVAudioPCMBuffer>, vocabulary: [String]) -> AsyncThrowingStream<TranscriptEvent, Error>`,
  where `TranscriptEvent = .partial(String) | .final(String)`. SpeechTranscriber's volatile/finalized
  results map directly onto this; Parakeet and cloud engines conform to the same shape.
  `vocabulary` is read fresh at the start of each call (engines are rebuilt per session
  anyway) and biases `AppleEngine` via `AnalysisContext.contextualStrings`. Word replacements
  and fuzzy correction (`Vocabulary/VocabularyProcessing.swift`) are engine-agnostic
  post-processing, applied in `AppState` to both `.partial` and `.final` text — not part of
  the engine contract itself.
- `AppState` — the single `@Observable` store. Settings persist via `UserDefaults` (with a
  one-time importer for the old `settings.json` planned in M2). No component does its own
  read-modify-write of settings. Also owns the M1 core loop: `toggleDictation()` →
  `startDictation()`/`stopDictation()` wire `AudioCapture` → `AppleEngine` → `OverlayPanel` → `PasteService`.
- `PolishBackend` — `func polish(_ text: String, style: PolishStyle) async throws -> String`.

## Milestones

**Scope only — the Progress Tracker below is the single source of truth for status.**
(These bullets used to carry their own status and drifted out of step with the table.)

- **M0 — Repo + pipeline**: Xcode project, folder skeleton, CI, committed shared scheme,
  build-release script.
- **M1 — Core loop MVP**: hotkey → capture → SpeechTranscriber streaming → live overlay
  partials → paste on stop.
- **M2 — Daily-driver parity**: Settings UI, PTT, history + importer, vocabulary UI, sounds,
  launch-at-login, Sparkle, onboarding.
- **M3 — AI polish**: Foundation Models default backend, styles system, Ollama + cloud backends,
  Keychain key storage, smart dictation (Cmd+Shift+B).
- **M4 — Backend flexibility (the USP)**: cloud streaming engine, Parakeet CoreML optional engine,
  per-backend selector UI.
- **M5 — Beta → release**: feature audit, signing/notarization/appcast, landing page, beta soak.
- **Phase S — Smriti integration** (decided 2026-07-07, see `docs/SMRITI_INTEGRATION_PLAN.md`):
  port Smriti's features into this app (smriti repo stays untouched; copy or rewrite here).
  S1 memory core (AX capture + FTS5 store) · S2 context-aware dictation (screen terms →
  `contextualStrings`) ⭐ · S3 meeting intelligence (consent-first, our engines) · S4 voice
  reply assist · S5 memory surfacing + MCP server · S6 website update (OmWhisper-centric).
  Order (priorities set 2026-07-07): M2 → S2 → M3 → S3 → S4 → S1 → S5 → M4 → S6/M5 —
  S4 ships window-context-only (no stored memory), gains memory snippets when S1 lands.
  "Learning the user" scope within Phase S = writing tone only (tone.md, in S4). All
  Smriti-derived features are OFF by default.
- **Phase F — Frontier features** (decided 2026-07-07, all approved; see
  `docs/FRONTIER_FEATURES_PLAN.md`): post-2.0 differentiator waves — 2.1: F5 brain-dump
  mode, F6 per-app output shaping + voice snippets, F7 vocab packs (after M3) · 2.2:
  F1 voice editing ("scratch that"), F4 cross-lingual dictation (speak Telugu/Hinglish →
  polished English) · 2.3: F3 only-my-voice (FluidAudio speaker ID), F2 whisper mode
  (after M4). F2's whispered-WER benchmark runs early as a parallel research spike.
  None block the committed spine or M5.
- **Phase 3 — Digital Twin** (decided 2026-07-07, after S5; same plan doc): nightly local
  distillation of the day's memory into a facts store (confidence/recency/source) +
  editable `profile.md` → twin-grounded replies & memory chat, auto-vocabulary learning
  from corrections, habit-level nudges. T1 profile distillation → T2 grounding →
  T3 auto-vocab → T4 habits. Local-only, inspectable, off by default; framed as "writes
  like you, knows what you know" — never as cloned judgment.
- **Phase D — Hub Window / Porcelain** (design approved 2026-07-07/08, see
  `docs/hub-concept.html` and `docs/DESIGN_DIRECTION.md`): migrates today's window
  sprawl (`Settings`/`History`/`Memory` scenes) into one hub window with a glass
  sidebar over a light "Porcelain" palette — the dark emerald-on-green-black
  identity stays scoped to the overlay HUD and onboarding only. D1 foundations →
  D2 hub shell + migrations → D3 Home dashboard + menu-bar mini-panel → D4 polish/a11y
  pass. Independent of the M/S/F/T roadmap (pure UI layer), sequenced as "the natural
  next block" after M4.

## Verification — checks that cannot fail are not checks

Three times on 2026-07-31, something was recorded as verified when the check
used could not have reported failure. Each cost hours later.

| Recorded as | What was actually true |
|---|---|
| "✓ Gatekeeper: accepted" from `spctl --assess` | `spctl --status` is `assessments disabled` on this Mac, so it accepts *anything* |
| "a `SalientTermExtractorTests` flake, unrelated to this work" | Deterministic and correct for **53 consecutive CI runs** — a real `NSSpellChecker` auto-language bug |
| "Check for Updates… clicking it doesn't crash (fails gracefully)" | A silent no-op. The feature had **never worked**, from the day it was written |

The shape is always the same: an observation with only one possible outcome,
written down as if it had two. "Didn't crash", "looks right", "passes in
isolation" and "returns 200" are not evidence — they are the absence of one
particular kind of evidence.

**Before writing "verified", ask what result would have proved it broken.** If
there isn't one, the check is decorative. What broke each of the above open was
an observation that *could* have come back negative:

- a **clipboard sentinel** written before a click, read after — proving whether the
  action ran at all, which no amount of reading the code could establish
- a **literal-string search** instead of `grep` treating `.` as a wildcard
- a **`.notice` log inside the action itself**, surfaced through the app's own
  Copy Debug Info, which named the failure in one line after four wrong hypotheses

Corollary, learned the same day: when symptoms survive a correct fix, suspect a
**second independent bug** rather than a wrong diagnosis. The dead update button
was a hit region limited to the letterforms *and* a delegate lookup that always
returned nil; either alone produced identical symptoms, so fixing one changed
nothing observable and looked like failure.

## Progress Tracker

| Milestone | Status | Notes |
|-----------|--------|-------|
| M0 — Repo + pipeline | ✅ Done | pbxproj configured (bundle ID, macOS 26.0 target, sandbox off, Swift 6 language mode); source skeleton; CLAUDE.md/README/CI/build-release.sh; committed shared xcscheme (`omwhisper-native`, not autocreated — see naming gotcha). Confirmed build+run in Xcode. |
| M1 — Core loop MVP | 🔶 Live, sign-off pending on #1 | Running live end-to-end (2026-07-07) after fixing three session-blocking bugs: SwiftUI `MenuBarExtra` silently dropping real clicks on macOS 26 (→ AppKit `NSStatusItem`); Hardened Runtime with no `audio-input` entitlement (mic permission silently denied, no prompt); an off-MainActor `SFSpeechRecognizer` callback crashing under Swift 6 isolation checks. Pulled forward from M2: push-to-talk (`PushToTalkMonitor`, hold Fn/Globe), start/stop sounds, paste-reliability hardening (Accessibility-gated, no more silent no-ops), richer menu-bar icon states. See "Measured status" under Sign-off criteria above for the numbers — #2/#3/#5 pass, #1 doesn't consistently, #4 shipped below. WER spike vs. Parakeet still owed. **Overlay 3-style system shipped (2026-07-11)** — OVERLAY_SPEC §3's Full/Orb/Whisper-line presentations + an `overlayStyle` picker, per `docs/superpowers/specs/2026-07-11-overlay-styles-design.md` and `docs/superpowers/plans/2026-07-11-overlay-styles.md` (5 tasks, inline execution on branch `overlay-styles`), closing the "only Full built" gap flagged in the docs audit. Architecture: one fixed transparent click-through `NSPanel` (unchanged), `OverlayView` now a router dispatching on a session-frozen `sessionOverlayStyle` (captured from `overlayStyle` at each `overlay.show`, so a mid-session change never reshapes a live overlay, per §3) to `FullStyleOverlay` (today's pill, extracted verbatim — suite-green is the regression proof), `OrbStyleOverlay` (96pt circle, `OmOrbView` only), or `WhisperLineOverlay` (132×38 lozenge: vector micro-ॐ via `OmGlyph` + 5 `audioLevel`-driven mint bars). Minimal styles morph to a labeled `ErrorCapsule` on error so failures always surface (§3) — a pure SwiftUI layout change, no panel resize. `UI/OverlayStyle.swift` (enum + card copy), `WhisperLineOverlay.swift` (holds the pure tested `barHeight(amplitude:index:phase:)` helper with a 0.15 floor so bars stay alive when there's no live audio), `OrbStyleOverlay.swift`. `AppState`: `overlayStyle` setting (access/withMutation) + `sessionOverlayStyle`/`overlayPreview` + `previewOverlay(_:)` — a canned warming→listening→finalize→exit HUD demo for the picker's Preview button that drives a `previewAmplitude` override on `audioLevel` (orb/bars look live without real audio) and **never touches `dictation`** (no history/paste/sounds/menu-icon side effects). New "Recording overlay" `PorcelainSection` in `GeneralSettingsView` — three selectable cards with live mini-previews (breathing mini-orb, rippling mini-bars, mini-pill) + the Preview button. 8 new tests (`OverlayStyleTests` ×4, `WhisperBarsTests` ×4), all passing (278 total). One real edge-path bug caught in self-review (no review subagent this pass, inline execution) and fixed: a settings Preview running when the user pressed a global dictation hotkey would clobber the real session's overlay — fixed by cancelling the preview Task at both real-dictation entry points and guarding the preview's teardown on `dictation == .idle` so it abandons quietly if a real session took over. **Live verification owed**: each style rendering over another app during a real dictation (Full = regression check), the minimal-style NOTHING HEARD capsule morph, style-change-applies-next-session, the picker's live previews + Preview button (no history/paste), and Reduced-Motion degradation. |
| M2 — Daily-driver parity | 🔶 In progress | PTT/sounds/paste-hardening/menu-bar states already shipped under M1 (see above). Vocabulary UI + engine biasing shipped (2026-07-07): `Vocabulary/VocabularyProcessing.swift` (whole-word replacements + length-gated bounded-Levenshtein fuzzy correction, ported from the Tauri app's `engine.rs`/`vocab_correct.rs`), `UI/VocabularySettingsView.swift`, `SettingsView` restructured into a `TabView` (General/Vocabulary). History + importer shipped (2026-07-07), per `docs/superpowers/specs/2026-07-07-history-importer-design.md`: GRDB added as SPM dependency (linked to both the app and test targets); `History/HistoryStore.swift` (GRDB `DatabaseQueue`, `transcriptions` table via `DatabaseMigrator`, record/fetchPage/search/delete/deleteAll/deleteOlderThan/storageInfo/exportAll — txt/md/json ports of the Rust `export_history`); `History/LegacyHistoryImporter.swift` (one-time read-only copy from the old app's `com.omwhisper.app/history.db`, gated by `hasImportedLegacyHistory`); `UI/HistoryView.swift` (paginated/searchable list, tap-to-expand rows, bulk-select delete, txt/md/json export via `NSSavePanel`, Clear All, Auto-Delete-After stepper) wired as a new `Window("History", id: "history")` scene + menu-bar item. `HistoryStore`/`TranscriptionEntry`/`LegacyHistoryImporter` are `nonisolated` (GRDB I/O has no MainActor affinity — see AppState concurrency note); `AppState.init()` opens the store and runs import+cleanup via a `nonisolated` background-Task launcher, matching the `AudioCapture` pattern. 33 new tests (`HistoryStoreTests`, `LegacyHistoryImporterTests`), all passing; live-verified against the real Tauri app's history.db (94 rows imported correctly, rendered in the History window). Also fixed a bug the `Window` scene introduced: macOS's window-state restoration (triggered by Xcode's Stop button killing the process uncleanly) was auto-reopening Settings/History at next launch, and closing that window then quit the whole app — fixed via `.defaultLaunchBehavior(.suppressed)` on both scenes plus `AppDelegate.applicationShouldTerminateAfterLastWindowClosed` returning `false` (menu-bar/`LSUIElement` apps must survive their last window closing). Launch-at-login shipped (2026-07-07): `AppState.launchAtLogin` wraps `ServiceManagement.SMAppService.mainApp` directly (no UserDefaults mirroring — the system login-item registry is its own source of truth), toggle added to `GeneralSettingsView`; live-verified both directions via `sfltool dumpbtm` (`disposition` flips enabled ⇄ disabled). Audio/About settings tabs shipped (2026-07-07), ported from the old app's Settings.tsx Audio/About sections (researched directly, not assumed — analytics/crash-reporting toggles, feedback form, and update UI deliberately dropped: no backing infra in this app, and updates are Sparkle's job). `Capture/AudioCapture.swift` gained mic device selection: `availableInputDevices()` via `AVCaptureDevice.DiscoverySession`, `start(preferredDeviceUID:)` resolves the UID to a `AudioDeviceID` via `AudioObjectGetPropertyData`/`kAudioDevicePropertyDeviceUID` and applies it with `AudioUnitSetProperty(kAudioOutputUnitProperty_CurrentDevice)` before `engine.start()` (AVAudioEngine has no higher-level device-picker API on macOS) — persisted UID, not name (old app matched by name, which breaks for two identical mic models). `AppState.soundVolume` (default 0.2, matches old app) applied via `NSSound.volume` in `SoundPlayer`. `UI/AudioSettingsView.swift` (input device picker, sound toggle + volume slider — no live level meter, matching the old app which only had one in the recording overlay, not Settings) and `UI/AboutSettingsView.swift` (version from `CFBundleShortVersionString`/`CFBundleVersion`, doc link, footer credit) added as new `SettingsView` tabs; "Recording sounds" toggle moved from General into Audio. Live-verified on real hardware: device picker lists actual devices (USB mic, Continuity iPhone mic, built-in), explicitly selecting the built-in mic and running a real start/stop dictation cycle worked end-to-end (proves the CoreAudio device-ID resolution path, not just the picker UI). Sparkle runtime wiring shipped (2026-07-07) — code only, not yet live (no signing key/appcast published, see below). Added as SPM dependency (app target only). `SPUStandardUpdaterController` on `AppDelegate` + "Check for Updates…" menu item (enabled state bound to `updater.canCheckForUpdates`). `SUFeedURL` = `https://omwhisper.in/appcast.xml` — chosen over a bare GitHub Pages URL because the Tauri app is being deprecated 2026-07-08 and `omwhisper.in` becomes this app's actual home (see `tauri-app-deprecation` memory); confirmed via research that `omWhisperWebApp`'s existing `version.json`/`updater.json` are Tauri-updater-format (JSON + minisign sig) and structurally incompatible with Sparkle's appcast.xml + EdDSA, so nothing there was reusable beyond the hosting slot. Hit and fixed a real Xcode issue along the way: `GENERATE_INFOPLIST_FILE`'s `INFOPLIST_KEY_*` synthesis only recognizes Apple's own known keys — a third-party key like `SUFeedURL` resolves fine as a build setting but is silently dropped from the generated Info.plist. Fixed with a `PBXShellScriptBuildPhase` ("Patch Info.plist (custom third-party keys)") that PlistBuddy-injects it after generation; needed `ENABLE_USER_SCRIPT_SANDBOXING = NO` on the app target too (Xcode's script-phase sandbox otherwise blocks the write with "Operation not permitted", and declaring the same path as a formal `outputPaths` entry instead causes a "Multiple commands produce Info.plist" conflict with the real generator). Live-verified: menu item appears, clicking it doesn't crash (fails gracefully — no appcast published yet), Info.plist correctly contains `SUFeedURL` alongside every pre-existing key. **Explicitly not done yet** (needs the user's go-ahead first — semi-irreversible/public): generating the real EdDSA signing keypair (`SUPublicEDKey` not in Info.plist), publishing an actual `appcast.xml`, and the `build-release.sh` `generate_appcast` step. Onboarding first-run flow shipped (2026-07-11), per `docs/superpowers/specs/2026-07-11-onboarding-first-run-design.md` and `docs/superpowers/plans/2026-07-11-onboarding-first-run.md`, executed via `superpowers:subagent-driven-development` (4 tasks, fresh implementer + reviewer subagent per task, all approved; branch `onboarding-first-run`, worked in place — this session works in-repo, not in a worktree). The never-wired `docs/onboarding-prototype.html` became a real 4-step dark-identity `Window("Welcome")`: Welcome → Permissions (front-loads mic+speech via a new `AppState.requestDictationPermissions()` — today they ambush the user on first dictation) → **a REAL live-dictation "Try it" demo** → Done (teaches ⌘⇧V + Fn, launch-at-login checkbox). The prototype's "say *continue*" wake-word step and fake target-app/stats theater were dropped (no wake-word feature; the demo is real). The try-it step runs the genuine capture→transcribe→overlay pipeline but suppresses paste + history via a new `AppState.onboardingDemoActive` flag guarding `stopDictation`'s two `phase == .pasting` blocks, mirroring the live `finalizedTranscript`/`volatileTranscript` into an in-window field (so it never pastes → never trips the deliberately-deferred Accessibility permission). Shown once, gated by a new `hasCompletedOnboarding` UserDefaults flag opened on first launch inside `applicationDidFinishLaunching`'s existing `guard !isRunningUnderTests`; `#if DEBUG` "Reset Onboarding…" menu item replays it. `UI/OnboardingView.swift` (pure `OnboardingStep` step-machine + `wordsPerMinute` helper — the only unit-tested parts, 8 tests — plus the container + 4 private step views + 3 dark-only styling helpers; reuses the real `OmOrbView(.dark)`, `.porcelainWindow(colorScheme: .dark)`, and only named `Color.om*` tokens since `Color(hex:)` is `private` to `OmColors.swift`). Dark identity per the scope rule (ignores the appearance picker). 8 new tests, all passing (270 total). The final whole-branch review (opus) caught two real edge-path bugs the per-task reviews couldn't see across, both fixed before merge: (1) tying `onboardingDemoActive` to `.onAppear`/`.onDisappear` leaked — a demo session still recording when the user hit "Skip setup"/Cmd+W would clear the flag while the session ran on, so the eventual stop pasted the demo text into the frontmost app + wrote history; fixed by bracketing the flag to the *session* instead of the view (set on the `.recording` transition, cleared at `stopDictation`'s tail), so a session that outlives the step is still suppressed and a no-dictation visit never sets it; (2) the try-it step was a dead-end for anyone who denied permission or wouldn't speak (only "Skip setup", which *ends* onboarding, skipping the Done step + launch-at-login) — fixed with a gated "Skip this step →" that advances to the finale, matching the spec's "denial is never a dead end" rule. Consciously-left minors (final review): WPM under-reports slightly (measures to `.idle` incl. the finalize drain); global hotkeys are live on non-try-it steps (a real session there just writes a history row, paste no-ops into the window); Cmd+W ≠ completion (onboarding reappears next launch). **Live verification owed** (no Swift toolchain live-run this pass): first-run auto-open on a fresh flag; the mic/speech system prompts on the Permissions step; the core proof — holding Fn (and ⌘⇧V) starts a real dictation, the overlay shows, words appear in the field, and **nothing pastes into another app or writes history** (the `onboardingDemoActive` bracket, incl. the Skip-setup-mid-record leak case the fix closes); launch-at-login checkbox + Start-dictating dismissing for good. |
| M3 — AI polish | ✅ Sub-project 1 + 2 (2a Ollama + 2b Cloud) shipped | Split into two sub-projects during brainstorming — sub-project 1 (styles + `SystemLLM`/Foundation Models backend + Smart Dictation + Polish Selected Text) shipped 2026-07-07, per `docs/superpowers/specs/2026-07-07-m3-core-ai-polish-design.md` and `docs/superpowers/plans/2026-07-07-m3-core-ai-polish.md`, executed inline (7 tasks) in an isolated worktree. `Polish/PolishStyles.swift`: 7 built-in styles (Professional/Casual/Concise/Translate/Email Format/Meeting Notes/Smart Correct — correcting a stale "6 built-ins" reference elsewhere; Smart Correct was added later upstream in the old Tauri app), fixed UUIDs so `activePolishStyleID` survives relaunches, one canonical prompt per style (not the old app's short/blunt-for-tiny-model variant — Foundation Models is meaningfully more capable than that app's bundled 0.5B GGUF). `Polish/SystemLLM.swift`: wraps `LanguageModelSession`/`SystemLanguageModel` (API verified directly against the macOS 26 SDK's `FoundationModels.swiftinterface`, not guessed), fresh session per call, 5s timeout race via `withThrowingTaskGroup`. `AppState` gained `polishBackend`/`activePolishStyleID`/`translateTargetLanguage`/`customPolishStyles` settings, `beginSmartDictation()` (Cmd+Shift+B, refactored `toggleDictation()` into a shared `toggleOrStop(smart:)` rather than duplicating the state machine), `beginPolishSelectedText()` (Cmd+Shift+P), and a unified fallback: any reason polish doesn't produce text (backend Disabled, Apple Intelligence off, model error, timeout) pastes the *original* text unconditionally — fixing a real gap in the old app, where one specific failure mode silently dropped the user's dictated text instead of falling back. `Paste/PasteService.swift` gained `copySelection()` (simulated Cmd+C, mirroring the existing `paste()` save/restore pattern in reverse — chosen over `kAXSelectedTextAttribute`, which is inconsistently supported across third-party apps). New `POLISHING` overlay sub-phase reuses the existing closed color palette (no new colors, per `OVERLAY_SPEC.md` §2); the orb needed zero changes since it already renders calm at `dictation == .idle`, which is Polish Selected Text's state throughout (confirmed by reading `OmOrbView`'s existing floor/glow functions before assuming a change was needed). New `UI/AISettingsView.swift` tab (backend picker, style picker, target-language picker, custom style CRUD). 14 new tests, all passing (78 total). Two real, non-obvious bugs found via live verification (by the user, not caught by the automated suite or by my own AX-based headless testing, which repeatedly proved unreliable this session): (1) `PasteService.copySelection()` compared the freshly-copied text against the *prior* clipboard content and treated a match as "nothing selected" — so re-selecting text that happened to already be on the clipboard silently no-op'd Polish Selected Text even though a real copy had just occurred; fixed to rely solely on `NSPasteboard.changeCount`, which increments on every write regardless of content equality. (2) The AI tab's backend radio buttons didn't visually update until switching Settings tabs and back — root-caused to `polishBackend`/`activePolishStyleID`/`translateTargetLanguage`/`customPolishStyles` being plain computed properties over `UserDefaults` with no stored backing: `@Observable` only auto-instruments *stored* properties, so a plain get/set computed property never fires a change notification on its own. Invisible for every pre-existing Toggle-bound setting in this file (a `Toggle`'s own click animation looks correct regardless of whether the view body actually re-renders), but a `.pickerStyle(.radioGroup)` `Picker` needs a genuine Observation signal to re-highlight the selected option. Fixed the four new settings with manual `access(keyPath:)`/`withMutation(keyPath:)` calls — Apple's documented pattern for `@Observable` properties backed by external storage. **This same gap existed on every other pre-existing UserDefaults-backed setting in `AppState.swift`** (`pasteAfterStop`, `soundEnabled`, `fuzzyVocabCorrection`, `contextAwareDictationEnabled`, etc.) — masked there by `Toggle`'s forgiving behavior. **Closed 2026-08-01 (PR #4)**: all 8 remaining properties instrumented — see the M5 row for the two that were genuinely broken in shipped 2.0.4. Live-verified end-to-end on real hardware by the user directly (automated AX-based verification proved too flaky this session — see below): Smart Dictation with backend Disabled pastes raw text unchanged; Smart Dictation with backend System + a filler/self-correction-laden sentence produces genuinely cleaned-up text (fillers removed, self-correction resolved) with the overlay showing `POLISHING`; Polish Selected Text replaces a selection in place with polished text and no-ops cleanly when nothing is selected; no stray processes left running. Sub-project 2 (Ollama + Cloud backends, Keychain API key storage, test-connection UI) intentionally deferred, not started. **Sub-project 2 was split during brainstorming (2026-07-10) into 2a (Ollama, local) and 2b (Cloud, egress) — the user chose "Ollama first, cloud later,"** so the Cloud backend + its egress-privacy decision (redact-PII vs. warn-only) are deferred to 2b, and no Keychain changes were needed here (Ollama is local, no API key). **M3-2a — Ollama polish backend shipped 2026-07-10**, per `docs/superpowers/specs/2026-07-10-m3-2a-ollama-polish-backend-design.md` and `docs/superpowers/plans/2026-07-10-m3-2a-ollama-polish-backend.md`, executed inline (6 tasks) in `.worktrees/m3-2a-ollama-polish`. `Polish/Ollama.swift`: `nonisolated struct Ollama: PolishBackend` — `POST {baseURL}/api/chat` (`stream:false`, system+user messages), with pure directly-tested static helpers (`chatURL`/`tagsURL` incl. trailing-slash trimming, `requestBody`, `parseChatContent`, `parseModelNames`) mirroring `CloudEngine`'s helper style, plus `checkStatus(baseURL:)`/`listModels(baseURL:)` (`GET /api/tags`) for the Settings UI; a fixed 30s timeout (`ponytail`-marked — the paste path's raw-text fallback covers a timeout, so it's not a user setting). Ported the old Tauri app's `strip_llm_wrapper` as `Polish/PolishPostProcessing.swift` (`stripLLMWrapper` + `isMetaCommentary`/`stripInlineCommentary`, all `nonisolated` — the project's MainActor-by-default pinned the two private helpers until marked, a real build error), applied to **Ollama output only** (`SystemLLM` left byte-for-byte unchanged — Foundation Models is well-behaved and already shipped). Extracted the `{language}` substitution `SystemLLM` did inline into a shared `PolishStyle.systemPrompt(targetLanguage:)` (both backends now build identical prompts; `SystemLLM.instructions` deleted). `AppState`: `PolishBackendKind += .ollama` (Cloud still deferred), `ollamaBaseURL` (default `http://localhost:11434`)/`ollamaModel` settings (access/withMutation), and a single `activePolishBackend() -> PolishBackend?` dispatch that **both** `polishedText(for:)` (dictation stop-paste, Smart Dictation, Polish Selected, Re-polish) and Reply Assist's `draftAndStream` route through — so both honor the Ollama choice; the fail-safe (any failure → paste raw) and the one-time Foundation-Models-unavailable nudge (fired only for `.system`-but-off) are preserved. **Deliberate scope boundary**: Chronicler stays System-only (its map-reduce chunking is tuned to `SystemLLM`'s 5s/2000-char envelope; with `.ollama` selected, chronicle auto-generation is inactive, same as `.disabled`) — a `ponytail`-noted follow-up. `UI/AISettingsView.swift`: an Ollama radio row + a reveal section (base-URL `porcelainField`, a Test Connection button → `checkStatus`+`listModels` populating a model `Picker` from `/api/tags`, "no models installed" / returning-user hints, and a "nothing leaves this device" note). 15 new tests (`PolishStyleSystemPromptTests` ×3, `PolishPostProcessingTests` ×6 incl. the inline-commentary edge case, `OllamaTests` ×6 pure helpers — no network, matching `CloudEngineTests`), all passing (234 total). **Live verification owed** (needs a real Ollama install): the actual localhost round-trip isn't unit-tested — with Ollama running + a model pulled, select Ollama, Test Connection populates the model list, a real Smart Dictation / Polish Selected cycle produces Ollama-polished text, and stopping with Ollama unreachable falls back to raw text. Matches M4.1/M4.2's "pure pieces tested, live round-trip separate" status. **M3-2b — Cloud (OpenAI-compatible) polish backend shipped 2026-07-10** (completes M3 sub-project 2, and with it M3), per `docs/superpowers/specs/2026-07-10-m3-2b-cloud-polish-backend-design.md` and `docs/superpowers/plans/2026-07-10-m3-2b-cloud-polish-backend.md`, executed inline (6 tasks) in `.worktrees/m3-2b-cloud-polish`. The one genuine design decision — egress privacy — was made by the user (2026-07-10): **port the redactor** (privacy-first) over warn-only. `Polish/Redactor.swift`: a faithful `NSRegularExpression` port of the old Tauri app's `redactor.rs` — a 10-detector registry (PEM private keys, AWS/Slack/GitHub/Google/OpenAI keys, Bearer tokens, EMAIL, Luhn-validated CARD, PHONE heuristic, high-entropy SECRET), overlap resolution (earliest-start wins, ties by detector priority), stable typed `[REDACTED_<TYPE>_<n>]` placeholders (same value → same placeholder), `redact(_:) -> Redaction`, and `Redaction.rehydrate(_:)` (order-independent — bracket-delimited placeholders are never substrings of each other). Validators (`luhnValid`/`isPhone`/`isHighEntropySecret`/`shannonEntropy`) ported line-for-line. `Polish/CloudLLM.swift`: `nonisolated struct CloudLLM: PolishBackend` whose `polish()` **redacts first, unconditionally** (Cloud is the only cloud text path — no separate `prepare_outbound` gate needed), builds the system prompt via `PolishStyle.systemPrompt` + a "keep `[REDACTED_…]` verbatim" clause when anything was scrubbed, `POST {apiURL}/chat/completions` (`Bearer` key, temperature 0.3), `stripLLMWrapper`s the result (reused from 2a), then `rehydrate`s — fail-closed. Pure directly-tested helpers (`completionsURL`/`requestBody`/`parseContent`/`placeholderInstruction`) + a static `testConnection` ("Hello." → "OK") for the UI. `Transcription/Keychain.swift` generalized: extracted a private account-parameterized `load/save/delete(account:)` core; the M4.2 AssemblyAI methods now delegate to it (unchanged behavior — its `KeychainTests` stayed green); added `cloudLLMAccount = "cloud-llm-api-key"` + `loadCloudLLMKey`/`saveCloudLLMKey`/`deleteCloudLLMKey`. `AppState`: `PolishBackendKind += .cloud`, `cloudAPIURL` (default `https://api.openai.com/v1`)/`cloudModel` (default `gpt-4o-mini`) settings (the key stays in Keychain, read on demand), and a `.cloud` case in `activePolishBackend()` returning `CloudLLM(...)` only when a key is saved (else nil → raw fallback). `polishedText(for:)`/`draftAndStream` were **unchanged** — they already route through `activePolishBackend()` (2a), so **Reply Assist via Cloud is auto-redacted for free**. `UI/AISettingsView.swift`: a Cloud radio row + reveal section (API-URL/model `porcelainField`s, a `SecureField` + Save/Clear backed by `Keychain`, a Test Connection button, saved-status line, and an upfront honest privacy note — text is sent, but secrets/PII are redacted before egress). 17 new tests (`RedactorTests` ×11 — the full ported suite incl. Luhn-card-only, phone-not-year-range, high-entropy-vs-plain-word, stable placeholders, rehydrate; `CloudLLMTests` ×5 — URL/body/parse/placeholder-instruction; `KeychainTests` +1 — cloud-key round-trip), all passing (251 total). One real build-vs-SourceKit divergence worth noting: the `nonisolated(unsafe) static let detectors` array is genuinely needed (the real compiler requires it — `[Detector]` isn't Sendable because of the `NSRegularExpression`/closure fields), even though SourceKit repeatedly flagged it as "unnecessary" — the actual `xcodebuild` errors when it's dropped, so the persistent SourceKit warning is a false alarm (verified both directions). **Deliberate scope, same as 2a**: Chronicler stays System-only; redaction is cloud-only (System/Ollama are on-device); one-shot (no streaming); OpenAI-compatible providers only. **Live verification owed** (needs a real API key): the actual `/chat/completions` round-trip isn't unit-tested — with a key saved, select Cloud, Test Connection succeeds, a real Smart Dictation / Polish Selected produces cloud-polished text, a sentence with an email + fake card round-trips (redact → rehydrate) with the real values intact, and clearing the key falls back to raw. Matches M4.2/2a's "pure pieces tested, live separate" status. **OLLAMA OPERATIONAL FINDINGS 2026-08-02 — three latent bugs, all invisible until someone ran a large model.** Surfaced by switching to `gemma4:8b` and clicking Generate today; every one had been shipped since M3-2a. **(1) The fixed 30s timeout killed the FIRST call to any large model.** Measured on gemma4:8b: **5.0s warm, 20.0s semi-warm, 36.4s from cold** — and Ollama evicts after ~5 minutes idle, so cold is the NORMAL case. The Standard template (the default, used by Transcribe & Summarize) is the first call of a run and failed every time. Split into `Ollama.dictationTimeout = 30` and `Ollama.longFormTimeout = 300`; meetings and chronicles take the long one (no fallback, the user is deliberately waiting), dictation keeps the short one (its fallback pastes the user's own words, so a slow model costs the polish and not the text). The `ponytail` note on the original constant had literally predicted this — "promote to a user setting only if people with big models ask". **(2) Every URLSession failure was collapsed into `.unreachable`**, so a timeout read as "Couldn't reach Ollama. Is it running?" — it *was* running. Same misleading-diagnosis shape as "Apple Intelligence is off" when it is on; now `.timedOut(seconds)` names the real cause. It paid for itself within the hour: `timedOut(30.0)` is what revealed the fix hadn't reached the test harness. **(3) REASONING MODELS RETURNED EMPTY CONTENT.** gemma4/qwen3.5/deepseek-r1 emit a hidden reasoning trace out of the SAME output budget as the answer. Measured with a chronicle-sized chunk: **2,464 chars of `thinking`, 599 of `content`, `done_reason: length`** — truncated. With longer input the budget was spent entirely on reasoning and `content` came back EMPTY, surfacing as "Ollama returned an empty response" and failing whole chronicles. `requestBody` now sends **`think: false`**; the identical request then finishes with `done_reason: stop`. Verified harmless on non-reasoning models (llama3.2, qwen2.5-coder) — the field is ignored, not rejected. **MODEL SIZING — measured on this 16 GB Mac, same transcript.** Bigger download ≠ better model: gemma4 is 8.0B params in **9.6 GB**, qwen3.5 is 9.7B in **6.6 GB** (quantised harder). | model | size | full diagnostics run | templates | attribution | invention | · llama3.2 | 2.0 GB | — | — | **refuses everything** | — · qwen2.5-coder:7b | 4.7 GB | **1m21s** | 5/5 complete | **wrong** | **invents "cortisol"** · **qwen3.5** | 6.6 GB | 3m38s | 5/5 complete | **correct** | minor · gemma4 | 9.6 GB | ~15–20 min | 5/5 but drops headings | correct | minor. **qwen3.5 is the best balance** and is what both apps are set to. **A 9.6 GB model on a 16 GB Mac froze the machine** — swap was already 22.3 GB of 23.5 GB before loading it, and evicting gemma4 alone freed ~5.4 GB. **Recommending a model on answer quality without checking whether the hardware can hold it is a real mistake, made here.** **Meeting Ask needs ≥7B**: `llama3.2:3b` returns "Nothing relevant." for content plainly present, making the feature look broken. |
| M4 — Backend flexibility | ✅ M4.1 + M4.2 shipped | M4 was split during brainstorming into M4.1 (this — the local CoreML alternative engine, no new privacy surface) and M4.2 (CloudEngine — provider choice, redaction on egress, Keychain key storage), matching this project's established pattern for multi-subsystem milestones. M4.1 — ParakeetEngine + backend selector UI shipped 2026-07-09, per `docs/superpowers/specs/2026-07-09-m4-1-parakeet-engine-design.md` and `docs/superpowers/plans/2026-07-09-m4-1-parakeet-engine.md`, executed inline (5 tasks) in `.worktrees/m4-1-parakeet-engine`. Directly addresses the migration plan's flagged risk #1 (SpeechTranscriber accuracy is "the big unknown," with ParakeetEngine as the documented promotion candidate) — a formal WER comparison between the two engines was explicitly deferred per brainstorming decision; only the engine + picker shipped. FluidAudio (new SPM dependency, app-target only, mirrors Sparkle's existing app-only pattern) added via hand-edited `project.pbxproj` `XCRemoteSwiftPackageReference`/`XCSwiftPackageProductDependency` entries, following GRDB/Sparkle's exact existing pattern (no `Package.swift` in this project). Research into FluidAudio's actual API (cloning the repo and reading source directly, not from training-data memory) found a real fork: `StreamingAsrManager`-conforming engines (EOU/Nemotron variants) have genuine incremental streaming via a partial-transcript callback but **no vocabulary biasing**; `SlidingWindowAsrManager` — despite its own doc comment explicitly saying it does *not* conform to `StreamingAsrManager` ("offline encoder with overlapping windows, not a cache-aware streaming architecture") — actually has both: `streamAudio(_:)`/`transcriptionUpdates: AsyncStream<SlidingWindowTranscriptionUpdate>` for live partials (`isConfirmed` maps directly onto this app's `.final`/`.partial`) AND `configureVocabularyBoosting(vocabulary:ctcModels:)`. Chose `SlidingWindowAsrManager` specifically because it's the only one satisfying both this app's streaming-partials UX goal and sign-off criterion #4 (vocabulary respected). `Transcription/ParakeetEngine.swift`: unlike `AppleEngine` (a stateless struct recreated per session), a `final class` holding one persistent, lazily-created `SlidingWindowAsrManager` for the engine's lifetime — loading Parakeet's CoreML models is expensive (multi-second) and must not repeat per dictation session; `reset()` (not model reload) is the per-session boundary. Vocabulary boosting needs a *second*, separate model (`CtcModels`, the CTC keyword-spotting model) — only downloaded lazily when the caller actually has custom vocabulary, so most Parakeet users never pay that extra download. Mutable `manager`/`ctcModels` state guarded by `OSAllocatedUnfairLock` (matching `AudioCapture`'s established pattern) rather than actor isolation, since `transcribe()` must stay `nonisolated` per the `TranscriptionEngine` protocol. `AppState` gained `engineKind: EngineKind` (default `.apple` — ships an opt-in alternative, not a default-experience change), `parakeetEngine` (not private — the Settings UI reads `isReady`/calls `ensureModelsLoaded` directly), and a computed `activeEngine` replacing the old fixed `engine` property at the one `engine.transcribe(...)` call site. New `UI/TranscriptionSettingsView.swift` tab: radio picker (Apple/Parakeet selectable, Cloud shown disabled "coming in a future update"), a "Download Parakeet Model" button wired to FluidAudio's own `loadModels(progressHandler:)` (real download progress, no custom SHA-pinned downloader needed unlike the old Tauri app's hand-rolled ONNX Parakeet downloader — architecturally unrelated anyway, reference-only). 2 new tests (`ParakeetEngineTests`, testing the pure `isConfirmed`→`.final`/`.partial` mapping function directly rather than importing FluidAudio into the test target — kept FluidAudio app-target-only end to end), all passing (176 total in the full suite). Three real, non-obvious bugs found and fixed during implementation, none anticipated by the design or plan: (1) the SPM version requirement (`minimumVersion = 0.4.0`) resolved to a real but *stale* tagged release from September 2025 that predates `SlidingWindowAsrManager`/vocabulary boosting entirely — caught immediately by a real "cannot find type" build failure, root-caused to the brainstorming research having read FluidAudio's `main` branch via GitHub's contents API rather than the actual tagged release a real SPM dependency resolves to; fixed by checking all available tags, confirming the same API exists identically at the latest (`v0.15.5`), and bumping the version requirement — a concrete lesson that "read the default branch" and "read what a versioned dependency will actually resolve to" are different research questions. (2) `ParakeetEngine` (like `MCPServer` in S5.2) silently became `@MainActor` under this project's `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` default, breaking `nonisolated func transcribe` and the pure `mapUpdate` test calls — fixed by marking the class `nonisolated`. (3) A latent, pre-existing gap: `TranscriptEvent`/`EngineKind` (in the original M1-era `TranscriptionEngine.swift`) were never marked `nonisolated`, masked until this sub-project's tests were the first code in the whole project to actually compare two `TranscriptEvent` values with `==` from a nonisolated context — fixed by adding `nonisolated` to both. Live-verified end-to-end on real hardware: a normal dictation cycle with no Settings changes confirmed Apple's engine is completely unaffected (the critical regression check, since `activeEngine` must default to `.apple` for every existing user); Parakeet model download showed real, advancing progress; a real dictation session with Parakeet active produced live partials in the overlay and correct final pasted text; a custom vocabulary word triggered the one-time CTC model download on first use and was transcribed correctly; switching back to Apple worked cleanly with no leftover state. No formal WER benchmark run (explicitly deferred, see above). M4.2 — CloudEngine (AssemblyAI streaming) shipped 2026-07-10, per `docs/superpowers/specs/2026-07-09-m4-2-cloud-engine-design.md` and `docs/superpowers/plans/2026-07-10-m4-2-cloud-engine.md`, executed inline (6 tasks) in `.worktrees/m4-2-cloud-engine` — this was the last M4 sub-project, so M4 is now fully complete. Provider chosen after fetching AssemblyAI's and Deepgram Flux's live docs directly (not assumed): AssemblyAI's ephemeral-token flow and simple `end_of_turn`-based Turn events matched this app's existing `TranscriptEvent`/`vocabulary` contract with no semantic reconciliation, where Deepgram Flux's turn-detection model would have fought this app's own PTT-defines-the-end design. Every API shape used in the implementation (token endpoint, WebSocket query params, Turn/Termination message JSON) was verified live against `assemblyai.com/docs` during plan-writing, not carried over from the design's earlier summary — a deliberate response to the M4.1 lesson that unverified API research produces real build failures. `Transcription/Keychain.swift` (new, first Keychain usage in this codebase): a minimal `SecItemAdd`/`SecItemCopyMatching`/`SecItemUpdate`/`SecItemDelete` wrapper scoped to exactly one named generic-password item (service = bundle ID, account = `"assemblyai-api-key"`) — the API key never touches `UserDefaults`. `Transcription/CloudEngine.swift`: a stateless `struct` (like `AppleEngine`, unlike `ParakeetEngine`'s persistent class — a WebSocket connection has no warm-up cost worth amortizing across sessions) with pure, directly-tested helpers (`cappedKeyterms` — 100 terms max, each truncated to 50 chars, AssemblyAI's own documented limits; `connectionURL` — builds the `wss://streaming.assemblyai.com/v3/ws` URL with `sample_rate=16000&encoding=pcm_s16le&format_turns=true` plus a JSON-encoded `keyterms_prompt` when non-empty; `parseServerMessage` — decodes a `Turn` message's `end_of_turn` into `.final`/`.partial`, returns nil for every other message type) wired into an effectful `transcribe()`: reads the Keychain key, exchanges it for a 60s ephemeral token via `GET /v3/token` (this one call uses `Authorization: Bearer <key>` — note the WebSocket connection itself uses the token with no Bearer prefix, a real asymmetry in AssemblyAI's own API), opens a `URLSessionWebSocketTask`, converts mic buffers to 16kHz mono Int16 PCM via the existing `BufferConverter`, sends binary audio frames concurrently with a receive loop draining Turn events, and on mic-stream-end sends `{"type":"Terminate"}` with a `ponytail`-marked fixed 1s drain wait before closing (event-driven wait for the `Termination` message specifically was deliberately skipped as unnecessary complexity — revisit only if live testing shows truncated endings). Hit the exact same `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` gotcha `ParakeetEngine`/`MCPServer` hit before it — `CloudEngine`'s nested `TurnMessage` Decodable conformance and `sampleRate` constant were unreachable from the type's own `nonisolated static` helper functions until the whole struct was marked `nonisolated`, caught immediately by a real build error during Task 2, not anticipated by the plan. `mergeEngineVocabulary` (new, `Vocabulary/VocabularyProcessing.swift`, alongside `applyReplacements`/`fuzzyCorrect`): replaces the inline screen-term merge at `AppState`'s `engineVocabulary` construction site — Apple/Parakeet keep getting the user's custom vocabulary plus S2's auto-extracted screen terms (case-insensitive deduped), but Cloud gets only the user's own explicitly-typed `customVocabulary`, since screen-auto-extracted terms were never reviewed or approved by the user and selecting Cloud shouldn't silently widen what leaves the device beyond the audio itself; a `log.debug` line fires whenever Cloud excludes a non-empty screen-terms list, giving the exit criterion's "verifiable via a debug log line" requirement a concrete home. `UI/TranscriptionSettingsView.swift` extended: the Cloud radio row (previously disabled with "coming in a future update") is now selectable, revealing a new section with a `SecureField` for the API key, Save/Clear buttons backed directly by `Keychain.swift` (no `AppState` setting for the key itself), a saved/not-saved status line, and an upfront privacy/cost warning line shown in the same view as the radio button, never gated behind a separate disclosure step. 20 new tests (`KeychainTests` — real Keychain round-trip in the test host process, no mocking, matching `HistoryStore`/`MemoryStore`'s "not scary, just I/O" precedent; `CloudEngineTests` — keyterm capping/truncation, URL construction, Turn/Begin/Termination/malformed-JSON message parsing; `EngineVocabularyMergeTests` — Apple/Parakeet merge screen terms, Cloud excludes them entirely even with zero custom terms), all passing (196 total in the full suite). Honest verification-status note: every pure piece is unit-tested, but the actual WebSocket connection against AssemblyAI's real servers was **not** live-verified in the implementation pass (no key available then). **Live-verified 2026-07-11** — and the first real run exposed three bugs in the never-tested auth path, all corrected (the never-live-verified path was exactly where the errors hid): (1) the token request and WS upgrade used `Authorization: Bearer <key>` / a token-in-header, but AssemblyAI uses the **raw key with no `Bearer`**, and a native app is the "server-side" case that puts the raw key **directly in the WS `Authorization` header** — so the ephemeral-token round-trip was dropped entirely (it exists only for untrusted browser clients, where the token goes in `?token=`, never a header); (2) `speech_model=universal-3-5-pro` is **required** on realtime and was missing; (3) `format_turns` is not a Universal-3.5-Pro knob (formatting tracks `end_of_turn`, which `parseServerMessage` already keys on) and was dropped. Root-caused against AssemblyAI's official coding-agent docs (the user pasted them) — this is exactly the "unverified API research produces real failures" lesson from M4.1, now confirmed on the cloud path too. Also added `CloudEngine.testConnection(apiKey:)` (validates the key against the token endpoint) + a Settings **Test Connection** button and a clear "key saved to Keychain" status (the AssemblyAI section previously just blanked the field on save, reading as if no key existed). `connectionURLBaseParams` test updated (drops `format_turns`, asserts `speech_model`/`mode`). **M4 is now fully proven end-to-end** (Apple live from M1, Parakeet live from M4.1, Cloud live now); the formal WER benchmark across engines remains the only deferred M4-adjacent item. **WER benchmark SHIPPED 2026-08-01** (`OmWhisper --wer <corpus-dir>`, DEBUG; `Benchmark/WER.swift` pure+12 tests, `Benchmark/WERBenchmark.swift` runner reusing `MeetingTranscriber.transcribeFile`, `scripts/make-wer-corpus.sh`, findings in `docs/wer-corpus/README.md`). First numbers on a synthetic `say` corpus (8 samples, 168 words, M2 Pro), biasing off: Whisper turbo 2.4% · ElevenLabs 3.0% · Parakeet v3 4.2% · Whisper small 4.2% · Parakeet v2 4.8% · **Apple 6.0%** · Whisper base 8.3%. RTF: Apple/Parakeet ~0.04–0.05x, Whisper turbo 0.45x, ElevenLabs 0.18x — **on-device is not the slow option**. Absolute numbers are a floor (synthetic speech is clean/unaccented/close-mic'd) and the corpus is too small to rank engines that land close together — one word is 0.6% WER. **The real result is the vocabulary A/B: custom vocabulary has no measurable effect on Apple Speech or either Parakeet variant** (byte-identical transcripts), which puts sign-off criterion #4 and S2 in doubt — see the North Star section above for the evidence and caveats. Cloud providers other than ElevenLabs were skipped (no keys). Two methodology lessons worth keeping: the runner originally **truncated printed hypotheses to 60 chars**, which caused a real misreading of the first run (the failing word was off-screen, so the wrong word got blamed — truncation removed); and the first A/B was run with a vocabulary word (`async`) that Apple/Parakeet **already got right**, so it measured nothing until genuinely hard jargon was added. **Whisper engine + Parakeet model picker + download-state overhaul shipped 2026-07-11/12** (post-M4, user-requested — reverses the documented "Whisper/Moonshine engines" scope-drop), design+plan at `docs/superpowers/specs/2026-07-11-whisper-engine-design.md` / `docs/superpowers/plans/2026-07-11-whisper-engine.md`, executed inline on `main` via short fix-branches (no worktree) per this session's live-testing rhythm. **Parakeet picker**: `ParakeetModel` enum (v3 multilingual default / v2 English) + `AppState.parakeetModel` synced to the engine (which caches one model in memory, so switching variant evicts it). **Whisper engine** (`Transcription/WhisperEngine.swift` + pure `WhisperModel.swift`, WhisperEngineTests): a 4th `TranscriptionEngine` via WhisperKit (new SPM dep, app-target-only), motivated by language coverage Apple/Parakeet lack (Indic etc.). Transcribe-on-release (Whisper is chunk-based, not streaming): accumulate the mic stream → 16kHz mono Float32, one `transcribe(audioArray:)` on stream-end → single `.final`, no partials. Curated 3 (Base/Small/Large v3) + a language picker (Auto-detect + WhisperKit `Constants.languages`); vocabulary → decoding prompt via `tokenizer.encode`. **Started on WhisperKit 0.18.0, upgraded to 1.0.0** mid-debug (the bump dropped swift-transformers, kept every API used except the removed `usePrefillCache`; 1.0.0 API verified against source before bumping — the M4.1 lesson again). **The large-v3 debugging saga** (long, entirely live-diagnosed from on-device `os.Logger` output; base/small worked immediately, large-v3 returned empty through five wrong hypotheses each ruled out by real logs): (1) a mid-download false "Ready" (`isDownloaded` checked folder-non-empty; a slow multi-GB download flips it true early → dictating an incomplete model — fixed to require the core `.mlmodelc` files + render progress before the disk check), (2) ANE-vs-GPU compute (a numerically-correct GPU encoder ALSO returned empty → ruled compute out), (3) the model build (`_turbo` vs `_626MB`, both empty), (4) `usePrefillCache` (1.0.0's notes flagged removing that path, but disabling it on 0.18.0 didn't help), (5) the WhisperKit version (1.0.0 — still empty). **Root cause: OpenAI's `large-v3-v20240930` "turbo" checkpoint is broken in WhisperKit** (empty in every config); the FULL `large-v3_947MB` transcribes reliably. WhisperKit's own pruned `large-v3_turbo` was faster but **inconsistent** (pruning drops utterances), so the full model shipped — reliable-slow beats fast-flaky for dictation; a UI note warns large models are slow + points to Base/Small. **Download-state overhaul** (both engines): `isReady`/"Ready" now means DOWNLOADED-ON-DISK (`AsrModels.modelsExist` for Parakeet; a WhisperKit model-folder check at swift-transformers' `~/Documents/huggingface/…` default), not loaded-in-memory — so "Ready" survives switching variants, navigating hub sections, and relaunch; Whisper's `transcribe` lazy-loads a downloaded model from disk (never a big download). Download progress + the download `Task` moved from the transient Settings view into `AppState` (stored `@Observable`) so a download survives the view being recreated on hub navigation (it previously orphaned + reset). **Two latent Swift 6 concurrency errors** surfaced by a clean build (incremental compilation had cached stale objects under a since-stricter toolchain), fixed at root: `MeetingConsentPanel`'s `@Sendable` repeating `Timer` → a MainActor-inherited `Task`; `MemoryStore.prune`'s unused `deleteAll` result → `_ =`. **One pre-existing timezone bug** fixed en route: `HistoryStore.homeStats` computed the Home streak in local time while `wordsToday`/`last7` use SQLite UTC — read 0 in early-morning UTC+ hours; pinned the streak to UTC. Full suite 279→283, live-verified on real hardware across every engine/model. **Cloud multi-provider shipped 2026-07-12** (spec+plan `docs/superpowers/{specs,plans}/2026-07-12-cloud-multi-provider*`, 5 tasks inline on short fix-branches): `CloudEngine` refactored from AssemblyAI-only into a dispatcher over `CloudProviderKind` (AssemblyAI + Deepgram = streaming WS; ElevenLabs + OpenAI + Groq = batch transcribe-on-release). AssemblyAI logic extracted to `AssemblyAIProvider` (its existing tests moved + stay green = regression proof); `DeepgramProvider`; `BatchCloudTranscriber` (a per-provider `Config` + `pcmToWav` minimal WAV encoder + multipart POST + `.text` parse, shared by the 3 batch providers). **Key architecture fact**: a `sending AsyncStream` can't be forwarded into a provider function without a Swift 6 data-race error, so `CloudEngine` OWNS the audio loop (consumes it inline, like every other engine) and providers own only their wire format (request/parse/close, or batch config) — NOT the audio stream. Per-provider Keychain keys via `Keychain.load/save/deleteSTTKey(provider)` (`.assemblyAI` keeps its M4.2 account); `AppState.cloudProvider` setting; `activeEngine` builds `CloudEngine(provider:)`; `TranscriptionSettingsView` gained a provider `Picker` + per-provider key field/Test/privacy note; engine picker label is now "Cloud (online)" not "Cloud (AssemblyAI)". Default models: Deepgram `nova-3`, ElevenLabs `scribe_v1`, OpenAI `gpt-4o-transcribe`, Groq `whisper-large-v3-turbo` (all verified vs live docs). All 4 new provider APIs verified against live docs 2026-07-12. 20 pure tests (`CloudProviderKind`, WAV/multipart/parse, Deepgram URL/parse). Suite 289. **ElevenLabs + AssemblyAI live-verified by the user**; Deepgram/OpenAI/Groq live round-trips owed (need real keys). |
| M5 — Beta → release → freeze | 🔶 In progress | **Feature audit done 2026-07-31** — the whole app checked against its own claims rather than assumed from this tracker. Verdict: complete and well ahead of where the tracker said, with four real gaps (all since closed). **P0 (blocks the first public .dmg): (1) Sparkle can never update anyone** — `SUFeedURL` is set but `SUPublicEDKey` is absent from Info.plist, no appcast is published, and `build-release.sh` has no signing step (just a reminder at lines 151–153). The public key is baked into the shipped binary, so a 2.0 that ships without it is *permanently* un-updatable — the one irreversible item in the release. Deliberately not done: needs the user's explicit go-ahead (generating the EdDSA key + publishing an appcast are public acts). **(2) No single-instance guard — SHIPPED 2026-07-31** (`SingleInstance.swift`): the old app had `ensure_single_instance()` (`lib.rs:251`); this one had nothing, so two copies meant two `CGEventTap`s on one hotkey, two mic grabs, two daemons writing the same SQLite files. Matched by **bundle ID, not path** — the case that actually happens is two *copies* of the bundle (a tester's `~/Downloads` build alongside `/Applications`, or Xcode Run over an installed one); Finder double-clicks are already deduped by LaunchServices, these are not. Uses `NSRunningApplication` natively — no PID lock file (the old Rust version's `kill -0` dance was working around not having AppKit, and a lock file adds a stale-lock failure mode). `enforce()` is called from `main.swift`'s **GUI branch only**, so `--mcp` is exempt *by construction* rather than by an arg list that could drift — that subprocess is meant to run alongside a live app. The losing launch posts a `DistributedNotificationCenter` ping and the winner opens the hub, since a menu-bar app that exits silently reads as "it didn't launch". Live-verified: second launch logged the first's pid and exited 0 with exactly one process left; `--mcp` answered an `initialize` request alongside a running instance; the winner received the ping. **P1: (3) clipboard-restore delay is not a setting** — `PasteService.paste(_:restoreDelay:)` defaults to 2s and *no call site passes it* (`AppState.swift:1185,1390`), so Appendix B's "w/ delay setting" doesn't exist; also missing the old `restore_clipboard` on/off flag. **(4) debug info + log export — SHIPPED 2026-07-31** (`DebugInfo.swift` + a Copy Debug Info button in About): **no log file and no rotation was written** — the app already logs via `os.Logger` and the unified log *is* the rotating log with macOS owning retention, so `OSLogStore(scope: .currentProcessIdentifier)` reads our own entries back with no entitlement. That hinged on an empirical check, since os_log is noted elsewhere as dead on dev builds: a probe run as a `swift` script returned `<compose failure [missing precomposed log]>` for every line, but inside the real `.app` bundle messages come back clean — the script result was a red herring (format-string metadata isn't recoverable for interpreted code). Config + permissions only, never transcription text/window titles/memory snapshots; keys report `key saved`/`no key`, never a value — it gets pasted into issues, so it must be safe to hand over unread. Dumping it from the real app is what caught three wording bugs (`yesNo(_:"key saved")` negating to "not key saved", `operatingSystemVersionString` already carrying "Version", and a UTC header above local-time log lines). **(3) SHIPPED 2026-07-31**: `AppState.restoreClipboard` (default on) + `clipboardRestoreDelayMS` (default 2000, clamped ≥0 on read — a stale UserDefaults negative would go straight into `Task.sleep`), both routed through one `pasteRespectingClipboardSettings` helper so the two paste sites can't drift; `PasteService.paste` gained `restoreClipboard:` and skips the restore entirely when off; controls under Settings → General. The new settings use `access`/`withMutation` — `pasteAfterStop` beside them did not, deliberately at the time: that's the pre-existing `@Observable` gap flagged in the M3 row, a Toggle's animation hides it and a Stepper wouldn't, and fixing one of ~8 leaves the rest lying. **All 8 fixed 2026-08-01 (PR #4)** — and the "Toggles hide it, so it's cosmetic" reasoning turned out to be wrong twice over. `customVocabulary`/`wordReplacements` back a `ForEach`, and `autoDeleteAfterDays` a `Stepper`, so three were never Toggle-masked at all: **deleting a custom vocabulary word left the row on screen** in shipped 2.0.4 until you navigated away and back. Adding one looked fine only *by accident* — `addWord()` also sets `newWord = ""`, and that `@State` write forces the re-render the setting itself never signalled; `removeWord()` writes no `@State`, so nothing repainted. No unit test: constructing `AppState` opens the real history/memory stores and runs the auto-delete cleanup, which is the same trap `KeychainTests` fell into when it was silently deleting real API keys — so this is build-clean + 346 tests green + a live click check (Hub → Vocabulary → delete a word). **(5) CapsLock PTT — WITHDRAWN 2026-07-31, was never a gap.** The old app's `spawn_ptt_for_key` opens `if key != "Fn" { return None }` — **CapsLock PTT never worked in the Tauri app**; it appeared in the picker and in `MACOS_ONLY_KEYS` (`lib.rs:632`) but the CGEventTap (`fn_key.rs`) only ever handled Fn and Left Ctrl. Right ⌥/Right ⌃ were dead there too and this app implements both for real, so native is *ahead* on PTT. Not worth building either: CapsLock latches — macOS emits `flagsChanged` on lock-state toggle, not press/release, so hold-to-talk isn't expressible (you'd get press-to-start/press-to-stop, a stuck LED, and nothing for anyone who's remapped the key). The audit had compared the two apps' *picker options* rather than their behaviour; **general lesson for the rest of Appendix B — a checklist entry describes what the old UI offered, not what it did.** **(6) The app had NO ICON — found 2026-07-31 by R, missed by the audit entirely.** `AppIcon.appiconset` had all ten slots defined and zero images, so no `AppIcon.icns` was ever compiled and the bundle had no `CFBundleIconName`: it would have installed into `/Applications` as the generic macOS placeholder (`ASSETCATALOG_COMPILER_APPICON_NAME` was already set — the catalog was simply empty). Rebuilt from the committed `OmGlyph` vector rather than re-cropping the Tauri raster (which was a circle on transparency carrying a ring + dashed texture — reads smaller among squircles and looks wrong inside one; Apple's HIG asks for the rounded rect). `scripts/make-app-icon.swift` **compiles `OmGlyph.swift` in rather than duplicating its geometry**, so the icon can't drift from the mark the HUD/menu bar draw — re-run it to regenerate, never hand-edit the PNGs. Apple's grid (824×824 body in a 1024 canvas, radius 185.4) as fractions so every size shares proportions and each is rendered natively, not downsampled; existing tokens only (`#0A0F0D` ground, emerald→teal glyph gradient at 135°, `omBorder` hairline replacing the old disc's ring); corners are a plain rounded rect, not a true continuous-curvature squircle (noted in the script, indistinguishable at icon sizes — Icon Composer if pixel-exact ever matters). Verified by extracting `AppIcon.icns` back out of the built bundle and looking at it at 512px and 32px. Menu-bar icon was already fine (real vector `OmGlyph`, proper template mode). **Appendix B itself is stale** and the audit says so: "launch sound" describes something the old app never had (`sounds.rs` bundles only start/stop, played from `commands.rs:223,536` at record start/stop), "6 built-in styles" is 7, and Whisper is listed as dropped when it shipped 2026-07-11. **CI resurrected 2026-07-31** — it had been red for **53 of 54 runs** (green once, 2026-07-07) and was therefore being ignored. It was telling the truth the whole time: `SalientTermExtractor.rareWords()` called `checkSpelling(of:startingAt:)`, which honours `NSSpellChecker.automaticallyIdentifiesLanguages` — **ON by default on a fresh account, off on this machine**. With it on the checker infers a language from the text itself, so a short snippet reads as French/Italian and **"classe" is a real word in both** — the rare word the pass exists to surface is exactly the one that disappears. So S2's `contextualStrings` biasing has been quietly worse for every user on the default while the tests looked green locally. Fixed by pinning to `checker.language()` (the user's own spell-check language — non-English users still get theirs, but the result no longer depends on a per-call guess). Diagnosed by pushing a throwaway probe test and reading the runner's real state rather than guessing between "service absent" and "language misconfigured" (different fixes); that one round-trip also *proved the fix before it was written* — the same runner returned `NSNotFound` for the auto-language check and `loc=0 len=6` for the explicit-language one. The tell was hiding in plain sight: `commonWordsNotFlaggedAsRare` passed throughout precisely because it expects nothing. Merged as PR #1 (`m5-release-readiness`); **main and PR CI both green, 343 tests in 50 suites** — the first green run since 2026-07-07. Merged as PR #2 (`m5-polish`); main green, **346 tests in 51 suites**. **The audit's P1 list is now clear.** **DISTRIBUTION PATH PROVEN END TO END 2026-07-31.** Developer ID certificate already existed (`Developer ID Application: … (Y87BZN47C5)`, valid to 2031 — an earlier audit note claiming none was installed was wrong, from reading only the tail of `security find-identity`). `build-release.sh` needed two real fixes before it worked: it read `CFBundleShortVersionString` from `omwhisper-native/Info.plist`, **a file that doesn't exist** under `GENERATE_INFOPLIST_FILE` (every .dmg would have been `OmWhisper_unknown_arm64.dmg` — now reads `MARKETING_VERSION` from build settings), and the archive step passed only `CODE_SIGN_IDENTITY`, which failed two ways (`GRDB_GRDB … requires a development team`; `conflicting provisioning settings … automatically signed, but code signing identity … manually specified`) — fixed by also passing `CODE_SIGN_STYLE=Manual`, `DEVELOPMENT_TEAM` and an empty `PROVISIONING_PROFILE_SPECIFIER`, leaving the checked-in project on automatic signing for development. `.env` was **not** gitignored despite this file claiming it was — fixed before any credential was written to it; `*.p12`/`*.pem`/`*private*key*` are ignored too. Notarization Accepted first try, stapled. **`SUFeedURL` and `NSAudioCaptureUsageDescription` both survive into Release** — that PlistBuddy phase had only ever run under Debug and was the biggest unknown. **Sparkle shipped**: EdDSA key generated (`SUPublicEDKey = KWxanNe25QRF/+jZypGqO+K9s3Cp30ptU8YOrmBgvgY=`, injected via the same PlistBuddy phase, verified inside the notarized .dmg), and `build-release.sh` now runs Sparkle's own `generate_appcast` (pointed at a directory holding only the .dmg — the exported .app and staging copy would otherwise read as separate updates). Both private keys exported and backed up by R. **One verification trap found**: the script's `✓ Gatekeeper: accepted` was a false green — `spctl --status` reports `assessments disabled` on this machine and `spctl --assess` then accepts anything; the script now warns instead. `source=Notarized Developer ID` is the signal that actually means something. Also noted: the ticket staples to the .dmg, not the enclosed .app, so a dragged-out app verifies online at first launch (normal for .dmg distribution). **Live-verified by R 2026-07-31 from the notarized .dmg installed to /Applications**: onboarding first-run on a cleared `hasCompletedOnboarding` works (owed since 2026-07-11), install/permissions/icon all as expected, and the **single-instance guard passed its real two-bundle-copies case** (`/Applications` vs. the export dir; second launch logged the first's pid and exited, one process left). **Remaining for M5 — hosting, not code**: the appcast's `enclosure url` is `https://omwhisper.in/OmWhisper_2.0.0_arm64.dmg` and `appcast.xml` must be served at `https://omwhisper.in/appcast.xml` to match `SUFeedURL` — auto-update stays inert until both URLs are live (S6 territory, now on the critical path). Then the remaining live backlog (real multi-person call, Deepgram/OpenAI/Groq round-trips, D4b motion + dark mode, cross-engine WER) → beta soak → tag the Tauri repo `v-final`. ~~Remaining for M5 — the distribution path, which has NEVER been run~~: no git tags, no releases, no `.env`, and **no Developer ID Application certificate installed** (only an "Apple Development" cert, which cannot notarize) — checked 2026-07-31, so `build-release.sh`'s archive→notarize→dmg chain has never executed once. Strictly ordered: Developer ID cert + app-specific password (R's to create) → `build-release.sh` runs → a real .dmg exists → Sparkle key signs it → appcast published → auto-update proven. `build-release.sh` also sets no DMG volume icon, so the disk image mounts generic — fold in while that script is being changed anyway. That first fresh install closes three owed live verifications at once: onboarding first-run on a clean `hasCompletedOnboarding`, the single-instance guard in its real `/Applications`-vs-`~/Downloads` case, and whether a Release build's Info.plist PlistBuddy patch phase + TCC behave like Debug's (**that phase has only ever run under Debug**). **SHIPPED 2026-07-31: v2.0.0 → v2.0.3, and auto-update is proven end to end.** `v2.0.0` published (notarized, stapled, appcast live). `v2.0.1` existed only to exercise the update path — no functional change — and it worked: the installed 2.0.0 fetched the feed **over the apex→www 307 redirect** (that install predated the SUFeedURL correction), compared build numbers, **verified the EdDSA signature against the baked-in public key**, downloaded, installed and relaunched, staying `codesign --verify --deep --strict` clean and `source=Notarized Developer ID` afterwards. It did all that **silently**, because `SUAutomaticallyUpdate` was on — which is why the button looked dead and is why `v2.0.2` registers `SUAutomaticallyUpdate: false` as a *default* (register, not write, so a user who ticks Sparkle's own box keeps that choice). `v2.0.2` also fixed the sidebar hardcoding both its version ("2.0" while running 2.0.1) and **"listening locally"** — a claim it could not know and would have made while streaming to a cloud engine. `v2.0.3` fixed **Check for Updates, which had never worked since the day it was written** — see the Verification section above; two stacked bugs, a hit region limited to the letterforms and `NSApp.delegate as? AppDelegate` always returning nil because SwiftUI's `@NSApplicationDelegateAdaptor` installs a forwarding shim rather than your object. That same nil cast had silently broken the single-instance ping, which was previously (wrongly) recorded as verified. Both now use `AppDelegate.shared`. `build-release.sh` gained a **build-number guard** that reads the live appcast and refuses to build unless `CURRENT_PROJECT_VERSION` exceeds what is already published — Sparkle compares on that number, and leaving it alone ships a release nobody is ever offered. **Docs shipped 2026-07-31 (v2.0.4)** — see the S6 row. **Release publishing automated 2026-08-01** (`scripts/publish-release.sh`, spec+plan `docs/superpowers/{specs,plans}/2026-08-01-release-publish-automation*`), prompted by finding that **omwhisper.in served 2.0.0 for four consecutive releases** — i.e. every visitor since 2.0.1 downloaded the build with silent auto-updates, a dead Check for Updates button and the wrong Documentation link. Two independent causes, both fixed: the site's `DOWNLOAD_URL` was a **version-pinned constant** nothing bumped (now derived from `appcast.xml` at site build time via a `?raw` import, so it cannot disagree with what Sparkle offers; needed `src/vite-env.d.ts`, which this Vite project never had), and `build-release.sh` **ended by printing a reminder** to upload the .dmg and publish the appcast by hand — an instruction that cannot fail and cannot be observed, the same shape as the three items in the Verification section above. `build-release.sh --publish` now runs preflight *before* archiving (gh auth via `gh api user`; clean tree in **both** repos — for the native repo that's correctness, since `gh release create` tags whatever is checked out, so a dirty tree yields a tag that doesn't match the shipped binary), then creates/reuses the release, uploads with `--clobber`, copies the appcast into the web repo and pushes it, then **verifies against reality**: live appcast build number, the deployed site bundle's .dmg filename, and an HTTP 200 on the enclosure URL **read out of the deployed feed** (the only check that can catch a wrong `--download-url-prefix`, since a local variable can only confirm what the script already believes). Default behaviour unchanged — a bare `build-release.sh` publishes nothing. `--verify-only` answers "is what's live consistent?" any time; `--dry-run` shows the plan and touches nothing. Every check proven in **both** directions before commit (verify passes on live 2.0.4, fails on a bogus build number, fails on a feed with an unreachable enclosure; `--publish` on a dirty tree aborts in 15s rather than after a 10-min notarize). One real bug the plan itself had, caught only by running it: preflight originally used `gh auth status`, which **exits non-zero when any configured account has a stale token** — this machine has three accounts with one expired, so it reported failure while every real `gh` command worked. **Publish path proven at dry-run level only** — the real `gh release create`/push path runs for the first time on the next actual release. Also fixed that day: a footer link to a **nonexistent X handle** (404, `TODO` placeholder) and the footer's "always offline" claim, false since cloud engines shipped — the same class as the FAQ that denied cloud transcription, which the S6 website pass caught and this one survived. **v2.0.5 SHIPPED 2026-08-01 — and the publish path ran for real for the first time.** 50 commits since 2.0.4 (meetings SP1–SP3, editable details, dev-build isolation, Light default). `build-release.sh --publish` did the whole chain unattended: archive → notarization **Accepted** → staple → `gh release create` with the first hand-written notes (`docs/releases/v2.0.5.md`; 2.0.0–2.0.4 all used `--generate-notes`) → appcast pushed → Vercel deploy → then **verified against reality**: live appcast reports build 6, the deployed site serves the 2.0.5 .dmg, and the enclosure URL *read out of the live feed* downloads. It waited out a window where the site still served 2.0.4 rather than declaring success early. Note `--dry-run`/`--verify-only` live on `publish-release.sh`; `build-release.sh` only takes `--publish`. Gatekeeper's `✓ accepted` remains meaningless on this Mac (assessments disabled — the script says so itself); notarization-Accepted + successful staple is the signal that means something. **Remaining:** the live backlog (real multi-person call, Deepgram/OpenAI/Groq round-trips, D4b motion + dark mode, cross-engine WER on real speech) → beta soak. **v2.0.6 SHIPPED 2026-08-02** (46 commits: memory semantic search, web-area capture, visible-windows capture, exclusions + `.env`, the Foundation Models locale gate, chronicles-via-Ollama). Notarization Accepted, stapled, verified against the live feed (appcast build 7, site serves the 2.0.6 .dmg, enclosure read out of the deployed feed returns 200). **Two release-path lessons, both new:** (1) **`build-release.sh --publish | tail -60` reports exit 0 even when the script fails** — a pipeline returns the LAST command's status. The first 2.0.6 attempt "succeeded" while producing no .dmg, no tag and no release; only checking the actual world (`gh release list`, `ls .build-release`) caught it. Never pipe the publish through anything. (2) **The Release archive built universal (arm64 + x86_64) and `Float16` is unavailable on x86_64**, so the semantic-search vector codec broke the archive — invisible to 439 passing tests and every Debug build, because Debug is `ONLY_ACTIVE_ARCH = YES`. Fixed at root by pinning `ARCHS = arm64` in both project-level configs: the app is Apple-Silicon-only by design and the .dmg was already *named* `_arm64` while shipping a universal binary, so this makes the build match the documented platform rather than working around the compile error. **Any Release-only build failure now has a precedent: check the architecture before the code.** |
| S1–S6 — Smriti integration | ✅ S1–S6 all shipped (S6 completed 2026-08-01) | `docs/SMRITI_INTEGRATION_PLAN.md`. S2 — context-aware dictation shipped (2026-07-07), per `docs/superpowers/specs/2026-07-07-s2-context-aware-dictation-design.md`, built via `superpowers:subagent-driven-development` (5 tasks, fresh implementer + reviewer subagent per task, all approved). `Context/SalientTermExtractor.swift`: `properNouns(in:)` (NLTagger `.nameType` — on-device NER for personal/place/org names), `codeIdentifiers(in:)` (regex catching camelCase/PascalCase/snake_case/dotted.paths, including acronym-prefixed identifiers like `NSAttributedString`/`URLSession` via a case-transition lookahead rather than an enumerated pattern list), `rareWords(in:)` (`@MainActor` — `NSSpellChecker` has main-thread affinity, unlike the other two pure-Foundation passes; anything the system dictionary doesn't recognize, a free proxy for "technical/rare" with no bundled corpus), `extractSalientTerms(from:limit:)` (merges all three, case-insensitive dedupe, caps at 30). `Context/ScreenContextReader.swift`: ported from `smriti`'s `AXReader.swift` (same author, MIT, read-only reference) with a 0.6s time budget (vs Smriti's 2.0s — this sits on the critical path to first-partial latency) and hardcoded exclusions (password managers, private/incognito browsing) matching Smriti's `Config.defaults` verbatim; `BrowserURL.swift` deliberately not ported (nothing in this pass calls it — the design spec listed it as "for future use," which the implementation plan correctly treated as scope creep to drop). `AppState` fires the capture the instant `dictation = .starting` is claimed (same moment as the overlay's early-show), concurrently with permission checks/`audioCapture.start()` via a `nonisolated` Task launcher; awaited once right before `engine.transcribe(...)`, merged into a separate `engineVocabulary` value — `vocabSnapshot` itself (which also feeds `fuzzyCorrect`'s harder post-hoc rewrite) stays exactly `customVocabulary`, unchanged. `contextAwareDictationEnabled` toggle in the Vocabulary settings tab, off by default (every Smriti-derived feature ships off by default). 13 new tests, all passing (64 total in the full suite). Two real bugs found and fixed along the way, both via live testing rather than caught by review alone: (1) every `xcodebuild test` run — via CLI or Xcode's own Cmd+U — was launching a real, fully-interactive instance of the app (live global-hotkey `CGEventTap` actually intercepting real keystrokes, a menu-bar `NSStatusItem` that didn't self-terminate, a real Sparkle update check); fixed with `isRunningUnderTests` (`NSClassFromString("XCTestCase") != nil` — the only one of three candidate checks that's launch-method-agnostic; env-var and launch-argument checks each only worked for one invocation path) guarding `AppState.init()`'s hotkey/PTT/permission-prompt/HistoryStore setup, `AppDelegate.applicationDidFinishLaunching`'s status item, and Sparkle's `startingUpdater`. (2) `rareWords`'s `while` loop could hang on a zero-length `NSSpellChecker.checkSpelling` match (never advancing `offset`) — since it's a synchronous `@MainActor` call with no internal `await`, a stuck loop monopolized MainActor and blocked every other task waiting on it, not just its own caller; fixed with a length guard plus a 500-iteration safety cap. Live-verified end-to-end on real hardware: toggled the setting on in a running instance, brought a text-heavy window to front, ran a real start/stop dictation cycle through the menu bar — no crash, clean menu teardown, setting persists correctly; toggled back off and confirmed the same clean cycle. (Automated/headless verification couldn't produce real spoken audio, so the `start-to-first-partial` latency claim from the design spec's Timing section — that concurrent-with-startup timing adds no perceptible delay — is architecturally sound and code-reviewed but not independently re-measured against the 1.3–2.0s baseline in this pass; worth a real spoken-word check before considering the latency risk fully closed.) S3 sub-project 1 (meeting detection, consent, recording) shipped 2026-07-08, per `docs/superpowers/specs/2026-07-07-s3-meeting-detection-consent-recording-design.md` (amended) and `docs/superpowers/plans/2026-07-07-s3-meeting-detection-consent-recording.md` (amended), executed inline (7 tasks) in `.worktrees/s3-meeting-detection`. `Meetings/CallDetection.swift` (pure bundle-ID/AX/URL matching — allowlist of Zoom/FaceTime/Teams/WhatsApp/Slack/Discord/Webex, verification-required apps also need frontmost or a call-like window title), `Meetings/MeetingWatcher.swift` (2s CoreAudio mic-in-use poll driving a pure, fully-tested `nextState` state machine: idle → micActive → prompting → recording/declined, with debounce/timeout durations), `Meetings/MeetingConsentPanel.swift` (interactive `NSPanel` with a 10s countdown — not `OverlayPanel`, which is deliberately click-through), `Meetings/MeetingRecorder.swift`, `UI/MeetingsSettingsView.swift` (single toggle, off by default), debug-only `Meetings/MeetingSelfTest.swift` + a `#if DEBUG` menu item. `MeetingRecorder` went through a significant architecture pivot mid-implementation: the original design (matching `smriti`'s reference implementation) used ScreenCaptureKit's audio-only capture, which shipped and was live-verified working — but the user then raised a firm, correct objection: SCK's audio-only mode still triggers the full Screen Recording permission and a system-wide "Currently Sharing" indicator for a feature that only ever wanted audio, and a real crash (see below) left that indicator orphaned, requiring a logout to clear. Backed by a screenshot showing macOS's own System Settings has a distinct "System Audio Recording Only" permission tier (separate from "Screen & System Audio Recording"), with a real app (Littlebird) already using it. Rewrote `MeetingRecorder` around CoreAudio process taps instead: `AudioHardwareCreateProcessTap` + `CATapDescription.initStereoGlobalTapButExcludeProcesses` taps system-wide output (no screen involvement at all), combined with the real mic device into an `AudioHardwareCreateAggregateDevice` aggregate, read via raw HAL I/O (`AudioDeviceCreateIOProcIDWithBlock`/`AudioDeviceStart`) — not `AVAudioEngine`; an `AVAudioEngine`-based version (pointing `inputNode` at the aggregate via the same `kAudioOutputUnitProperty_CurrentDevice` mechanism `AudioCapture.setInputDevice(_:on:)` uses) never worked live despite the aggregate device itself verifiably having the right channel count, across three targeted fixes — `AVAudioEngine`'s higher-level abstraction evidently doesn't negotiate cleanly with a freshly-created virtual/tap-backed device. Two more real, non-obvious findings from live debugging the CoreAudio-tap rewrite: (1) macOS silently denies process-tap audio delivery (tap/aggregate objects create fine, `AudioDeviceStart` reports success, but the IO callback never fires) unless `NSAudioCaptureUsageDescription` is present in Info.plist — a separate TCC gate from `NSMicrophoneUsageDescription`, no prompt and no error without it; hit the exact same `GENERATE_INFOPLIST_FILE`/`INFOPLIST_KEY_*` synthesis gap Sparkle's `SUFeedURL` hit (M2, this same table), fixed by extending the existing PlistBuddy Info.plist-patch build phase. (2) The aggregate device delivers the mic sub-device and the stereo tap as two *separate* `AudioBuffer`s within one `AudioBufferList` (confirmed via live buffer-shape logging: `mNumberBuffers=2`), not one combined multi-channel buffer as originally assumed — simplified the capture code to deinterleave each buffer independently rather than channel-range-slicing a single combined buffer. Also fixed, from the original SCK version, a real crash (`OmWhisper-2026-07-07-232744.ips`): `MeetingRecorder`'s stream callbacks had no `nonisolated` marker, so were implicitly `@MainActor` by project default, but the real callback queue is a background thread — Swift 6's runtime isolation checker trapped the instant real audio arrived; fixed by following `AudioCapture.swift`'s established pattern exactly (`nonisolated` throughout, mutable state behind `OSAllocatedUnfairLock`). 20 new tests (`CallDetectionTests`, `MeetingWatcherLogicTests`), all passing (98 total in the full suite). Live-verified end-to-end on real hardware, final CoreAudio-tap architecture: debug self-test captures both tracks correctly (system audio -8.7dBFS OK, mic -17.8dBFS OK); System Settings confirms OmWhisper listed under "System Audio Recording Only", not the full Screen Recording section — no video frame, no system sharing indicator, matching the user's original ask. Real-call consent-flow test (detection → 10s countdown panel → accept/decline/timeout with an actual meeting app) and the dictation-suppression test are **deferred, not yet run** — the user chose to postpone that pass rather than block on it this session. Transcription/summary/UI (sub-project 2) remain a separate, not-yet-started plan. **S3 sub-project 2 — meeting transcription/summary/UI shipped 2026-07-11** (completes S3), per `docs/superpowers/specs/2026-07-11-s3-2-meeting-transcription-summary-ui-design.md` and `docs/superpowers/plans/2026-07-11-s3-2-meeting-transcription-summary-ui.md`, executed inline (6 tasks) in `.worktrees/s3-2-meeting-transcription`. Decisions (brainstorming 2026-07-11): **manual trigger** (a per-meeting "Transcribe & Summarize" button, matching S5.1's chronicle button — no background-job orchestration), **one pass**, **strictly on-device** (transcription via `AppleEngine`, summary via `SystemLLM`, *regardless* of the user's dictation/polish backend — recorded calls never egress), **dual-track labeled** (You/Others, no turn-by-turn interleaving since `TranscriptEvent` exposes no per-segment timestamps). `Meetings/MeetingStore.swift` (separate `meetings.db`, GRDB + FTS5 via `synchronize(withTable:)`, mirrors `MemoryStore`; a `Meeting` row is inserted on `onStopRecording` so it shows immediately as "Recorded"). `Meetings/MeetingTranscriber.swift`: reads `me.caf`/`them.caf` via `AVAudioFile` → buffer chunks → an `AsyncStream` fed to `engine.transcribe(...)` (the same contract `AudioCapture` uses live) → joins `.final`s → `labeledTranscript(you:others:)` (pure, tested; `@preconcurrency import AVFoundation` for the buffer/stream boundary). `Meetings/MeetingSummarizer.swift`: word-packed map-reduce (`chunk` keeps content even for one long line, unlike Chronicler's per-snapshot blocks) through the backend, two hidden fixed-UUID `PolishStyle`s (chunk-summarizer + meeting-writer producing `## Summary` + `## Action items` markdown), mirroring Chronicler. `AppState`: `meetingStore` opened independently in `init()`; `meetingStartedAt`/`meetingAppName` captured on `onStartRecording`; `recordFinishedMeeting()` inserts the row on stop; `transcribeMeeting(id:)` (the view's path) transcribes on-device + summarizes if `SystemLLM.isAvailable()` (else transcript-only + the shared Foundation-Models-off nudge), always saving the transcript (`try?` on summary — best-effort). `UI/HubMeetingsSectionView.swift` (toggle bar + searchable meetings list + `NavigationSplitView` detail with the Transcribe & Summarize button, markdown summary/transcript, delete) replaces the toggle-only `MeetingsSettingsView` (deleted — its only reference was the hub `.meetings` case, now repointed; toggle inlined). One real build error, not SourceKit noise: `TranscriptionEngine.transcribe`'s *protocol requirement* wasn't marked `nonisolated` (only the conformers were), so calling it through the protocol existential from `MeetingTranscriber`'s nonisolated code was MainActor-bound and failed — fixed by marking the requirement `nonisolated` (its own doc comment already said "Implementations should mark this nonisolated"; all conformers already do, and MainActor callers are unaffected). 12 new tests (`MeetingStoreTests` ×6 real GRDB round-trips incl. FTS search over transcript+appName and delete-removes-directory; `MeetingTranscriberTests` ×3 `labeledTranscript`; `MeetingSummarizerTests` ×3 `chunk`), all passing (263 total). **Live verification owed** — the one real risk: feeding a recorded `.caf` through the streaming `AppleEngine` is the unproven path (`AppleEngine` converts fed buffers via `BufferConverter` and SpeechAnalyzer is built for long-form, so it should hold, but needs a real run). Record a real Meet/Zoom call → it appears in the list → Transcribe & Summarize produces a plausible You/Others transcript + sensible summary; a meeting with Foundation Models off still stores a transcript. **This also finally exercises S3-1's still-deferred real-call chain** (detection → consent → record → now transcribe) end-to-end. If file transcription fails live, the noted fallback is a file-based `SFSpeechRecognizer`. S4 — reply assist shipped 2026-07-08, per `docs/superpowers/specs/2026-07-08-s4-reply-assist-design.md` (amended) and `docs/superpowers/plans/2026-07-08-s4-reply-assist.md` (amended), executed inline (7 tasks, later amended by 3 more live-verification fix commits) in `.worktrees/s4-reply-assist`. `ReplyAssist/DoubleTapDetector.swift` (pure timing state machine — consumes/resets a pending single tap on a 0.45s window, ported near-verbatim from smriti's `AssistListener.swift`, same author/MIT/read-only-reference), `ReplyAssist/ReplyAssistMonitor.swift` (`NSEvent.flagsChanged` global+local monitors distinguishing right-⌥ via `keyCode == 61` — modeled directly on `PushToTalkMonitor`, NOT smriti's 30ms `CGEventSource` polling, which exists there only because smriti runs as a launchd daemon; `isSuppressed` closure mirrors `MeetingWatcher`'s exact contract), `ReplyAssist/ReplyContext.swift` (AX focused-element resolution with smriti's full fallback chain — per-app → system-wide → flip `AXManualAccessibility`/`AXEnhancedUserInterface` and retry 8×200ms for slow Electron trees → walk for `AXFocused==true` — plus the placeholder-read-as-value gotcha for empty web/Electron fields; `reply`/`continueDraft`/`rewrite` classification is a pure, fully-tested function), `ReplyAssist/ReplyStreamTypist.swift` (synthesized-keystroke streaming with a sentinel-checked buffered prefix so a backend failure never types garbage into the user's field), `ReplyAssist/ToneProfile.swift` (tone.md digest/prompt-shaping helpers — sourced from this app's own `HistoryStore`, not smriti's continuous screen-capture approach, since S1's capture mechanism doesn't exist yet at this point in the build order and the app's own dictation history is a better fit besides). `AppState` gained `replyAssistEnabled` (off by default, same `access(keyPath:)`/`withMutation(keyPath:)` + collaborator-wiring pattern as `meetingsEnabled`) and `beginReplyAssist()`/`draftAndStream(...)`, drafting via a fixed-UUID internal `PolishStyle` (never shown in the AI tab's user-facing picker) through the existing `PolishBackend.polish(_:style:targetLanguage:)` — no protocol changes needed. 19 new tests (`DoubleTapDetectorTests`, `ReplyContextClassificationTests`, `ReplyStreamTypistSentinelTests`, `ToneProfileTests`), all passing (117 total in the full suite). The original design's `ReplyAssistPanel` (type-intent/leave-blank/hold-to-speak UI, plus a scoped voice-capture path bypassing the `dictation` state machine) was built and worked live, but the user changed direction after seeing it in practice: double-tap right ⌥ now silently auto-drafts from context with **no panel and no voice path at all**; a second double-tap while a draft is in flight cancels it (`ReplyStreamTypist.cancel()`), replacing the panel's now-nonexistent Escape handler — the panel file and voice-capture method were deleted outright, not kept behind a flag. Three more real, non-obvious bugs found only via live testing, all fixed in the same pass: (1) `SystemLLM.polish(...)` timed out on every draft — `ScreenContextReader.captureFrontmostWindowText()` can return up to 50,000 characters (fine for S2's local vocabulary extraction) and the AX-read draft/selection text was equally uncapped, so a text-heavy background document blew the 5s on-device timeout; both capped at 2,000 characters, with the continuation case keeping the text's *tail* via `.suffix(...)` rather than its head, since continuing a draft cares about its most recent content. (2) Drafted text initially had nowhere to land — showing the panel called `NSApp.activate(ignoringOtherApps: true)` so it could reliably receive input, which silently stole focus from the target field's app; fixed (and later made moot entirely once the panel was removed, since nothing activates OmWhisper in the final silent-only flow) by capturing `NSWorkspace.shared.frontmostApplication` before showing the panel and reactivating it before streaming. (3) Typing into Claude's web chat produced real, scattered character corruption ("been" → "bn", "Thank" → "Tha") — smriti's ported 20-UTF16-unit chunk size, sent as one synthetic `keyboardSetUnicodeString` event, was too large a burst for that input's event handling to reliably absorb; reduced to single-character chunks. Live-verified end-to-end on real hardware across the final architecture: double-tap in a real field (both a native-ish context and Claude's web chat, a complex non-native input surface) correctly classifies reply/continue/rewrite mode and silently drafts+types clean text; a second double-tap mid-stream cancels cleanly, leaving already-typed text in place; double-tapping while regular dictation is recording correctly does not trigger the assist at all, confirming the suppression contract holds. S1 — memory core shipped 2026-07-08, per `docs/superpowers/specs/2026-07-08-s1-memory-core-design.md` and `docs/superpowers/plans/2026-07-08-s1-memory-core.md`, executed inline (7 tasks, plus 1 live-verification fix commit) in `.worktrees/s1-memory-core`. `Memory/BrowserURL.swift` (ported near-verbatim from smriti — AX-only page-URL resolution across 9 browsers, no AppleScript/Automation permission needed; deferred as scope creep at S2 time, genuinely needed now for domain exclusion). `Context/ScreenContextReader.swift` had 3 members (`collectText`, `copyAttribute`, `textBearingRoles`) changed from `private` to internal so a new `Memory/WindowSnapshotReader.swift` could reuse S2's proven AX tree-walk rather than forking it, while returning richer metadata (`bundleID`/`appName`/`windowTitle`/`url`) than S2's own `captureFrontmostWindowText()` needs — S2's public contract and behavior are unchanged (confirmed: full suite stayed at 124/124 immediately after the visibility change, before any new S1 code was added). `Memory/MemorySnapshot.swift` + `Memory/MemoryStore.swift`: a *separate* GRDB `DatabaseQueue`/file (`memory.db`, not `history.db`) from `HistoryStore` — memory (background capture) and dictation history are differently-sensitive, differently-defaulted data; a user must be able to wipe one without touching the other. Schema is a from-scratch GRDB design (not a port of smriti's raw-SQLite3 `Store.swift`): `snapshots` table + `UNIQUE(bundleID, windowTitle, contentHash)` dedup index + an FTS5 virtual table kept in sync via GRDB's `synchronize(withTable:)` (verified directly against the pinned GRDB 7.11.1 source before writing any code — it auto-generates the AFTER INSERT/DELETE/UPDATE sync triggers, no hand-written trigger SQL needed, confirmed real and worked on the first attempt live). `Memory/MemoryCapture.swift`: `Timer`-driven daemon modeled on `MeetingWatcher`'s poll pattern (5s default interval, matching smriti's own proven default) — NOT smriti's `SIGUSR1` signal handler, which exists there only because smriti runs as a separate launchd CLI daemon with no other IPC channel; pause here is just a settings-backed flag the daemon's `isSuppressed` closure reads each tick. `AppState` gained `memoryEnabled`/`memoryPaused`/`memoryRetentionDays` (same `access(keyPath:)`/`withMutation(keyPath:)` + collaborator-wiring pattern as `meetingsEnabled`/`replyAssistEnabled`) and a `memoryStore` opened independently of `historyStore` in `init()` (one failing to open must not affect the other) via a shared Application Support directory computation. `UI/MemorySettingsView.swift` (toggle + pause + retention stepper only — no search/browse UI, that's S5's job entirely), debug-only `Memory/MemorySelfTest.swift` + a `#if DEBUG` menu item. 25 new tests (`BrowserURLTests`, `MemoryStoreTests`, `MemoryCaptureExclusionTests`), all passing (137 total in the full suite) — notably `MemoryStoreTests` includes a real prune-deletion test (not just the `olderThanDays: 0` no-op path caught in self-review before implementation): `dbQueue` was made internal (not private) specifically so the test could backdate a row's `lastSeenAt` directly and verify `prune()` actually deletes it, since `upsert()`'s production API always stamps "now" with no injectable clock. One real bug found via live testing: `MemorySelfTest`'s search-probe selection used `split(separator: " ")`, which only splits on literal spaces — capturing a newline-separated file-tree listing (Xcode's navigator) produced a giant multi-line "first word" as the probe instead of a real single token; fixed with `split(whereSeparator: { $0.isWhitespace })`. Live-verified end-to-end on real hardware: self-test correctly captures app/bundleID/window title/content and finds it via FTS5 search; **direct `sqlite3` inspection of the real `memory.db`** (not just the self-test's own one-shot write) confirmed the background `Timer` daemon is genuinely populating the store independently — 8 distinct rows captured across Fork/Arc/Xcode/cmux/OmWhisper itself over about a minute of real usage, with correct metadata and small per-app row counts (2 each, not one row per 5s tick), confirming dedup is collapsing unchanged repeat captures rather than growing unbounded. Exclusion behavior (password managers, private/incognito browsing, excluded domains) and the pause/disable toggle paths are **deferred, not yet live-verified** — the user chose to postpone that pass rather than block on it this session; the underlying logic is unit-tested (`MemoryCaptureExclusionTests`, `ScreenContextReader.isExcluded` already proven by S2) but not confirmed against the real daemon end-to-end. S5.1 — chronicles + memory browse UI shipped 2026-07-09, per `docs/superpowers/specs/2026-07-09-s5-1-chronicles-search-ui-design.md` and `docs/superpowers/plans/2026-07-09-s5-1-chronicles-search-ui.md`, executed inline (6 tasks) in `.worktrees/s5-1-chronicles-search-ui`. S5 was split into two sub-projects during brainstorming (S5.1 here, S5.2 MCP server next) and in-app "Memory Chat" was descoped entirely — this app has no `PolishBackend` capable of true multi-turn tool-calling (unlike smriti's approach of shelling out to a live `claude` CLI process against its own MCP server, which needs Claude Code installed plus a subscription), and the MCP server alone already satisfies S5's exit criterion ("what was I working on before lunch?" answerable from Claude Desktop). `Memory/MemoryStore.swift` gained a `chronicles` table (`MemoryChronicle`: day/summary/snapshotCount/createdAt, `day` as primary key, GRDB's `PersistableRecord.save` giving insert-or-replace upsert for free — no hand-written `ON CONFLICT` SQL needed, simpler than smriti's raw-SQLite upsert) plus `fetchPage`/`delete`/`deleteAll`/`storageInfo` (mirroring `HistoryStore`'s exact method shapes) and `upsertChronicle`/`getChronicle`/`listChronicles`/`snapshotsForDay`. `Memory/Chronicler.swift`: the one real architectural departure from smriti — this app has exactly one `PolishBackend` (`SystemLLM`, on-device Foundation Models) whose `polish()` call has a hardcoded 5s timeout that S4 already found needs inputs capped around 2,000 chars, so a whole day's snapshots (smriti pipes up to 120,000 chars through a full `claude -p` CLI call) can't go through in one shot; instead, map-reduce: `chunk(_:limit:)` (pure, greedily packs formatted snapshot blocks into ≤1,800-char groups, a single oversized block gets its own group rather than being split or dropped) each summarized individually via `polish()` (map, two new fixed-UUID internal `PolishStyle`s — chunk-summarizer and chronicle-writer, same hidden-style pattern as S4's reply draft, never shown in the AI tab's picker), then the concatenated chunk-summaries (themselves capped at 1,800 chars) get one final `polish()` call that writes the actual markdown chronicle (reduce) — every individual LLM call stays inside the already-proven-safe zone, zero changes to `PolishBackend`/`SystemLLM`. `Memory/ChronicleScheduler.swift`: `@MainActor` Timer wrapper mirroring `MemoryCapture`'s own poll/prune shape exactly, fires once at `start()` plus every 24h, generates *yesterday's* chronicle only if one doesn't already exist (idempotent — restarting the app repeatedly doesn't regenerate the same day over and over). `AppState` gained a `chronicleScheduler` wired into the existing `memoryEnabled` setter (no new master toggle — implicit under the same flag as `MemoryCapture`), additionally gated on `polishBackend == .system && SystemLLM.isAvailable()` via the same live-read `isSuppressed` closure contract as every other background daemon in this app, plus `regenerateChronicle(day:)` (the view layer's only path to `systemLLM`, which stays `private`). New `Window("Memory", id: "memory")` scene + "Memory…" menu item (mirrors "History…" exactly): `UI/MemoryView.swift` (Snapshots/Chronicles segmented picker; Snapshots tab is a near-verbatim structural copy of `HistoryView`'s proven shape — searchable list, debounced reload, tap-to-expand rows, pagination — deliberately dropping `HistoryView`'s Export menu and multi-select bulk delete as unneeded scope for now) and `UI/MemoryChroniclesView.swift` (`NavigationSplitView` day-list + markdown-rendered detail pane, SwiftUI-native rebuild of smriti's raw-AppKit `ChronicleTimelineSection`, which per this project's standing convention is wireframe reference only). One real design gap caught during the plan's own self-review, before any code was written: the original design only offered a per-day "Regenerate" button inside the detail pane, which is unreachable when zero chronicles exist yet (the normal first-run state) — nothing to select, no button ever appears, so a user could never generate their first chronicle. Fixed by moving to a single always-visible toolbar button ("Generate Today's Chronicle", doubling as regenerate-today since `Chronicler.generate` always overwrites) rather than a per-selected-day action; regenerating an arbitrary *past* day beyond today is intentionally not supported yet (`ponytail` comment marks it — add if it turns out to matter). Also caught in the plan's own self-review, before implementation: a broken `??`/`==` operator-precedence bug in the scheduler's idempotency check, a non-mutating-vs-mutating `PersistableRecord.save` mismatch, and an unproven `#expect(throws:)` async-closure pattern with zero prior usage anywhere in this test suite (swapped for the same do/catch + `Issue.record` idiom already used elsewhere). 19 new tests (`MemoryStoreTests` chronicle/pagination additions, `ChroniclerTests`), all passing (156 total in the full suite) — including one bug the tests themselves caught before any live run: `fetchPage`'s `ORDER BY lastSeenAt DESC` alone is non-deterministic for same-second inserts (`ISO8601DateFormatter`'s default precision is whole seconds, and three `upsert()` calls in one test executed within the same second), fixed with a secondary `Column("id").desc` tiebreaker — a genuine correctness fix for real same-second capture bursts, not just a test artifact. Live-verified end-to-end on real hardware by the user directly: enabling Memory + confirming the AI backend is System, opening the new Memory window from the menu bar, Snapshots tab showing recent captures with working search, and — the core new mechanism — clicking "Generate Today's Chronicle" in the Chronicles tab produced a real markdown-rendered chronicle from actual captured snapshots and a real `SystemLLM` call end-to-end. S5.2 — MCP server shipped 2026-07-09, per `docs/superpowers/specs/2026-07-09-s5-2-mcp-server-design.md` and `docs/superpowers/plans/2026-07-09-s5-2-mcp-server.md`, executed inline (6 tasks, plus 1 live-verification fix commit) in `.worktrees/s5-2-mcp-server`. The real architectural question: this app is a GUI menu-bar app (`@main struct OmWhisperApp: App`), not a CLI tool like smriti, so there was no existing subcommand dispatch to hang an `mcp` subcommand off of. Resolved by removing `@main` from `OmWhisperApp` and adding a new `main.swift` (the target's actual entry point) that branches on `CommandLine.arguments.contains("--mcp")` before SwiftUI's lifecycle starts — calling `OmWhisperApp.main()` explicitly is the same static call `@main` would have synthesized. The `--mcp` path (`MCP/MCPLauncher.swift`) never touches `AppState` — no hotkeys, no `NSStatusItem`, no Sparkle, no audio — it opens `HistoryStore`/`MemoryStore` directly via a new shared `AppSupportDirectory.resolve()` helper (extracted from `AppState.init()`'s inline `FileManager` lookup so both paths resolve the identical on-disk location without duplicating it; confirmed a pure extraction via the full suite staying green immediately after, before any MCP code existed) and runs `MCP/MCPServer.swift`, a stdio JSON-RPC 2.0 server ported from smriti's `MCPServer.swift` (same author, MIT, read-only reference) with GRDB-record field names instead of raw SQLite3 row reads. Six read-only tools: `search_memory`/`get_recent_activity`/`get_snapshot`/`get_chronicle`/`list_chronicles` (over the S5.1 memory/chronicle store, via two new `MemoryStore` methods this task added — `recent(minutes:limit:)` and `getSnapshot(id:)`) plus `search_transcriptions` (over `HistoryStore`, smriti has no equivalent since it doesn't do dictation). Gated by a new `AppState.mcpAccessEnabled` toggle (off by default, same `access(keyPath:)`/`withMutation(keyPath:)` pattern as every other setting) — unlike every other feature toggle in this app it wires no in-process collaborator, since its only effect is being read fresh by a *future* `--mcp` subprocess launch, not anything the current process does; `MCPLauncher.run()` checks it before opening any store and refuses with a clear stderr message + `exit(1)` if off, never silently serving with it disabled. New `UI/MCPSettingsView.swift` Settings tab shows a copy-paste-ready Claude Desktop config JSON block (built from `Bundle.main.executablePath`, so it always reflects wherever this exact running copy actually lives) with a Copy Config button, plus the toggle. 18 new tests (`MemoryStoreTests` recent/getSnapshot additions, `MCPServerTests` — the latter calling `MCPServer.callTool(name:args:)` directly against in-memory stores rather than driving the actual stdio loop, which isn't cleanly unit-testable in-process), all passing (174 total in the full suite, plus what was recorded at the time as one `SalientTermExtractorTests.detectsRareWords()` flake — **that diagnosis was wrong**, see the M5 row: it was a real, deterministic `NSSpellChecker` auto-language bug, and dismissing it as flaky is why it survived until 2026-07-31). Two real, non-obvious build errors hit and fixed during implementation, neither anticipated by the plan: (1) `MCPServer` silently became `@MainActor` under this project's `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` default, which broke calling `callTool` synchronously from plain (non-MainActor) test functions — fixed by marking the class `nonisolated`, matching `HistoryStore`/`MemoryStore`'s existing convention for anything meant to run off the GUI path. (2) Swift 6 then flagged the tool-definitions static array (`[[String: Any]]`) as not provably `Sendable` — fixed with `nonisolated(unsafe)` on that one `let`, safe here since it's immutable literal data never mutated after init, exactly the case the compiler's own suggested fix targets. One real bug found only via live testing, not anticipated by the plan or design: adding MCP as a 9th Settings tab pushed the tab bar past its fixed 520pt window width, so macOS silently collapsed the overflow into a ">>" menu — hiding the new tab entirely on first launch after this change; caught by the user via screenshot, fixed by widening `SettingsView`'s frame to 660pt. Live-verified end-to-end on real hardware: the toggle-off refusal path (piping a raw `initialize` JSON-RPC message into `OmWhisper --mcp` with the toggle at its default `false` correctly printed the disabled-access stderr message and exited 1, no stdout); the Settings UI (toggle, real non-placeholder config path, Copy Config) after the width fix; and the full protocol end-to-end with the toggle on — `initialize`/`tools/list`/`tools/call` piped directly into the real built binary returned correct `serverInfo`, all 6 tool schemas, and a real (empty-but-correct, no captures in the immediate test window) `get_recent_activity` result, proving the complete dispatch → store → render path runs without crashing against the real compiled binary. An actual live Claude Desktop connection (editing its config, restarting it, asking a real question) was **not completed this session** — deferred after being started, matching this session's established pattern of deferring the last, externally-dependent leg of live verification (S1's exclusion tests, S3's real-call test) once the core mechanism is already proven; the MCP protocol layer itself carries no remaining risk, a real Claude Desktop round-trip at this point is client-side plumbing, not a question about this app's own correctness. **S6 — website + documentation shipped 2026-07-31.** Two sub-projects, both live, specs and plans in the **web repo** at `docs/superpowers/{specs,plans}/2026-07-31-website-2.0*` and `*-docs-site*` (that repo gitignores `docs/`, so they exist on disk only). **(1) Website brought in line with 2.0** — the site described the Tauri app while advertising a 2.0 download, and parts of it contradicted the shipped product outright: the FAQ *denied* cloud transcription ("there is no cloud fallback mode") and three places said macOS 13, so a Ventura user downloaded a .dmg that cannot run. Repositioned around "Voice to text, your way" (on-device by default, cloud opt-in) rather than the expired "offline and private"; privacy page rewritten to separate *what reaches us* (nothing — there is no backend) from *what reaches a provider you chose*, naming providers, redaction, and what Memory/Meetings store. `app-screenshot.png` was **retired rather than refreshed** — it had advertised a BETA badge and a Whisper-only model list for months — replaced by `OverlayHUD.tsx`, a CSS rendition of the real HUD built from `OverlayView.swift`'s own geometry, which cannot rot silently and carries no private data. `og-image.png` is regenerated by `scripts/make-og-image.swift` (compiles `OmGlyph.swift` in, so the mark cannot drift; renders at explicit pixel dimensions because `NSImage.lockFocus` honours the display's backing scale and output would otherwise depend on the monitor). **Two claims were found only by looking at the rendered page**, both worse than anything the source audit caught because they were never true: a **"Voice Analysis" panel** scoring the visitor's speech (Clarity 97, Tone 94, Naturalness 98 — no such feature exists anywhere in the Swift codebase) and an invented **99.2% accuracy** figure with no benchmark behind it. **(2) Documentation** at `omwhisper.in/docs` — five hand-written JSX pages (getting started, dictating, engines, beyond dictation, troubleshooting) sharing a `DocsLayout`; no markdown renderer, since five pages do not justify a dependency. The old Jekyll site at `rockykusuma.github.io/omwhisper/` now points Mac users onward while keeping its pages for Windows. **Its Pages build had been failing since 2026-07-06** ("Page build failed" ×2) so it had served stale content for 25 days — Jekyll parses every `.md` under `docs/`, and two internal planning docs contain Liquid syntax in code blocks. Excluding them fixed the build *and* stopped `NATIVE_MIGRATION_PLAN`, `DICTATION_GAP_ANALYSIS`, `WINDOWS_DEV_SETUP` and the `plans/`+`superpowers/` folders from being publicly served — all verified 404 afterwards. One near-miss worth remembering: the web repo's `.gitignore` had unanchored `docs/`, which matches any directory of that name at any depth, so the first two commits silently excluded `src/components/docs` and `src/pages/docs` and would have failed the Vercel build; now `/docs/`. **S6 COMPLETED 2026-08-01** (spec `docs/superpowers/specs/2026-08-01-landing-beyond-dictation-section-design.md`). Two problems, both about the site describing an app that isn't this one. **(1) Two false claims on `/docs/beyond`, fixed and deployed**: cross-lingual was documented as needing a cloud provider and a key — it does not (`CrossLingual.engineKind` forces on-device Whisper; `crossLingualUsesSarvam` needs enabled AND the toggle AND a saved key, so the default is offline and keyless); and meetings were documented as using "whichever engine and polish backend you have selected" — wrong twice, since `transcribeMeeting` hardcodes Apple + Whisper and `meetingSummaryBackends()` only ever appends Ollama and SystemLLM, making the true claim *stronger* than the one being made. Same shape as the FAQ that used to deny cloud transcription: **the site talking down its own privacy story.** The Privacy page had both right all along — only the docs page was wrong. **(2) The landing page sold only dictation.** Meetings, Memory, Reply Assist, Brain-dump, Cross-lingual, MCP, AI Polish, Vocabulary and History were invisible unless a visitor opened the docs, so anyone comparing OmWhisper to Meetily or Littlebird would conclude it has no meetings feature. New `BeyondDictation.tsx` between HowItWorks and FAQ: three cards (Meetings / AI polish / Cross-lingual — amended from an earlier AI-Polish/Vocabulary/History pick that predated SP2+SP3) over six compact rows, framed by "Every one of these is off until you turn it on", which turns a long feature list from bloated into restrained. **No screenshots, deliberately** — `app-screenshot.png` was retired in S6 after advertising a BETA badge and a stale model list for months; assets rot silently. **Copy constraint that must not drift: never "teach it your jargon" for Vocabulary** — engine biasing is *measured* inert on Apple and both Parakeet variants, so only replacement rules and fuzzy correction can be claimed. Verified by reading the deployed bundle: every new line present, `"your jargon"` absent. **Mobile layout is NOT verified** — `resize_window` reported success twice while `window.innerWidth` stayed 1920, a tool reporting success without effect. **Meetings competitive-parity SP1 — meeting identity — SHIPPED 2026-08-01** (spec `docs/superpowers/specs/2026-08-01-meetings-competitive-parity-design.md`, researched Meetily + Littlebird live that day; standing decisions recorded there: meetings stay on-device-only but Ollama counts as on-device, titles come from calendar+window-title, Q&A = MCP tools + one-shot in-app ask; three sub-projects SP1 identity → SP2 notes quality → SP3 intelligence, SP2/SP3 approved but NOT started). SP1 (plan `docs/superpowers/plans/2026-08-01-sp1-meeting-identity.md`, 6 tasks inline in a worktree): meetings.db v2 migration — `title`/`attendees`/`speakerNames` columns (GRDB Codable collections → JSON TEXT, proven by round-trip test) + FTS recreated to index title (synchronize() DOES repopulate from existing rows on create — migration test proves old rows stay searchable); stored transcripts keep immutable generic labels, `MeetingDiarization.applySpeakerNames` resolves names at read time (detail view, copy, summarizer input), so a rename propagates everywhere and re-transcribe just resets the mapping (fresh diarization labels aren't stable); title = calendar event by greatest overlap (all-day excluded, opt-in `meetingsCalendarEnabled` default off) → cleaned AX call-window title **captured at record START** (the window is gone by stop — auto-stop fires because it disappeared) → appName; attendees (EKEvent minus current user) double as rename-suggestion chips; rename popover on non-You speaker labels; Regenerate summary re-runs `MeetingSummarizer` over the resolved transcript without re-running ASR. +13 tests. Live-verified end-to-end in the dev sandbox (see Build section): recording→diarization→rename→calendar prompt→summary all green on real hardware. **Two live-caught bugs fixed same day, both worth remembering**: (1) **the calendars entitlement was missing** — Hardened Runtime silently blocked `requestFullAccessToEvents` (no prompt, no TCC entry, no error — the exact M1 mic-entitlement shape); fixed with `com.apple.security.personal-information.calendars` + `MeetingCalendar.requestAccess` now logs the error it used to swallow, so config-blocked and user-denied are distinguishable; (2) **blank summary card** — the model wrote the body on the heading line (`## Summary — text…`, a format the summarizer prompt's own template teaches), `AppMarkdown.sections` made the whole paragraph the title and the empty-section filter dropped everything, so a real 422-char summary rendered as an empty white card; parser now splits heading-line bodies (regression test uses the live payload). Also 2026-08-01: **default appearance is now Light** (R's call — Porcelain is the app's face; users who explicitly picked System/Dark keep their choice). **SP2 — notes quality — SHIPPED 2026-08-01** (plan `docs/superpowers/plans/2026-08-01-sp2-meeting-notes-quality.md`, 4 tasks inline in a worktree): 5 fixed-UUID summary **templates** (Standard/Standup/Client call/1:1/Interview — only the reduce prompt varies, Standard keeps `meetingWriteStyle`'s UUID so stored defaults survive) + user-authored `customMeetingTemplates` (separate list from `customPolishStyles` — a meeting structure is not a dictation style); `MeetingSummarizer.generate` parameterized with `template`/`chunkLimit`; **summaries route through Ollama when it's the selected polish backend** (`ollamaChunkLimit = 12_000` vs SystemLLM's 1,800 — an hour-long call goes from ~40 lossy chunks to ~6), with SystemLLM as primary otherwise AND as the retry when Ollama fails, and **Cloud never in the meeting path** whatever the polish backend; editable summary (raw markdown, `setSummary` writes summary only); per-run template menu on Regenerate + a Templates… sheet. **One real bug found en route, not in the plan**: `MeetingSummarizer`'s reduce stage did `prefix(1_800)` on the joined chunk summaries, **silently dropping the back half of a long meeting** before the summary was written — the identical bug Chronicler had already fixed and commented. Now collapses (re-summarizes the summaries) until they fit; the regression test traces the transcript's LAST word into the final reduce input, which truncation fails. Every template prompt also states "never put content on the same line as a heading" — the old prompt's own `## Summary — 2-4 sentences` example is what taught the model the format that rendered as a blank card. **Editable meeting details SHIPPED 2026-08-01** (spec+plan `docs/superpowers/{specs,plans}/2026-08-01-editable-meeting-details*`): `MeetingStore.setDetails` (blanks normalised to nil inside the method, so a cleared title falls back to appName and no caller can persist `""`), pure `MeetingDetails.parseAttendees`, an Edit-details popover, and a test pinning that re-transcribe's writes don't clobber typed details. Typed attendees feed SP1's rename chips for free. **Its spec records why the automated Outlook routes were all rejected on evidence** — worth reading before anyone re-proposes them: EventKit is blocked tenant-wide (`AADSTS7000112`, Apple Internet Accounts disabled by the Entra admin); Outlook's AppleScript `.sdef` is complete and the app answers pings, but under **New Outlook** a live probe returned **0 exchange accounts / 0 events**; New Outlook keeps no local calendar store (web client, WebKit caches only); and **Graph Explorer — Microsoft's own first-party tool — required admin approval for `Calendars.Read`**, so an unverified third-party publisher would face at least that. Microsoft Graph is specced-but-unbuilt, gated on an admin approving it. **SP3 — post-meeting intelligence — SHIPPED 2026-08-01, completing the competitive-parity plan** (plan `docs/superpowers/plans/2026-08-01-sp3-meeting-intelligence.md`, 4 tasks inline in a worktree): **`search_meetings` + `get_meeting` MCP tools** (`MCPServer.init` gained `meetingStore` with a `nil` default so pre-SP3 call sites compile; the store opens inside `MCPLauncher`'s existing access guard, so `mcpAccessEnabled` still gates it; `get_meeting` renders through `applySpeakerNames`, since "what did Alice say" is unanswerable from "Speaker 1"); **export** (pure `MeetingDetails.export(_:format:)` → markdown or plain text via `stripMarkdown`, `NSSavePanel` following `HistoryView.export`'s pattern) plus Copy summary, both under a new Share menu; **one-shot Q&A** (`MeetingSummarizer.answer` — map each chunk to question-relevant extracts, drop `NOTHING RELEVANT` ones, reduce to an answer; returns "That wasn't discussed in this meeting" rather than speculating) and **follow-up email drafting**, both through `AppState`'s existing meeting-backend selection so Ollama-or-System applies and **Cloud still never sees a recording**; three hidden fixed-UUID styles (…0010/0011/0012) deliberately absent from `builtInTemplates`, pinned by a test. UI: an ask card under the summary, a draft sheet that only ever copies (never sends). +6 tests, 399 total. **MCP tools verified against the real `--mcp` subprocess**, not just unit-tested — piping `initialize` + `tools/call` into the built binary returned a real meeting row and a detail payload with speaker names resolved and no raw labels; that dispatch path is invisible to the GUI. **SP2/SP3 verification pass 2026-08-02** (`Meetings/MeetingAIDiagnostics.swift`, `--diagnose-meeting-ai [id]`, DEBUG-only, stdout as the evidence channel like `MeetingDiagnostics`): those features shipped in 2.0.5 with tests over their pure pieces but no run against a real backend, because every entry point is behind a button. **PASS**: all 5 templates produce distinct summaries with correct per-template headings (the check fails if the template parameter is accepted and ignored); export markdown+text both carry the transcript with speaker renames applied; the follow-up email has a Subject line and real action items. **BUG FOUND AND FIXED — Ask refused questions the transcript plainly answered.** Two compounding flaws: when every extract was discarded `answer()` refused **without ever consulting the answering model** (on a 3,330-char transcript against a 12,000 limit that is ONE chunk, so the map step bought nothing and only added a failure mode — now falls back to answering from the transcript itself, still grounded because `questionAnswerStyle` is told to refuse when the material lacks the answer); and the discard test was `contains` anywhere, so an extract reading "Nothing relevant to the budget, but they discussed X at length: …" was thrown away whole (now length-bounded via `isNothingRelevant`). **It was non-deterministic** — the same question refused on one run and answered on the next — which is exactly why testing once would have missed it. +4 tests. **Model capacity is the remaining limit, not the code**: with `llama3.2:3b` the extract step returns "Nothing relevant." and everything refuses; with `gemma4:8b` the same transcript yields a 1,140-char extract and specific grounded answers naming who said what, while still refusing an unanswerable control question. **Ask/summaries need an ~8B model; a 3B model makes the feature look broken.** One comparison on one transcript — direction is solid, treat the threshold as unmeasured. **Three of my own diagnostic checks were wrong before any app bug was real** (export compared against the raw transcript when export applies `applySpeakerNames`; the text format also applies `stripMarkdown`; and the "answerable" question assumed a work call when the meeting is a podcast) — the WER-spike lesson again: fix the metric before believing the failure. **MCP `search_memory` was still keyword-only** while the hub had semantic search — the client with the most need for it ("what was I working on before lunch?") was the one doing exact-word matching; now routed through `hybridSearch` + `diversified` with `AppleEmbedder` (no app state, no asset), verified against the real `--mcp` subprocess. **Semantic index confirmed complete**: 1,844/1,844 snapshots, 4,050 passages, every vector 1,024 bytes (512 dims × float16). **Ollama's 30s ceiling killed the first call to any large model — found 2026-08-02 by switching to gemma4:8b on R's instruction.** Measured: **5.0s warm, 20.0s semi-warm, 36.4s from a genuinely cold model**, and Ollama evicts after ~5 minutes idle — so cold is the NORMAL case, not an edge case. The Standard template (the default, used by Transcribe & Summarize) is the first call of a run and failed every time. Two fixes: long-form paths (meeting summaries, chronicles) get `Ollama.longFormTimeout = 300`, while dictation keeps `dictationTimeout = 30` (there the fallback pastes the user's own words, so a slow model costs the polish and not the text); and **a timeout is no longer reported as "Couldn't reach Ollama. Is it running?"** — every URLSession failure was collapsed into `.unreachable`, sending people to check a service that was already up, the same misleading-diagnosis shape as "Apple Intelligence is off" when it is on. Proven both directions from a cold model: `THREW timedOut(30.0)` before, a 667-char summary after. **Two process lessons**: the honest error message paid for itself immediately — under the old code this would have read as "Ollama is down" and sent the investigation to the wrong place; and `MeetingAIDiagnostics` **duplicated** `meetingSummaryBackends()` instead of calling it, so it ran a DIFFERENT configuration from the one that ships and reported the fixed timeout as still broken — a harness that copies production logic will drift from it. **The recommendation to switch models would have made things worse without the re-run R asked for.** **CHRONICLE INPUT REDUCTION — shipped 2026-08-02** (spec+plan `docs/superpowers/{specs,plans}/2026-08-02-chronicle-input-reduction*`, 3 tasks on branch `chronicle-input-reduction`). Chronicles fed **every** snapshot to the model. Measured on the real store: **1,429 snapshots describe 117 windows across 26 fifteen-minute buckets, and two apps (Orca, Arc) are 80% of them** — ~715,000 chars, so **~60 sequential model calls for one paragraph**. A live run with qwen3.5 **did not finish in 20 minutes** and drove swap 10.6 GB → 25 GB on a 16 GB Mac. The key realisation: **the collapse loop was already discarding most of it** (it re-summarises summaries when they overflow), so the app was paying an 8B model at ~20s/call to throw away duplicates a cheap heuristic throws away better. `Chronicler.select` now keeps the most substantial capture per **(15-min bucket, app)** before anything reaches the model — chronological order preserved, cap **strides rather than truncates** (a `prefix(cap)` would silently drop the whole later day while still returning exactly `cap` rows; a test catches that), and an unparsable timestamp gets its own bucket rather than being dropped. **Window titles survive where bodies don't** — the kept block lists the other titles from its group, since titles are the highest-signal content per character. Chunking, the collapse loop and storage are **untouched**. Plus progress (`onProgress(completed, total)`) and **Cancel**, covering the automatic nightly run that previously gave no indication at all; cancelling writes nothing and raises no alert (`catch is CancellationError` in both `AppState` and the view) because it is a choice, not a failure. Rejected: **semantic clustering** over the passage vectors — principled and it reuses the embeddings shipped the same day, but it needs threshold tuning with no correct answer, loses time ordering, and would make chronicles depend on the semantic index being complete. Rejected: a flat evenly-sampled cap — weights a 5-minute burst the same as an hour of focused work. **MEASURED, same day / same machine / same model: 20+ min (did not finish) → 126 s**, and the chronicle got *richer*, not poorer — 2,749 chars still naming `en-IN`, `chunkLimit`, MCP tests and the CG-bounds-vs-`NSScreen` bug. That is the point: the model was previously losing detail across ~60 compression passes plus collapse rounds, so removing duplicates cheaply up front means **fewer lossy passes and more specifics surviving**. Suite 446 → 456. **The accepted cost, stated as a cost**: a genuine burst of distinct work inside one 15-minute window in one app collapses to a single entry; preserved titles soften it but do not remove it. **NOT verified**: the `Generating… N of M` counter and the Cancel button — measured via the scheduler, so the UI path (and that cancelling raises no alert and writes no row) has never been exercised. **Two UI bugs found by screenshot the same day**: the shared markdown renderer passed plain `String`s to `Text()`, so the model's own `**bold**` rendered as literal asterisks in **every** chronicle and meeting summary (`Text(.init(_:))` parses inline markdown; `AppMarkdown`'s warning against `Text(.init(wholeDocument))` is about block layout and does not apply per line); and the chronicle header showed only the day, so regenerating produced two visually identical headers — `createdAt` was stored all along and simply never displayed. **MEMORY CAPTURE BLOCKED THE MAIN THREAD — same-day regression, fixed 2026-08-02** (spec+plan `docs/superpowers/{specs,plans}/2026-08-02-memory-capture-off-mainactor*`). **The whole app froze**: global shortcuts, Fn push-to-talk and the UI all stopped responding. `MemoryCapture` is `@MainActor` and called `captureVisible` **synchronously**, so the main thread was blocked for up to 3s of every 5s poll — and live logs showed `tick budget spent` on EVERY tick, i.e. the full allowance burned each time. `GlobalHotkey`'s `NSEvent` callbacks, `PushToTalkMonitor` and SwiftUI all run there: the shortcuts were never broken, they were **starved**. Three rounds of debugging went into chasing a shortcut bug that did not exist. **Caused by visible-windows capture the same day** — it multiplied the work by display count and raised the budget 2s → 3s. Its spec called the time budget "the constraint that actually bites" and sized it carefully against the 5s poll, **but never asked which thread would pay it**. Bounding work per tick does nothing if that work blocks the main thread; `MemoryCapture`'s own header ("a lightweight poll, not a real-time audio path") had silently stopped being true. Fix: `tick()` reads settings on MainActor and hands the AX walk + store writes to a `nonisolated` function on the cooperative pool — **everything below `MemoryCapture` was already `nonisolated` and designed for exactly this** (`ScreenContextReader`'s header even says AX has no MainActor affinity) — with results hopping back for the callbacks and `Degradation` (which is `@MainActor`). **The in-flight guard is required BY the fix, not defensive**: the main-thread block was what accidentally serialised ticks, so off-thread a slow tick would overlap the next and stack concurrent AX sweeps. The flag clears **before** `finish()` and unconditionally — clearing only on the happy path would stop capture forever after one failure, silently. `memoryLog` and `maxContentLength` needed `nonisolated` markers under the project's MainActor-by-default isolation. Suite 470 → 473. **Live-verified**: tick thread IDs now VARY between ticks (they were identical before — the clearest evidence the work moved off main), capture kept writing rows, the guard fired exactly once when a capture overran and recovered on the next tick, and **R confirmed shortcuts respond again**. **The lesson worth keeping: a time budget bounds how much work happens, never which thread pays for it.** **Memory now captures VISIBLE windows, not just the focused one — shipped 2026-08-01** (spec+plan `docs/superpowers/{specs,plans}/2026-08-01-memory-visible-windows*`, 4 tasks inline on branch `memory-visible-windows`): the focused window plus the frontmost layer-0 window on each *other* display. `Memory/VisibleWindows.swift` splits pure selection (frontmost-per-display, layer-0/min-size/own-PID filters, centre-based display assignment — 9 tests) from `CGWindowListCopyWindowInfo`/`CGGetActiveDisplayList` enumeration; `WindowSnapshotReader.captureVisible()` captures focused-first inside ONE shared 3s tick budget (2s focused, 1s each other window) so monitor count can't push a tick past the 5s poll, and a single-display Mac skips enumeration entirely. **No new permission**: window bounds/pid/layer need none — only `kCGWindowName` requires Screen Recording, so it is never read and AX still supplies titles. AX exposes no window number, so a CG window is linked to its AX element **by matching geometry** (2pt tolerance); the `AXValue` unwrap goes through a `CFGetTypeID` guard rather than the force-cast the plan called for, since a third-party app returning something else would trap a daemon that runs every 5s. `MemoryCapture.tick()` loops, with domain exclusion moved INSIDE the loop so a second-display window is filtered independently. Settings copy corrected in both views — "the frontmost window" stopped being true, and understating what Memory records is not an option. **Live-verified with a check that could fail, both directions, same app/display/position — only the window title differed**: a TextEdit window on display 2 carrying a nonce string, never focused (Orca held focus on display 1), produced a row in the same tick as the focused window; renaming that same window to contain "Incognito" produced **zero rows over two minutes while Orca logged 10**, proving `isExcluded` runs per window on the second display. One methodology trap caught mid-verification: `lastSeenAt > datetime('now','-2 minutes')` **matches every row** — SQLite's `datetime()` returns `2026-08-01 13:58:42` (space) while `lastSeenAt` is `…T13:58:02Z`, and `'T' > ' '` lexicographically, so the WHERE clause was decorative. Use `strftime('%Y-%m-%dT%H:%M:%SZ','now',…)` when filtering these tables. **Not verified**: three-or-more displays, and the deferred-window path (`tick budget spent`) never fired on this two-display desk. **Memory exclusions — `.env` hardcoded + a user-editable Exclusions sheet — shipped 2026-08-01** (spec+plan `docs/superpowers/{specs,plans}/2026-08-01-memory-exclusions*`, 3 tasks inline on branch `memory-exclusions`). Prompted by the visible-windows live test capturing a real `.env` file open in TextEdit. **`.env` is hardcoded, not a setting** (`ScreenContextReader.isDotEnvTitle`) — matched at a path/word boundary (`.env`, `.env.local`, `docs/.env.example`, VS Code's `.env — project`; NOT `.environment-setup.md`), because a file whose whole purpose is holding secrets must not be capturable by forgetting to configure something; it covers the S2 dictation-context path too, which shares the gate. **User-editable: apps (bundle IDs, picked from running `.regular` apps by name), sites (the pre-existing domain list, moved in), window-title keywords** — one `ExclusionsEditor` sheet replacing `ExcludedDomainsEditor`, reached from Memory's ⋯ menu as "Exclusions…". `MemoryExclusions` is a pure `nonisolated struct` (apps + keywords only — **domains deliberately stay in `MemoryCapture.isDomainExcluded`, since a URL is only known AFTER the window is read**, so the spec's "three lists" line describes the UI, not the type). App/title checks run **before** `collectText`, and an excluded app is skipped before its AX tree is opened at all, so excluded content is never read rather than read-then-discarded; the bundle-ID check is deliberately duplicated on that fast path (worst case a wasted AX call, never a leak). Guarded: a **blank keyword must not match everything** — an empty needle is contained in every string, so a cleared row would otherwise silently disable all capture. A **pin test** asserts the hardcoded floor still applies with empty user lists — the only guard against a refactor routing capture through the user's list alone. Suite 430 → 437. **Live-verified both directions with two apps visible, so "zero rows" could not be confused with broken capture**: with Orca (focused, display 1) excluded and TextEdit (unfocused, display 2) not, Orca produced **0 rows over 55s** while TextEdit kept its row refreshed; removing the exclusion brought Orca back to **3 rows in 28s**. **NOT verified: the setters' live push** into `memoryCapture.exclusions` (adding an exclusion without relaunching). The sheet itself was verified by screenshot — and that caught a bug no test could: **a bare `Menu` inside a Porcelain card renders as blank space**, no chrome and no visible label, so the Apps section had no way to add anything. Fixed with `AISettingsView`'s existing idiom (`.menuStyle(.button)` + `.tint` + `.fixedSize()`). Also from that pass: the app list was running-apps-only (excluding Messages would have meant launching Messages), so a "Choose from Applications…" `NSOpenPanel` was added and `displayName` now falls back running → installed-on-disk → bundle ID; and the Memory settings bar's bare `ellipsis.circle` was replaced by labeled inline controls matching `HubMeetingsSectionView` — Exclusions is a privacy control and was undiscoverable behind a dot glyph, and a `Stepper` inside an `NSMenu` is clumsy. **Foundation Models gate fixed 2026-08-01/02**: `SystemLanguageModel.availability` reports `.available` on an `en_IN` Mac while `supportedLanguages` holds only 23 locales (`en-US`/`en-GB`/`en-AU`, **not `en-IN`**), so every generation threw `unsupportedLanguageOrLocale`. `SystemLLM.isAvailable()` checked only availability, so the app believed the model was usable — chronicles surfaced the raw framework error, and **polish surfaced nothing at all, because M3's fail-safe pastes the original text: Smart Dictation, Polish Selected, brain-dump and Reply Assist had been silently no-ops on such a Mac since M3 shipped.** `isAvailable()` now also checks language support (one gate, seven call sites), `unavailableReason()` names the real cause, and the user-facing strings no longer say "Apple Intelligence is off — enable it" when it is on. **Chronicles-via-Ollama shipped 2026-08-02** (spec+plan `docs/superpowers/{specs,plans}/2026-08-01-chronicles-ollama*`, 3 tasks on branch `chronicles-ollama`): `Chronicler.generate` gained `chunkLimit` (applied to the chunk pass, collapse loop and reduce input together); `AppState.chronicleBackends()` mirrors `meetingSummaryBackends()` — Ollama when selected (`ollamaChunkLimit = 12_000`), SystemLLM otherwise and as retry, **no `.cloud` case at all**; and — the load-bearing fix — `ChronicleScheduler` swapped its captured `PolishBackend` for a call-time closure, since it was handed `systemLLM` when Memory was enabled and its `isSuppressed` required `.system`, so **nightly chronicles were off for every Ollama user**. Unit test asserts 12k makes *strictly fewer* model calls than 1.8k — accepting the parameter and ignoring it would pass a "did it still write one" check. **Live-verified on the en-IN Mac where chronicles were previously impossible**: the button wrote one via Ollama (48 snapshots), and after relaunch the scheduler wrote the previous day's (1,436 snapshots, 732 chars) with nothing clicked; selecting System produced the correct "doesn't support your Mac's language (English (India))" alert. **Cloud-selected-produces-nothing is NOT yet tried.** A chronicles-only backend picker was proposed and **rejected on the merits**: the polish backend already expresses that choice, and a second control means either a hidden global change or a second concept — and then Meetings/Brain-dump/Reply Assist each want one. Instead the alert gained an "Open AI settings" button via a new `hubNavigate` environment value (view navigation, not `AppState` state), shown only for `backendUnavailable`. **One methodology note worth keeping**: a poll loop testing `[ "$(sqlite3 …)" != "0" ]` reported "SCHEDULER WROTE IT" when the query had actually failed with *database is locked* — an empty string is not `"0"`. The rerun distinguished empty/non-numeric from zero and printed the row as proof. |
| T1–T4 — Digital Twin (Phase 3) | ⬜ Planned | Same plan doc — after S5. Profile distillation → grounding → auto-vocab → habits. |
| D1–D4 — Hub Window / Porcelain (Phase D) | ✅ D1–D3 (live-verified) + D4a/polish/dark-mode + D4b shipped — Phase D complete (live-verify pass owed on D4b motion + dark mode) | D1 — Porcelain Foundations shipped 2026-07-10, per `docs/DESIGN_DIRECTION.md` and `docs/superpowers/plans/2026-07-10-d1-porcelain-foundations.md`, executed inline (6 tasks) in `.worktrees/d1-porcelain-foundations`. Design itself (palette, hub IA, component mapping) was already fully approved going in — `docs/hub-concept.html` (canonical color/behavior reference) and `.claude/skills/omwhisper-design/SKILL.md` (exact token table) — so this pass skipped brainstorming and went straight to an implementation plan. `UI/OmColors.swift` gained a `Color.Porcelain` namespace (bg/panel/panel2/ink/dim/hair/emerald/mint/teal/shadow + numeral gradient + accent tints), fixed values matching the design skill's table exactly, never adaptive to system appearance — same rule as the existing dark HUD tokens. `UI/OmOrbView.swift` gained a `struct OrbPalette` (`.dark`/`.porcelain`) and a `palette: OrbPalette = .dark` parameter, parameterizing the orb's layer stack (blob colors/alpha curve, blend mode, ring color/alpha, glyph under-copy, glyph luminance low/high colors) instead of forking the view — the Porcelain blob colors/alpha/ring formulas were copied verbatim from `hub-concept.html`'s own `orbTheme` JS object (not re-derived), and the `.dark` static instance's values are the exact pre-existing hardcoded constants, unchanged. Two pure functions (`lerpRGB`, plus `darkBlobAlpha`/`porcelainBlobAlpha`/`darkRingAlpha`/`porcelainRingAlpha`) are directly unit-tested (`OrbPaletteTests`, 9 tests) — the Canvas drawing itself isn't, matching this project's established position that SwiftUI Canvas/motion code is verified live, not by unit test. One subtle regression risk surfaced and was fixed during self-review before implementation, not after: the original plan draft had `drawReducedMotion`'s hardcoded `.omGlyphCore` naively become `glyphLuminanceColor(1)` (which resolves to `omGlyphPeak`, the *wrong* end of the range) instead of `glyphLuminanceColor(0)` (which correctly resolves to `omGlyphCore`, `palette.glyphLow`) — caught by checking the numeric mapping by hand rather than assuming the refactor was mechanical. `UI/PorcelainComponents.swift` (new): `omCard()` view modifier (white panel, 1pt hair border, 16pt radius, two-layer green-tinted shadow), `NavRow` (icon + title + optional badge, selected/hover states with a leading emerald→teal gradient bar), `SectionHeader` (uppercase tracked eyebrow + title) — all pure styling, no unit tests, matching this project's convention for SwiftUI view code (verified visually, not by test). `UI/DesignGalleryView.swift` (new, `#if DEBUG`-gated whole-file, mirroring `Meetings/MeetingSelfTest.swift`'s convention): renders the dark-vs-Porcelain orb side by side, a sample `omCard`, and a `NavRow` list — D1's own exit criterion ("a component gallery debug window renders all pieces"). `OmWhisperApp.swift` gained a debug-only `Window("Design Gallery", id: "design-gallery")` scene + `openDesignGalleryAction`/`openDesignGallery()` menu wiring, following the exact `Window("History", ...)`/`Window("Memory", ...)` pattern this project already established, plus the existing `#if DEBUG` self-test-menu-item convention. 9 new tests, all passing (205 total in the full suite) — critically, the full suite staying green confirms `OverlayView.swift`'s existing `OmOrbView(appState:)` call site (no palette argument) still compiles and behaves identically, since `palette` defaults to `.dark`. **Not yet live-verified**: the gallery window has never actually been opened and looked at in a running app in this pass (no Swift toolchain live-run in this environment) — whether the Porcelain orb genuinely reads as watercolor-on-white, the dark orb is pixel-identical to the live overlay HUD today, card shadows look right, and nav-row hover/selection states feel right are all real open questions, not confirmed. This is the one step needed before D1 is fully done, not before D2 (hub shell + migrations) starts. D2a — Hub Shell + Migrations shipped 2026-07-10, per `docs/superpowers/plans/2026-07-10-d2a-hub-shell-migrations.md`, executed inline (5 tasks) in `.worktrees/d2a-hub-shell-migrations`. D2 was split during this pass into D2a (this — build the real hub shell + migrate every section in, but touch nothing existing) and D2b (settings absorption + removing the old windows + menu cutover), matching this project's established pattern for risky multi-step milestones: D2a is purely additive, D2b is the actual point of no return, gated on D2a being seen live first. Scoping D2a required reading every view it migrates (`SettingsView`'s then-10 tabs, `HistoryView`, `MemoryView`+`MemoryChroniclesView`, `VocabularySettingsView`, `MeetingsSettingsView`, `ReplyAssistSettingsView`) and surfaced three real gaps the source design docs didn't cover, each resolved by applying the docs' own stated rules rather than inventing new ones: `ReplyAssistSettingsView` isn't in `DESIGN_DIRECTION.md`'s sidebar list at all (it postdates that doc) — added as a sidebar section since it's a feature toggle exactly like Meetings/Memory per the doc's own "content sections get sidebar slots" rule; `hub-concept.html`'s actual `<nav>` HTML omits Memory even though the mockup *has* a full Memory empty-state section (`v-memory`/`orbMem`) unreachable from that nav — treated as a mockup oversight, not a decision, since `DESIGN_DIRECTION.md`'s own ASCII sidebar diagram includes Memory; Home's content in D2a is a placeholder only, since the real dashboard is explicitly D3's job per `DESIGN_DIRECTION.md` §5's "New surfaces" vs. D2's "migrations" split. `UI/HubShellView.swift` (new): `HubSection` enum (7 cases: home/history/meetings/vocabulary/aiPolish/replyAssist/memory) drives a `NavigationSplitView` sidebar (D1's `NavRow` + a small Porcelain-palette `OmOrbView` brand mark) and a content switch — six of seven sections embed an existing view completely unchanged (`HistoryView`, `MeetingsSettingsView`, `VocabularySettingsView`, `AISettingsView`, `ReplyAssistSettingsView`), proving D1's components compose correctly with real, previously-shipped feature views rather than only D1's own demo content. `UI/HubHomeView.swift` (new): the D3-deferred placeholder. `UI/HubMemorySectionView.swift` (new): merges `MemorySettingsView`'s toggle/pause/retention controls (relocated into a compact top bar + overflow menu) with `MemoryView`'s Snapshots/Chronicles browse UI into one pane, per `DESIGN_DIRECTION.md`'s explicit instruction to migrate them together — `MemorySettingsView.swift` itself is untouched, still backing the old Settings tab's Memory row unchanged. `OmWhisperApp.swift` gained a `Window("OmWhisper", id: "hub")` scene + `openHubAction`/`openHub()` + a "Hub (Preview)…" menu item, following the exact `Window("History", ...)`/`Window("Memory", ...)` pattern already established — deliberately **not** `#if DEBUG`-gated (unlike D1's Design Gallery), since D2a's whole point is that the hub must be genuinely clickable and testable, not internal-only. No new tests (pure SwiftUI composition, matching D1's `PorcelainComponents.swift` precedent) — the existing 205 tests staying green is itself the regression proof that the untouched `Settings`/`History`/`Memory` scenes and their original menu items are completely unaffected, confirming D2a's "purely additive" constraint held. **Not yet live-verified**: like D1, the hub window has never actually been opened in a running app in this pass — whether the sidebar navigation feels right, each migrated section renders correctly inside the new shell, and the Memory section's settings-bar-over-browse-UI merge reads clearly are all real open questions. This is the step to close before D2b (the irreversible-feeling cutover) begins. D2b — Hub Cutover shipped 2026-07-10, per `docs/superpowers/plans/2026-07-10-d2b-hub-cutover.md`, executed inline (3 tasks) in `.worktrees/d2b-hub-cutover` — the user gave explicit go-ahead ("proceed, I trust you") to move straight from D2a into D2b without waiting for D2a's own live verification first, so D1 and D2 are now both shipped-but-unverified-live as a pair. `UI/SettingsView.swift` trimmed from 10 tabs to the 5 genuinely app-wide ones (General/Audio/Transcription/MCP/About — Vocabulary/AI/Meetings/Reply Assist/Memory already moved to their own hub sections in D2a), embedded as `HubShellView`'s new `.settings` `HubSection` case, rendered separately in the sidebar's footer below a divider (matching `hub-concept.html`'s `.side-foot` treatment) rather than the main content-section list; the fixed 720×440 frame that made sense for a standalone Settings window was dropped since it now flows inside the hub's own content area. Added a live privacy-status line to the sidebar footer — `hub-concept.html`'s static "🔒 All processing on this Mac" copy would have been actively misleading now that M4.2's CloudEngine is real, so it reads `appState.engineKind` and shows a different, honest line when Cloud is selected rather than copying stale mockup text verbatim. The actual cutover: removed the `Settings`/`Window("History")`/`Window("Memory")` scenes and their `openSettingsAction`/`openHistoryAction`/`openMemoryAction` plumbing entirely, collapsed four menu items ("Settings…"/"History…"/"Memory…"/D2a's "Hub (Preview)…") into one "Open OmWhisper…" (kept the `,` key equivalent). Blast-radius research before writing the plan (grepping every call site of `SettingsView(`/the three removed actions/`MenuContent` across both the app and test targets) turned up one real piece of pre-existing dead code — `UI/MenuContent.swift`, the original pre-AppKit-menu SwiftUI `MenuBarExtra` content view from the M0/M1 era, fully superseded since the "MenuBarExtra silently drops real clicks on macOS 26" fix but never actually deleted until now, when this plan's removals finally took away its last remaining reference (`openSettings`) too. No new tests (pure UI/menu wiring); the existing 205 tests staying green confirms nothing else in the app depended on what was removed. **Live-verification status**: neither D2a's hub shell nor D2b's cutover has been opened in a running app yet in this pass — this is now the single most important thing to check next: does "Open OmWhisper…" work end-to-end, does every migrated section (including the new Settings section) render and function correctly inside the hub, and does the whole app still feel coherent with the old window sprawl gone. D3a — Home Dashboard shipped 2026-07-10, per `docs/superpowers/plans/2026-07-10-d3a-home-dashboard.md`, executed inline (4 tasks) in `.worktrees/d3a-home-dashboard`. D3 split into D3a (this, the Home dashboard) and D3b (the menu-bar mini-panel), since the two share no code and have entirely different mechanics (SwiftUI content pane vs. `NSPopover` on the status item). Before writing the plan, read the old Tauri app's actual `get_stats_summary` (`omwhisper/src-tauri/src/history.rs`, the sibling repo checked out per `CLAUDE.md`'s "Working Alongside the Old Repo" convention) rather than assuming `DESIGN_DIRECTION.md`'s "port of the Tauri get_stats_summary" claim covered everything the new mockup needed — it turned out the old app's stats card only ever showed totals/today's *count*/streak, so only the streak-day algorithm (consecutive distinct days including today, walking backward, stopping at the first gap) was actually portable; "words today," "time saved vs. typing," and "speaking pace" are new computations designed here from the real `wordCount`/`durationSeconds` columns. `HistoryStore.homeStats()` (new) returns a `HomeStats` struct carrying those plus a 7-entry `last7DaysWordCounts` array for the quiet-reflection bar line; `speakingPaceWPM`/`minutesSavedVsTyping` are separate pure computed properties (clamped to never go negative) so they're unit-testable without DB I/O. `AppState.rePolish(_:)` (new, one line) reuses the exact same fallback-safe `polishedText(for:)` the live dictation path already calls — Home's "Re-polish" row action copies the result to the clipboard rather than pasting into the frontmost app, since sitting in the hub window gives no meaningful "target app". `HubHomeView.swift` rebuilt from D2a's placeholder into the real dashboard: time-of-day greeting (no user-name setting exists in this app, so it's nameless), a streak-days-aware sentence, three `omCard()` stat cards, the 7-day bar (static heights — the mockup's pulsing animation is D4's job), and the last 3 dictations with Copy/Re-polish. One real schema gap found and resolved, not silently worked around: `TranscriptionEntry` has no "destination app" field, so `hub-concept.html`'s per-row app icon has no backing data — recent rows show a generic icon + the entry's polish style/source instead, rather than adding a new column (real schema/importer scope, out of bounds for a dashboard task). Self-review before implementation caught one real bug in the plan's own draft: the test helper for backdating `createdAt` was drafted as a brand-new `#if DEBUG` production method on `HistoryStore`, when `HistoryStoreTests.swift` already had exactly this via its existing `seed()` helper (writing directly through the returned `DatabaseQueue`) — fixed to reuse it, avoiding an unneeded production-code addition. Two real build errors hit during implementation, neither anticipated by the plan: (1) the same `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` gotcha that's bitten `ParakeetEngine`/`MCPServer`/`CloudEngine` before it — the new `HomeStats` struct needed an explicit `nonisolated` (matching `TranscriptionEntry`'s existing pattern two lines above it in the same file) since it's constructed inside `HistoryStore`'s nonisolated `dbQueue.read` closure and compared directly in plain test functions. (2) `streakSentence`'s computed property had a `guard` statement before its `switch`, so Swift's implicit-return-for-single-expression-body inference didn't apply (that only fires when the switch is the *sole* statement in the body) — the switch's case expressions were silently treated as unused statements rather than a returned value, caught immediately by a real "missing return in getter" compile error; fixed with an explicit `return switch`. 10 new tests (`HistoryStoreHomeStatsTests` — real GRDB round-trips verifying the streak algorithm against several concrete day patterns, not just the empty-store case; `HomeStatsDerivedTests` — pure arithmetic), all passing (215 total in the full suite). **Not yet live-verified** — like D1/D2, nobody has actually opened the hub and looked at Home with real dictation history behind it in this pass. D3b — Menu-Bar Mini-Panel shipped 2026-07-10, per `docs/superpowers/plans/2026-07-10-d3b-menubar-mini-panel.md`, executed inline (4 tasks) in `.worktrees/d3b-menubar-mini-panel` — completes D3, and with it Phase D's committed scope through D3 (D4 motion/a11y polish is all that remains). The real work here was AppKit click routing, not SwiftUI: `AppDelegate` previously permanently assigned `item.menu = statusMenu`, which hands *every* click (left AND right) to the menu with no way to distinguish them — replaced with manual `button.action`/`sendAction(on: [.leftMouseUp, .rightMouseUp])` routing through a new `statusItemClicked()` dispatcher that checks `NSApp.currentEvent?.type`; right-clicks synthesize the traditional menu via the "assign `.menu`, `performClick`, un-assign" trick (a well-established AppKit pattern for exactly this dual-behavior case, so it never permanently hijacks left-clicks again), left-clicks toggle a `transient` `NSPopover` whose content is a fresh `NSHostingController(rootView: MiniPanelView(...))` rebuilt on every open — matching `menuNeedsUpdate`'s existing "read live state, never cache" principle, now applied to the popover too. `MiniPanelView.swift` (new): a 40pt Porcelain-palette orb + `miniPanelStateLine(for:)` (a small pure function, 4 directly-tested cases: Ready/Starting…/Listening…/Finishing…, extracted the same way `ParakeetEngine.mapUpdate`/`CloudEngine.parseServerMessage` extracted their pure decisions from effectful wrappers), an emerald→teal gradient Start/Stop button, the active polish style via a SwiftUI `Menu` (deliberately simplified from `DESIGN_DIRECTION.md`'s "click = cycle, right-click = menu" spec — true right-click detection inside a SwiftUI view hosted in an `NSPopover` needs custom AppKit gesture-recognizer bridging with no natural SwiftUI equivalent on macOS; a `Menu` satisfies the actual need without that complexity, documented inline as a deliberate simplification, not an oversight), the last dictation with copy, and an "Open OmWhisper" row wired through a closure back to `AppDelegate.openHub()` — `@Environment(\.openWindow)` doesn't work inside this view since it's manually hosted via `NSHostingController`, not part of the App's Scene graph. Menu-bar icon states (`updateIcon()`) were explicitly untouched per `DESIGN_DIRECTION.md`'s "already exist and stay." 4 new tests (`MiniPanelStateLineTests`), all passing (219 total in the full suite). **Live-verification status, and a real limit of this environment**: unlike D1-D3a's "haven't looked at it yet" gaps, this task changed *input routing*, not just rendering — whether right-click still shows the full menu correctly and left-click cleanly shows/hides the popover without visual glitches genuinely cannot be confirmed without a live run, which this environment can't do. This is the single most important thing to verify before Phase D's D1-D3 scope is considered fully proven, not just correctly built. **Live-verified on real hardware 2026-07-10** (by the user, via screenshots): the Home dashboard (greeting, stat cards, week bar, recent list) and the mini-panel popover (orb, Ready state, Start Dictating button, last-transcription card) both render correctly; the hub's sidebar navigation correctly reaches History, Meetings, Vocabulary, AI Polish, and Memory, each showing real live data (transcriptions, toggles, styles, captured snapshots). One real bug found and fixed in this pass: `ReplyAssistSettingsView` was the only settings view missing `.formStyle(.grouped)` — its `Form` painted no opaque background, so the hub's light Porcelain background showed through behind dark-mode system-label text, rendering it almost illegible; fixed by adding `.formStyle(.grouped)`, matching `MeetingsSettingsView`/`VocabularySettingsView`/`AISettingsView`'s existing pattern exactly. The screenshots also surfaced a real, un-fixed gap in D2a's scope: `DESIGN_DIRECTION.md` §4's migration principle calls this work a "re-skin + re-home, not a rewrite" — but D2a's plan interpreted "keep logic/store wiring untouched" as license to embed `HistoryView`/`MeetingsSettingsView`/`VocabularySettingsView`/`AISettingsView` completely unchanged, so those four sections still render in their original system-default/dark-mode-adaptive styling (plain `Form`/`List`, no Porcelain tokens) while Home and Memory correctly show the fixed, non-adaptive Porcelain palette — a visible inconsistency, not a functional bug. Not yet live-verified: right-click on the status item still showing the traditional `NSMenu` correctly, and a real start/stop dictation cycle triggered from the mini-panel's button. Closing the Porcelain re-skin gap on those four sections is folded into D4 (see below). **D4a — Porcelain re-skin shipped 2026-07-10**, per `docs/superpowers/plans/2026-07-10-d4a-porcelain-reskin.md`, executed inline (8 tasks) in `.worktrees/d4a-porcelain-reskin`. Re-examining the screenshots directly (not just the earlier paraphrase) before writing this plan found the real scope was bigger than "four sections" — `MemoryView`'s snapshot rows and `MemoryChroniclesView`'s day-list were *also* rendering in native List/Form's system-dark-mode chrome, not just History/Meetings/Vocabulary/AI Polish; all five needed the same fix. Presented the user a real tradeoff before starting: `List`/`Form` on macOS draw their own opaque materials that follow system appearance with no supported way to force them to a fixed hex color, and that native chrome is also what gives rows free keyboard navigation and VoiceOver traits — replacing it means rebuilding that accessibility floor by hand. User chose the full rebuild (`Rebuild everything to fixed Porcelain`) over the smaller "keep native, just guarantee legibility" option. `UI/PorcelainComponents.swift` gained `omRowCard()` (panel bg, 1pt hair border, 13pt radius, no shadow — ported verbatim from `docs/hub-concept.html`'s `.rowc` rule, deliberately lighter than `omCard()`'s two-layer shadow since it's for dense list rows, not standalone cards). `HistoryView.swift` and `MemoryView.swift`'s Snapshots tab: `List` → `ScrollView { LazyVStack { ForEach ... } }` of `omRowCard()`-wrapped rows, with `.onAppear`-driven pagination moved onto each row (identical trigger semantics to `List`). `MemoryChroniclesView.swift`: the day-picker sidebar's `List(selection:)` → a `ScrollView`/`LazyVStack` of custom rows using the same selected-state visual language as `NavRow` (`Color.Porcelain.accentTint2` fill) since a day-picker row is conceptually the same interaction. `MeetingsSettingsView.swift`/`VocabularySettingsView.swift`/`AISettingsView.swift`: `Form`/`Section` → `ScrollView` of `omCard()`-wrapped `VStack`s with small uppercase-tracked labels replacing `Section`'s native header; every `Toggle`/`Picker` kept its exact binding and gained `.tint(Color.Porcelain.emerald)`. Per the user's chosen direction, every row that used to be a bare `.onTapGesture` (which has no default accessibility trait and is not keyboard-reachable) became a real `Button` with `.buttonStyle(.plain)` and an explicit `.accessibilityLabel` — `HistoryRow`, `MemorySnapshotRow`, and the chronicle day rows — closing a real pre-existing a11y gap as a side effect of the rebuild, not just a cosmetic recolor. Every `.foregroundStyle(.secondary)`/`.foregroundStyle(.blue)` in the five files' row/section content was replaced with `Color.Porcelain.dim`/`.ink`/`.emerald` — no adaptive system colors left in the rebuilt paths. No new automated tests, matching this project's established convention that pure SwiftUI styling/layout is verified live, not unit-tested (`PorcelainComponents.swift`, `HubHomeView.swift`, `MiniPanelView.swift` set this precedent already) — the existing 219-test suite staying green after every task is the regression proof that no `@State`/`@Bindable`/action logic changed in any of the six views touched. One real SourceKit false-alarm during implementation worth noting since it looked different from this project's usual noise: several edits triggered "The compiler is unable to type-check this expression in reasonable time" on the new `ForEach`-inside-`LazyVStack`-inside-`ScrollView` blocks — a diagnostic class that (unlike "cannot find X in scope") can be a genuine macOS SwiftUI compiler limit, not just index staleness; a real `xcodebuild build` immediately after confirmed `** BUILD SUCCEEDED **`, so this batch was noise too, but it was verified rather than assumed given the different diagnostic shape. **Not yet live-verified** — like every other Phase D sub-project, nobody has opened the rebuilt History/Memory/Meetings/Vocabulary/AI Polish sections in a running app in this pass. Specific things to check live: all five sections now read as fixed light Porcelain regardless of system dark/light mode (the actual bug being fixed); History/Memory rows are still tap-to-expand and keyboard-focusable (Tab + Space/Return) now that they're real `Button`s; the nested Copy/Delete buttons inside an expanded row still receive their own taps correctly rather than the outer row-button intercepting them (a known SwiftUI nested-button interaction that wasn't previously exercised in this codebase); Vocabulary/AI Polish's `TextField`/`Picker` controls still edit correctly inside the new `omCard()` layout. D4b (motion/spring family, any remaining explicit VoiceOver hints beyond the labels added here, Dynamic Type check, `--dim`-on-white contrast verification) is not started — recommended as the next and final Phase D sub-project after this is live-verified. **Live-verification round 2 + polish + dark-mode shipped 2026-07-10** (all on `main`, no worktree — tight screenshot-feedback loop with the user). (1) Live screenshots caught that D4a missed `ReplyAssistSettingsView` and all five `SettingsView` sub-tabs (General/Audio/Transcription/MCP/About) — still native `Form`/`TabView`, dark-mode-adaptive — plus the sidebar rendered as *dark glass* because `.ultraThinMaterial` is an `NSVisualEffectView` vibrancy material that follows system Dark Mode regardless of the color layered under it; replaced with a fixed gradient. (2) Polish pass: black text fields (native `.roundedBorder` under Dark Mode) + a dark title-bar strip + ragged card widths all traced to the window inheriting system Dark Mode. Introduced `PorcelainPage`/`PorcelainSection` (uniform full-width cards, one component every settings screen uses), `porcelainField()` (panel2 fill + hair border, replacing `.roundedBorder`), a custom segmented `SettingsView` tab bar (no native dark tab bar), and stronger nav badges. (3) **System dark-mode support (design reversal, user decision 2026-07-10)**: the original approved rule was "Porcelain = always light, never a dark app window" — the user reversed it, asking the hub to adapt to system theme. Rather than force `.aqua` (the interim fix), made `Color.Porcelain.*` themselves **adaptive** via `porcelainAdaptive(light:dark:)` (dynamic `NSColor`): light porcelain in Light Mode, the brand's dark emerald-on-green-black identity (reusing the existing `om*` HUD token family) in Dark Mode — one change point, every hub component adapts for free. Removed the forced-`.aqua` window/popover pins (window now follows system); kept the transparent title-less top edge. The Canvas orb isn't token-driven, so `HubShellView`/`MiniPanelView` pick its `.dark`/`.porcelain` palette from `@Environment(\.colorScheme)`. Dark `dim` brightened above the HUD's `omDim` for AA-legible captions on the darker hub card. The overlay HUD stays hard-dark via `om*` regardless — only hub tokens adapt. Design skill's "Scope rule" updated to document the reversal so future UI work builds with adaptive tokens, not a pinned appearance. 219/219 tests green throughout. Live-verified by the user via screenshots: light mode reads clean (uniform cards, light fields, no dark strip); the dark-mode variant is built-and-tested but its live look is the next thing to eyeball. **Appearance picker shipped 2026-07-10** (straight to main): an in-app System/Light/Dark override (`AppState.appearancePreference`, UserDefaults-backed, default `.system`) at the top of Settings → General, since the adaptive tokens alone only followed the system with no way to force a theme. Applied via `.porcelainWindow(colorScheme:)` (sets the window's `NSAppearance` so the adaptive `NSColor`s + native controls + titlebar honor the override) + `.preferredColorScheme`; the mini-panel popover appearance + `MiniPanelView` match; the orb palette comes from an `effectiveScheme` that resolves `.system` against the live scheme. **D4b — motion + a11y polish shipped 2026-07-10**, per `docs/superpowers/plans/2026-07-10-d4b-motion-a11y.md`, executed inline (7 tasks) in `.worktrees/d4b-motion-a11y` — the final Phase D sub-project. Motion design was pre-approved via the design skill's §2 (calm-motion philosophy, the 40th-dictation test, one spring family, Reduced Motion non-negotiable), so this went straight to a plan like D1. `PorcelainMotion` (new, `PorcelainComponents.swift`): one spring (`.spring(response: 0.35, dampingFraction: 0.82)` — the skill's "350ms with slight overshoot") + `resolved(reduceMotion:)` returning nil under Reduced Motion so every caller degrades to no-animation from one place. Applied to: `NavRow` selection/hover (spring-animated fill + a scale/opacity transition on the gradient bar, reading `@Environment(\.accessibilityReduceMotion)` itself); the hub content pane (`.id(selection)` + `.transition(.opacity)` + spring `.animation(value: selection)` so sections cross-fade instead of hard-cutting); the Home 7-day bar (a calm ~5s ambient "breath" on the non-zero bars — the one deliberate non-spring animation, since a spring can't loop, `ponytail`-marked; static under Reduced Motion). The numeral count-up the D3a mockup planned was **deliberately dropped** — a number animating up on every Home open is exactly the "entrance fanfare" the 40th-dictation test forbids. A11y: the sidebar nav rows were `.onTapGesture` (no accessibility trait, not keyboard-reachable) — a real gap in the *primary* navigation — converted to real `Button`s with `.accessibilityLabel` + `.isSelected` trait (keyboard Tab/Space + VoiceOver now reach every section). Two WCAG AA-normal contrast fixes verified with a WCAG-ratio calc, not eyeballed: light `dim` `#66796F`→`#607269` (4.41→4.86 on the `#F7FAF8` canvas, was AA-large-only) and light `mint` `#0D9488`→`#0B7D73` (3.74→5.01 on white, used for the small Copy/Re-polish labels); dark values already passed 7.0+ and were untouched. Dynamic Type: fixed `.font(.system(size:))` is the design (skill §3) — audited for clipping (`grep lineLimit\|frame(height:)`), the only `lineLimit` uses are intentional truncating previews and no fixed-height *text* container clips, so no code change. One real build error, not a SourceKit false alarm: the env key is `accessibilityReduceMotion`, not `accessibilityReducedMotion` (the plan had it wrong) — caught by a real "cannot infer key path" failure, fixed across all three views. 219/219 tests green after every task (all motion/styling is verified-live convention, no new unit tests). **Not yet live-verified**: the motion (nav spring, content cross-fade, breathing bar), Reduced-Motion degradation, and keyboard/VoiceOver nav all need a real run to confirm they feel calm (not "loud") and actually degrade — plus the dark-mode variant's live look is still owed from the prior pass. |
| F1–F7 — Frontier (Phase F) | 🔶 F4 + F5 shipped; F1/F2/F3/F6/F7 planned | **F4 cross-lingual** (`CrossLingual.swift` + `Transcription/SarvamEngine.swift`) and **F5 brain-dump** (`UI/HubBrainDumpSectionView.swift`, ⌘⇧D) are live and in the hub sidebar — this row read ⬜ Planned until 2026-08-01. Note for copy: cross-lingual runs **fully on-device** (`CrossLingual.engineKind` forces Whisper); the cloud `SarvamEngine` is auto-selected only when a Sarvam key is saved, so it needs no key and no network. | `docs/FRONTIER_FEATURES_PLAN.md` — post-2.0 waves 2.1/2.2/2.3 after M3/M4. |

## Deliberately Not Supported

Windows · Moonshine engine · a VAD settings UI · a general model-manager UI (beyond the optional
Parakeet/Whisper downloads) · an auto-punctuation toggle (it is always on, in-engine).

## Build

```bash
# Build + test
xcodebuild -scheme omwhisper-native -project omwhisper-native.xcodeproj build test

# Release (archive → notarize → dmg)
bash scripts/build-release.sh
```

**Dev-build isolation (2026-08-01,** spec `docs/superpowers/specs/2026-08-01-dev-build-isolation-design.md`**):**
Debug builds are **OmWhisper-Dev** — `PRODUCT_BUNDLE_IDENTIFIER = com.omwhisper.mac.dev`,
product `OmWhisper-Dev.app`, display name "OmWhisper-Dev", `PRODUCT_MODULE_NAME` pinned to
`OmWhisper` so `@testable import OmWhisper` keeps compiling. Consequences, all deliberate:
separate UserDefaults/Application Support dir/Keychain service/TCC identity from the
installed app, Sparkle never starts (`isDevBuild` gate), the single-instance guard doesn't
cross-match — so ⌘R runs safely **beside** installed OmWhisper and can never touch its
databases (the immediate trigger: testing an unreviewed `meetings.db` migration against real
data). The test suite runs as the .dev app too, so tests can no longer reach real user data
or Keychain keys. All data paths route through bundle-ID-aware `AppSupportDirectory.resolve()`
— **never hardcode `com.omwhisper.mac` in a data path** (MeetingRecorder and ToneProfile used
to; consolidated). Release config verified untouched in both directions (`-showBuildSettings`
+ built Info.plists). One scheme as before: Run/Test = Debug = OmWhisper-Dev, Archive =
Release = the real app.

Requires: Xcode with macOS 26 SDK, a paid Apple Developer account for notarization, and
`APPLE_SIGNING_IDENTITY` / `APPLE_ID` / `APPLE_ID_PASSWORD` (app-specific password) /
`APPLE_TEAM_ID=Y87BZN47C5` set in the environment or a local `.env` (gitignored).

